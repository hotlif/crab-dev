[build]
output = "./src/token.ts"
prefix = "color-picker"

[token]

panel.padding = "16px"
panel.gap = "1rem"

panel.preview.height = "5rem"
panel.preview.width = "100%"
panel.preview.border.radius = "8px"
panel.preview.margin.top = "1rem"

panel.slider.container.gap = "1rem"