+++
title = "Theme 主题样式"
description = "通过语义令牌提供浅色、深色和强制颜色模式的主题样式"
kind = "component"
status = "experimental"
+++

# Theme 主题样式

`@crab-dev/rc-theme` 是 CSS-only 主题包，提供 L2 主题变量：

[打开组件工作台](/components/rc-theme/workbench/)

```sh
yarn add @crab-dev/rc-theme
```

普通组件随模块引用自动加载 CSS；仅声明本包依赖不会触发加载。本仓库在 Wake Docs 中配置主题入口，由 Wake 加载，无需手写 CSS 副作用导入：

```toml
# .website/wake.config.toml；路径相对于文档项目根目录。
[docs]
theme_css = "docs/theme.css"
```

`theme_css` 文件必须位于文档项目内。本仓库的 `.website/docs/theme.css` 通过 CSS 引用公共主题：

```css
@import "../../components/rc-theme/css/index.css";
```

其他构建器需要自行加载本包公开的 `css/index.css`；自动加载是 Wake 的构建能力。

默认主题为 Light。Dark 主题由 `data-theme` 驱动，并允许在页面局部建立主题边界：

```tsx
<main data-theme="dark">...</main>
```

## Light / Dark 并排示例

<Demos glob="./demos/*.demo.tsx" columns={1} />

## 避免首屏主题闪烁

主题包不会监听 `prefers-color-scheme`，也不会读写持久化状态。应用应自行决定主题，并在应用脚本执行和
首屏渲染之前尽早把 `data-theme="light"` 或 `data-theme="dark"` 写到 `document.documentElement`。
例如，可把下面的短脚本放在文档 `<head>` 中、主 bundle 之前：

```js
const savedTheme = localStorage.getItem("theme");
document.documentElement.dataset.theme = savedTheme === "dark" ? "dark" : "light";
```

如果应用选择跟随系统，也应由应用读取 `matchMedia("(prefers-color-scheme: dark)")`、持久化用户选择并
设置 `data-theme`；`@crab-dev/rc-theme` 只负责 CSS 变量映射。运行时切换只需更新同一属性。

主题包仅覆写 `--token-semantic-*`，所以组件级变量仍拥有最高优先级。`forced-colors: active`
会自动切换到 `Canvas`、`CanvasText`、`Highlight` 等系统颜色。

## 公共默认品牌与来源

Light 的品牌默认 / 悬停 / 按压为 Purple 40 / 30 / 20，其上文字为 White。Dark 从 Purple 80 经 80 与 90 的等量 OKLCh 混合过渡到 Purple 90，其上文字为 Purple 20；按压时仍保留品牌色相。表面与文本继续使用公共 Zinc 中性色，功能反馈分别使用 Red、Green、Amber、Blue。切换主题只改变 L2 映射，不会把 L1 原始色阶反转。

需要从一个颜色快速换肤时，使用 [ConfigProvider 的 brandColor](/components/rc-config-provider)。它会成对生成明暗模式的品牌角色并覆盖当前边界的 L2 变量，支持嵌套继承与局部重置。该可选能力由 ConfigProvider 提供，本包保持 CSS-only。

唯一配对数据位于 `@crab-dev/rc-token-semantic` 的 `themeColorContract`，本包编译为 CSS。文档站与独立工作台使用同一来源；站点的字体和阅读布局仍属于站点自身，不是公共组件默认尺寸。

## 局部主题与浮层

`[data-theme="dark"]` 可以嵌套在浅色页面中，也可以用 `[data-theme="light"]` 建立浅色岛。嵌套边界重新声明自己的 L2 值，因此只在外层祖先定义的业务覆盖不一定穿过内层主题边界。

浮层若通过 Portal 挂载到 body，已经离开局部主题祖先。应在浮层的已有根或挂载容器上同步主题和业务覆盖；不能只观察触发按钮的颜色。当前页面主题切换时，已打开浮层也要更新。主题包只提供选择器和变量，不管理应用状态或 Portal 生命周期。

## 保留业务品牌覆盖

通过 `TokenVars` 中的正式 L2 和 L3 变量覆盖品牌色；历史别名需要先迁移，参见[正式变量迁移](/components/rc-token-semantic)。把业务覆盖放在公共主题样式之后，并使用应用自有类与主题属性共同限定范围。例如下列片段只展示 Light 的中性主按钮配对；完整业务主题还要分别评审链接、焦点、选中和 Dark 配对。

```tsx
import { css } from "@crab-dev/css";
import global from "@crab-dev/rc-token-global";

const existingBrand = css`
    &[data-theme="light"] {
        --token-semantic-color-brand-primary: ${global.zinc[900]};
        --token-semantic-color-brand-primary-hover: ${global.zinc[800]};
        --token-semantic-color-brand-primary-active: ${global.zinc[950]};
        --token-semantic-color-text-on-brand: ${global.white};
    }
`;
// 将 existingBrand 与 data-theme="light" 同时放到应用主题根。
```

## 迁移检查

1. 将旧变量名和旧 `vars` 键迁移到 `TokenVars` 中的正式名称，再比较默认、悬停、按压三种主按钮与前景。
2. 检查链接、焦点、当前导航与选中数据；新默认不是把所有强调角色替换成同一个紫色。
3. 打开 Dialog、Drawer、Select 和 Tooltip 后切换主题，再关闭并检查焦点恢复。
4. 检查局部深色、浅色岛和已有 L3 覆盖。强制颜色下必须保留系统颜色，业务覆盖也须提供该模式的等价处理。
5. 运行令牌生成、契约测试和受影响包测试；在浏览器记录完整状态。公共映射测试通过不代表整个应用已符合 WCAG。

相关规范：[色彩配对](https://material-web.dev/theming/color/)、[无障碍](/guides/accessibility)、[令牌实施](/components/rc-token-semantic)。
