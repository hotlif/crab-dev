export const meta = {
    title: "基础用法",
    description: "最基础的空状态，使用 `preset` 选择预置场景",
};

import Empty from "../../src/index.js";

const BasicDemo = () => {
    return <Empty />;
};

export default BasicDemo;
