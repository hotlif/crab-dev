# @crab-dev/rc-breadcrumbs

展示页面层级，并提供返回上级页面的入口。

## 主要功能

- 通过 `items` 配置层级，通过 `href` 或 `onClick` 处理导航。
- 支持自定义分隔符和折叠过长路径。

## 文档

- [使用说明与 API](./docs/index.mdx)
- [示例源码](./docs/demos/)

在当前包目录运行 `yarn start` 可打开组件工作台，查看交互示例。
