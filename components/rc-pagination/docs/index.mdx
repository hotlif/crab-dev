+++
title = "Pagination 分页"
description = "切换页码、调整每页条数，并展示数据总量"
kind = "component"
status = "experimental"
+++

# Pagination 分页

切换页码、调整每页条数，并展示数据总量。

[打开组件工作台](/components/rc-pagination/workbench/)

## 何时使用

- 列表 / 表格 / 卡片墙需要按页加载大量数据
- 后端分页或前端切片分页，向用户暴露页码导航
- 需要在不同密度场景（表格内联 / 独立页面 / 抽屉底部）切换尺寸

## 设计要点

- 根据页面密度选择尺寸，分页控件应靠近对应的数据区域。
- 页数较少时显示完整页码，较多时通过省略号和跳转控件缩短导航。
- 保留悬停、聚焦、按下和禁用反馈，并遵循减少动态效果的系统偏好。

## 代码演示

<Demos glob="./demos/*.demo.tsx" columns={2} />

## API

<API source="../src/types.ts" symbol="PaginationProps" component="Pagination" />
