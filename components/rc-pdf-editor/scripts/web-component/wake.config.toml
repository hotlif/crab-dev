# Wake 0.1.45 browser bundle defaults to development defines, even with minify.
# Keep this production override separate from component tests and Docs.
root_dir = "../.."

[define]
"process.env.NODE_ENV" = '"production"'
