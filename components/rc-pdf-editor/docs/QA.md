# 0.0.1 验收记录

验收环境：Windows、Node.js 22、Yarn 4 PnP、Chromium，2026-09-21。

## 自动验证

2026-09-22 插件与跨页区域追加验收：

- 包内 `lint` 0 errors / 0 warnings、`typecheck`、`generate:token`、`build:library`、`generate:docgen` 通过。新增接口、令牌、ESM/CJS 声明和 Worker 产物均由命令生成。
- 全包 9 套件 79 项测试通过；随后结果回调加入异步等待/取消信号和公开旋转角度说明，定向重测插件与会话 17 项通过（测试总数现为 80 项）。取消按钮外观及等待反馈调整后，编辑器 29 项交互测试通过。无新增 act 告警或资源泄漏报告。
- 真实 PDFium 验证四种页面旋转及 CropBox 下，区域渲染与完整页面对应位置逐像素一致；会话测试解码实际 PNG，验证拼接尺寸、页面坐标映射、版本失效、取消、只读和图像预算。
- 交互测试覆盖跨页/反向拖动、不同页宽、滚动期间的文档坐标、键盘扩选、取消和布局变化；插件测试覆盖多编辑器隔离、安装/清理、动作命名空间、重排、文档版本取消、迟到 OCR 结果和异步结果交付失败。
- 文档站 `generate:docs`、`check:docs`、28 项 `test:generator`、`typecheck` 通过，Demo 基数更新为 257。

独立 Demo 浏览器核对：深色桌面实际采集第 1 页底部 80pt 和第 2 页顶部 80pt，得到 1200 × 320 PNG，两页文字内容正确、无界面页缝；坐标展开区显示两个连续的 1200 × 160 图像块。键盘跨页扩选显示两个独立选框，页间空隙不着色，Enter 完成、Escape 取消，焦点回到画布。390 × 844 下插件入口正常换行，取消按钮完整可达；选择期间卸载清除选区与动作，重新安装后可以再次采集。等待用户框选时不显示“正在处理”的旋转指示。

新增动作采用已有 standard icon button，取消采用自有 Button text 变体，安装使用 Checkbox；实际查看了 M3 Buttons 的 guidelines / specs / accessibility、States 概览，以及 Material Web Button 文档、实现、Text 样式和令牌，链接汇总在 [PLUGINS.md](./PLUGINS.md#视觉与交互依据)。M3 当前按钮规范含 Expressive 配置，Material Web 是基线实现；本次复用已有 small/round 文本按钮，不把旧图标按钮体系宣称为完成全量 Expressive 迁移。跨页选框属于文档工作区扩展，未宣称为 M3 原生组件。未对本轮选框进行全主题、屏幕阅读器或移动端触控专项验收；鼠标拖动覆盖由组件测试提供，真实浏览器本轮以键盘跨页操作核对。

Demo 不含 OCR 模型或识别后端，仅交付真实图像、元数据和可注入识别接口；没有进行识别准确率评测。完整能力与接入边界见 [PLUGINS.md](./PLUGINS.md)。

2026-09-22 API 重设计追加验收：包内 lint（0 errors / 0 warnings）、typecheck、7 套件 64 项测试、build:library、generate:docgen 均通过；最后导航调整另复测 24 项编辑器交互。文档站 generate:docs、check:docs、typecheck 通过。完整缺陷证据、破坏性变更与能力边界见 [API-REDESIGN.md](./API-REDESIGN.md)。以下记录保留为此前功能验收历史。

- 编辑器：`generate:token`、`generate:docgen`、`lint`、`typecheck`、`test`、`build:library` 通过。共 36 项测试：15 项真实 PDFium 引擎测试、13 项 Worker / React / WebGL 测试、3 项鼠标绘制测试、5 项页面选择与拖拽测试，无 act 告警及资源泄漏报告。
- Virtual：102 项测试通过，新增逻辑像素定位、滚动通知及边界调整验证；lint、类型检查、令牌/API 生成和库构建通过。
- Canvas：119 项测试通过，覆盖图片和字形纹理释放、实际绘制完成通知及上传失败保留旧图。令牌生成、API 生成、lint、类型检查和库构建通过。
- 文档站：28 项生成器测试、62 项组件测试、构建绑定检查器测试、文档一致性检查、lint、类型检查及生产构建通过。
- `yarn install --immutable` 通过；保留仓库原有 peer dependency 提示及 Yarn PnP ESM 实验性提示。新包未新增 peer 冲突。
- `yarn pack --dry-run` 确认包含 Worker、WASM、声明和 PDFium 许可证；测试字体、PDF 和测试用 WASM JSON 不进入发布包。

真实引擎测试覆盖：中文 TTF 嵌入及 Unicode 回读、原文字内容/字号/颜色/位置修改、透明图片增删替换、四个页面旋转方向、CropBox 坐标、重叠对象绘制顺序、页面增删排序合并提取、连续 50 步撤销重做、失败回滚、密码及权限、保存状态。测试夹具为仓库自行构造的 PDF 和最小 TTF；TTF 字形只用于验证编码与嵌入，不作为产品字体。

交互测试覆盖：真实打包 Worker 初始化与关闭、无效 WASM、对象列表入口、键盘移动、撤销、只读、打开失败保留文档、输入框及中文组合输入不抢占快捷键、保存回调失败保留修改。

连续预览回归覆盖：滚轮浏览不改变缩放、远页定位后读取实际画布像素、Home/End 导航、移除分页、只读定位及固定三个预览 Canvas（共享背景加当前页双缓冲），20 页夹具不增加预览画布数量。

缩放回归覆盖：模拟 ResizeObserver 验证对象选择、侧栏/窗口尺寸变化和翻页保持手动倍率，加载失败保留倍率，新文档及显式适应正确重置；Ctrl/Command 滚轮阻止默认浏览器缩放，合并同一帧输入，兼容像素/行/页增量，限制最小/最大倍率，卸载取消待处理帧。真实 WebGL 蓝色区域的中心在鼠标锚点缩放前后偏差小于 1 px；只读模式同样可用。

闪屏回归覆盖：主动延迟 Worker 响应及实际 rAF 绘制，逐帧读取 WebGL 像素，确认旧页在解码、纹理上传、新帧绘制前持续可见；切换后才关闭旧位图，且两块 Canvas DOM 节点持续复用。渲染失败保留旧画面并支持重试；过期响应不会覆盖新版本。绘图提交成功后由新帧接管，失败只移除待提交预览。稳定页面标识随排序、删除和撤销重做保持一致，未修改页复用缩略图缓存。

页面选择与排序测试覆盖：移除复选框、Ctrl/Command 切换多选、全选/取消、键盘导航、只读选择、批量操作禁用态、不连续选页整体移动、原地拖动不产生历史、每次拖动仅提交一次、Esc/指针取消/版本切换取消拖动、边缘自动滚动及列表外松开。真实 PDFium 导出回读验证批量移动的页面内容顺序，以及单步撤销和重做。

绘图测试覆盖：矩形、椭圆、直线、箭头和手绘在四种页面旋转及 CropBox 下保存为原生路径，回读后仍可修改；半透明描边/填充更新及图层像素；水平线/竖线选择、变换、删除、撤销重做；鼠标坐标转换、Shift 约束、取消指针、Esc、切换工具、只读和每笔只提交一次。真实 WebGL 纹理像素回读验证位图没有重复乘透明度。

## 浏览器核对

- 文档站生产资源能够初始化 Worker/WASM，打开本地 8 页 PDF；首次页面列表定位、缩略图、翻页和属性选择正常。
- 实际提交原文字字号修改，验证主画布、缩略图及历史状态同步；配置本地 TTF 后将原对象内容修改为中文。
- 浅色及深色桌面工作区、选中对象控制柄、前景图形与文字叠放已目视核对；修改过程中没有原文字重复绘制。
- 390 × 844 视口下验证工具栏图标换行、侧栏折叠/展开、页面导航和撤销，验收后恢复默认视口。
- 设计参考及 Material Web 缺少 Toolbar 对应实现的差异见 [README](./README.md#键盘与布局)。容器采用 M3 标准配色的顶部停靠工具栏，控件复用自有组件。
- 闪屏修复后在独立 Chromium 工作台连续修改字号、撤销/重做、鼠标绘制矩形及往返翻页，主画面保持可见，未修改页面的缩略图保持显示；该 8 页夹具在当前视口下始终为 8 个 Canvas 节点（主画布 2 个、3 个虚拟缩略图各 2 个），控制台无 warning/error。需先构建 `rc-canvas` 再启动消费方工作台，已启动的开发服务可能仍持有旧依赖模块。
- 本次另行构建组件工作台生产版本，实际加载随包发布的 Worker/WASM、打开 8 页夹具并提交原文字字号修改；文字选中状态、主画布和缩略图均更新，控制台无 warning/error。文档站生成同步及一致性、类型检查通过。
- 连续预览调整后，在 Chromium 工作台验证滚轮上下浏览、相邻页同时显示、缩略图定位、End 末页定位及滚动时对象树/当前页同步；滚动到第二页后修改字号，正文、缩略图及历史同步更新。100% 缩放下连续显示相邻页面，普通滚轮不改变倍率，控制台无 warning/error。分页控件已移除，底部保留页码状态、缩放和适应页面。
- 390 × 844 下连续滚动到第三页，侧栏默认折叠，当前页和倍率显示正确，控制台无 warning/error；核对后恢复默认视口。连续预览版本的组件工作台生产构建通过。
- 缩放修复：实际复现 8 页夹具设置 250% 后选择文字又回到 173%；原因是属性展开使测量尺寸变化，触发自动适应。修复后在 13 页 PDF 中设为 100%，选择文字、收起属性和普通滚轮浏览均保持 100%，控制台无 warning/error。
- 紧凑底栏：Chromium 实测百分比字段 72 × 32px、适应页面图标按钮 40 × 40px，底栏由 72px 降为 48px；390 × 844 下仍为单行 48px。核对禁用态、图标、焦点、中文提示，输入 250% 后 Tab 到图标并按 Enter 可重新适应，控制台无 warning/error。沿用自有字段和标准图标按钮，密度扩展及 Material Web 对照链接见 README。

## 适用边界

对象树与图标工具栏调整：页面对象改用 `rc-tree`，工具栏改用带中文可访问名称和 `rc-tooltip` 的自绘图标按钮；图标为 24px，点击区域至少 48px。新增回归断言覆盖树内方向键只改变选择、不移动 PDF 对象，以及非拖拽节点的可访问状态。

页面操作改为直接显示在顶部工具栏，移除“更多操作”及其弹窗；窄屏下重做、文字、图片和全部页面操作均可直接访问。桌面及 390 × 844 视口已核对图标、禁用态、中文提示与换行；实际通过工具栏完成插页、旋转、前移和删除确认。回归断言验证页面操作位于工具栏，以及只读时修改禁用、提取可用。

页面缩略图交互调整：复用 `rc-button`、`rc-virtual` 和 Canvas 缩略图，去除页面复选框。Chromium 工作台浅色模式实际 Ctrl 多选前两页，将两页同时拖到第三页之后，确认当前内容仍为原第二页且位置更新为第三页；一次撤销恢复原顺序和未修改状态。核对 Ctrl+End 跨虚拟列表定位末页、空格增选，以及 390 × 844 下侧栏展开/折叠、多选保留、缩略图和居中页码；控制台无 warning/error。页码继承按钮文字颜色，保证选中状态使用对应的前景色。M3 List 与 Material Web 参考及缩略图拖拽扩展差异见 README。

绘图工具栏调整：在 Chromium 工作台实际拖动绘制五种图形，选择图形修改线宽、虚线和半透明填充，核对主画布与缩略图、对象树和历史同步；修复选中半透明图形变暗的问题。核对 390 × 844 窄屏的图标换行与侧栏折叠，检查抓手平移及撤销重做；浏览器控制台无新增 warning/error。

本次浏览器验收使用 Chromium，尚未在 Safari、Firefox 或触屏设备上验收。复杂文档的字距、字体度量、裁剪或嵌套对象仍受 README 所述首版限制；这些测试不代表任意来源 PDF 都能可靠修改。内容编辑必须使用接入项目提供并覆盖相应字符的 TTF。

## 2026-09-22 图标显隐与业务动作扩展

- 包内 lint 为 0 errors / 0 warnings，typecheck、build:library、generate:token、generate:docgen 通过。
- 包内 `yarn test`：7 个套件、68 项通过，无 act 告警或资源泄漏；新增覆盖全部动作动态移除/恢复、会话不重载、隐藏工具退出、隐藏撤销按钮后键盘撤销、侧栏关闭焦点回退，以及扩展分区、动态禁用、异步防重、失败报告与只读权限。
- 文档站 generate:docs、check:docs、typecheck 通过；256 个唯一 Demo，28 项生成器测试通过。生成文件通过命令更新。
- Chromium 实际加载内置示例 PDF，验证逐项勾选、分组半选、全部隐藏、恢复默认、Space 切换、只读状态和文档摘要动作；暗色及浅色截图已目视核对。逐项配置采用可折叠区域，避免遮挡主要预览。
- 390 × 844 视口验证配置列表单列、工具栏换行及侧栏自动折叠；documentElement.clientWidth 与 scrollWidth 均为 375px，没有横向溢出。验收后恢复默认视口。
- 已在浏览器读取 M3 Checkbox 与 Icon buttons 的 guidelines、specs、accessibility，结合 Material Web 文档、实现、样式与令牌核对。参考链接集中在 [TOOLBAR.md](./TOOLBAR.md)。新扩展采用标准图标按钮，复选框复用本库实现；本轮未迁移已有全部按钮到新版 M3 的形变/尺寸变体，不宣称完成全库视觉合规审查。
- 新 Demo 独立预览控制台没有 warning/error。Wake 工作台容器加载时出现 `MutationObserver.observe: parameter 1 is not of type Node`；另开原有 Basic 空编辑器也能复现，记录为既有工作台问题，本轮未修改该工具链。
