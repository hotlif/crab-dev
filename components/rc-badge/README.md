# @crab-dev/rc-badge

在元素旁展示计数、更新提醒或状态。

## 主要功能

- 使用 `count` 展示数量，使用 `dot` 表示有更新。
- 使用 `status` 与 `text` 组合展示状态。

## 文档

- [使用说明与 API](./docs/index.mdx)
- [示例源码](./docs/demos/)

在当前包目录运行 `yarn start` 可打开组件工作台，查看交互示例。
