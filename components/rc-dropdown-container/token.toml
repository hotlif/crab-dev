[build]
output = "./src/token.ts"
prefix = "dropdown-container"
imports = ["@crab-dev/rc-token-semantic"]

[token]
box-shadow = "$ref(shadow.overlay)"
border-radius = "8px"
background-color = "#fff"
z-index = "$ref(z-index.float)"