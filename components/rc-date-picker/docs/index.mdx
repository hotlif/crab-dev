+++
title = "DatePicker 日期选择器"
description = "基于 Temporal 的日期选择器，支持范围限制、时区和本地化显示"
kind = "component"
status = "experimental"
+++

# DatePicker 日期选择器

基于 Temporal 的日期选择器，支持日期范围限制、时区和本地化显示。

[打开组件工作台](/components/rc-date-picker/workbench/)

## 主要功能

- 日历面板支持切换月份和年份。
- 通过 `range` 限制可选日期。
- 通过 `timeZone`、`locale` 和 `weekStartDay` 配置时区、本地化显示和每周起始日。
- 使用 `renderDisplayString` 自定义输入框中的日期文本。

`value` 使用 `Temporal.ZonedDateTime | null`，通过 `onValueChange` 接收日期变化。

## 代码演示

<Demos glob="./demos/*.demo.tsx" columns={2} />

## API

<API source="../src/datePicker/datePicker.tsx" symbol="DatePickerProps" component="DatePicker" />
