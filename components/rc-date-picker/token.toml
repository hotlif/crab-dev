[build]
output = "./src/token.ts"
prefix = "date-picker"
imports = [
    "@crab-dev/rc-token-semantic"
]

[token]

selected.background.color = "$ref(color.brand.primary)"
selected.text.color = "$ref(color.text.on-brand)"
cell.background.color-hover = "$ref(color.background.hover-subtle)"
cell.text.color-hover = "$ref(color.text.primary)"

header.padding = "$ref(space.section-gap) $ref(space.component-gap)"
header.cell.height = "40px"
header.font.size = "$ref(font.size.caption)"
header.font.weight = "400"

cell.padding = "3px 4px"
cell.border.radius = "$ref(radius.sm)"
cell.content.size = "24px"
cell.font.size = "$ref(font.size.body)"
cell.font.weight = "400"
cell.transition = "$ref(motion.fade)"

action.bar.margin.top = "$ref(space.component-gap)"
action.bar.gap = "$ref(space.component-gap)"

panel.font.size = "$ref(font.size.body)"
timezone.font.size = "10px"

opacity.out-of-range = "$ref(opacity.disabled)"
opacity.icon = "$ref(opacity.secondary)"
opacity.icon-hover = "$ref(opacity.hover)"
opacity.timezone = "$ref(opacity.tertiary)"