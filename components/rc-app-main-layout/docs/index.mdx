+++
title = "AppMainLayout 布局"
description = "为管理后台组织侧边导航、顶部操作和标签页内容"
kind = "component"
status = "experimental"
+++

# AppMainLayout 布局

为管理后台提供侧边导航、顶部操作区和标签页内容区。

[打开组件工作台](/components/rc-app-main-layout/workbench/)

## 何时使用

- 管理后台需要统一的侧边导航、顶部操作区和内容区。
- 多个业务页面需要通过标签页切换，并共享应用布局。
- 窄屏下需要将侧边导航收起到抽屉中。

使用时需包裹在 `AppMainLayoutProvider` 中，由 Provider 管理标签页状态。

## 代码演示

<Demos glob="./demos/*.demo.tsx" columns={2} />

## API

<API source="../src/layout.tsx" symbol="LayoutProps" component="Layout" />
