+++
title = "Skeleton 骨架屏"
description = "在内容加载期间展示布局占位，减少页面跳动"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-skeleton.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-skeleton.js";

# Skeleton 骨架屏

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-skeleton</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。完整代码包含公开导入和样式入口。

内容结构已知时，骨架屏提前保留空间，加载完成后替换为真实内容。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**占位结构**

使用 rows 表示待加载的段落。 内容结构已知时，骨架屏提前保留空间，加载完成后替换为真实内容。 观察占位行数与留白。 三行占位说明此处将出现文本。

**切换内容**

通过 loading 在占位与 children 之间切换。 内容结构已知时，骨架屏提前保留空间，加载完成后替换为真实内容。 显示内容，再切回骨架。 同一区域交替显示骨架与正文。

</div>

## 常见问题

### 所有加载都用骨架屏吗？

内容结构不确定或只是执行操作时，用 Spin 提供加载反馈。


## API

<API source="../_generated_api/rc-skeleton.ts" symbol="SkeletonProps" component="Skeleton" />

## 更多示例

<details>
<summary>展开进阶示例（5 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 动画形态 — `pulse` 透明度脉动，性能开销最小；`wave` 高亮带从左向右扫过。

##### 基础用法 — 默认渲染一行文本占位；通过 `rows` 控制行数，末行自动收窄。

##### 组合占位 — 搭配不同 `variant` 可以拼出卡片、列表项等复合占位形态。

##### 加载完成 — `loading=false` 时直接渲染 `children`，便于包裹真实内容。

##### 形状变体 — 通过 `variant` 选择占位形状：`text` / `rect` / `circle` / `button` / `avatar` / `image`。

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-skeleton/workbench/)

## 使用说明

在内容加载期间展示布局占位，减少页面跳动。


### 何时使用

- 列表、卡片或详情尚未加载完成，但内容结构已经确定。
- 需要提前保留内容空间，减少加载完成后的布局变化。

占位形状应接近实际内容。无法预估内容结构时，可使用 Spin 提供加载反馈。

### 主要功能

- 支持 `text` / `rect` / `circle` / `button` / `avatar` / `image` 六种常用形状。
- `rows` 控制多行文本占位，末行自动收窄以模拟段落尾部。
- `animation` 在 `pulse`（透明度脉动）与 `wave`（渐变扫过）之间切换；`active=false` 时保留静态底色。
- `width` / `height` 同时支持数字（自动补 `px`）与字符串（如 `"50%"`）。
- `loading=false` 时直接渲染 `children`，便于嵌入到数据驱动的分支里。
- 尊重 `prefers-reduced-motion`，自动禁用非必要动画。
