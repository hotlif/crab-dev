+++
title = "LineEdit 单行输入框"
description = "单行文本输入，支持前后缀、清除、字数统计和密码可见性切换"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-line-edit.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-line-edit.js";

# LineEdit 单行输入框

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-line-edit</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。示例使用公开组件入口，样式由 Wake 自动加载。

业务状态保存输入值，标签说明输入目的，长度限制要在输入前可见。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**提供标签**

添加可见 label、占位示例和 maxLength。 业务状态保存输入值，标签说明输入目的，长度限制要在输入前可见。 输入项目名称。 输入显示在字段内，最多允许 30 个字符。

**清除与计数**

连接受控值，再启用 showCount 与 allowClear。 业务状态保存输入值，标签说明输入目的，长度限制要在输入前可见。 修改名称，然后点击清除按钮。 输入、计数与输出同步更新，清除后为空。

</div>

## 常见问题

### 清除按钮为什么不生效？

allowClear 仅在受控模式下生效。确保 value、onChange 与 onClear 更新同一份状态。


## API

<API source="../_generated_api/rc-line-edit.ts" symbol="LineEditProps" component="LineEdit" />

## 更多示例

<details>
<summary>展开进阶示例（7 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

#### 基础与尺寸

##### 基础用法 — 一个简单的单行文本编辑器

##### 尺寸 — 通过 `size` 属性设置输入框尺寸

##### 前缀和后缀 — 通过 `prefix` 和 `suffix` 属性设置前缀/后缀图标

#### 输入能力

##### 可清除 — 设置 `allowClear` 后，输入框有内容时右侧显示清除按钮；配合受控 `value` 和 `onClear` 回调使用，`disabled` 或 `readOnly` 时清除按钮自动隐藏

##### 密码输入 — 设置 `type=\"password\"` 时右侧自动出现可见性切换按钮（眼睛图标），用户可随时核查已输入的密码内容，降低因误输入导致的挫败感

##### 字符计数 — 设置 `showCount` 后在输入框右侧实时显示已输入字符数；配合 `maxLength` 使用时显示「已输入 / 上限」格式，帮助用户掌握剩余可输入量

##### 验证状态 — 通过 `status` 属性设置 `error` 或 `warning` 验证状态。失焦时触发校验，边框颜色随状态改变，配合提示文字形成完整反馈闭环

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-line-edit/workbench/)

## 使用说明

用于单行文本录入，支持前后缀、清除按钮、字数统计和密码可见性切换。


### 何时使用

用于名称、搜索词等单行文本输入。可根据场景配置前后缀、清除按钮、字数统计和密码可见性切换。备注、描述等多行内容请使用 `@crab-dev/rc-text-edit`。

`allowClear` 仅在受控模式下生效；使用 `showCount` 时可配合 `maxLength` 显示长度限制。


### 结构与输入边界

标签说明字段名称，placeholder 只给输入示例。前缀表示单位或上下文，后缀动作只影响当前字段。单行输入保留原生横向编辑与选择能力；完整值应能通过聚焦、选择和复制获得，不以 CSS 截断替代编辑内容。

| 场景 | 选择 | 约束 |
| --- | --- | --- |
| 名称、搜索词、短编号 | 普通 LineEdit | 配置可见标签，按业务决定 maxLength |
| 密码核对 | type="password" | 切换按钮改变可见性，值不变；禁用时按钮同步禁用 |
| 已有内容可复制、不能修改 | readOnly | 保留完整文字对比度和键盘聚焦；密码可见性仍可切换 |
| 暂时不可用 | disabled | 输入和内部操作都不激活，原因由相邻说明提供 |
| 需要多行说明 | TextEdit | 不把换行文本压进单行高度 |

### 当前尺寸与状态实现

下列数值来自本包 `token.toml`，不是业务样板 36 / 28px 的统一替代。

| size | 容器最小高度 | 默认字号 / 行高 | 内边距 |
| --- | --- | --- | --- |
| large | 40px | 16px / 24px | 上下 4px、左右 12px |
| middle | 32px | 14px / 20px | 上下 4px、左右 12px |
| small | 24px | 14px / 20px | 上下 0、左右 12px |

高度可随内容和字体设置增长。粗指针下输入区最小高度为 44px，内部动作最小目标为 44×44px，边框仍计入外层尺寸；容器可能高于 44px。鼠标下的内部动作至少 24×24px，小尺寸带动作时也允许容器自然增高，不能裁掉动作焦点。

键盘输入焦点以品牌实线轮廓标示；错误保留反馈边框，焦点轮廓负责当前位置，两种职责叠加。`status="error"` 默认设置 `aria-invalid=true`，显式 `aria-invalid` 优先；warning 不自动宣告无效。错误原因和恢复方法仍需调用方以可见文字提供，并通过 `aria-describedby` 关联。

内部清除和密码动作复用 Button，具有独立焦点、系统颜色和减少动态效果支持。清除先把焦点还给输入框，再调用 `onClear`；由调用方更新受控值。提供 `id` 时动作关联该 ID，否则组件通过 `useId` 建立关联。

### 组合、误用与验收

- 正例：成员邮箱出错时显示“邮箱格式不完整，请补全 @ 后的域名”，关联输入框；用户仍可继续编辑。
- 反例：只有红色边框。读者不知道原因，非视觉用户也无法获得修正提示；补充可见错误文字及关联。
- 正例：只读项目编号保持可选择、可复制。反例：把所有只读字段都禁用，导致内容被淡化且键盘无法进入。
- `bordered=false` 用于已有容器的复合控件；宿主负责边界和整体分组，输入自身仍保留键盘焦点。避免宿主与输入重复绘制同一焦点轮廓，定制后必须实测。
- 验收：Tab 进入输入和动作；Enter 清除后继续输入；禁用密码按钮不能被 Tab 到达；只读值不能修改；错误和聚焦可以同时辨认；320px 下动作不遮住文本；放大文字后无垂直裁切。

关联：[输入与选择](/design/inputs)、[状态组合](/design/interaction)、[无障碍](/design/accessibility)、[Button](/components/rc-button)、[TextEdit](/components/rc-text-edit)。浏览器专项检查与包内 React 测试分开验收，工具链门禁受阻不表示后者通过。
