+++
title = "Canvas WebGL 画布"
description = "通过 React 组件声明图形，使用 WebGL 2 绘制二维画面"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-canvas.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-canvas.js";

# Canvas WebGL 画布

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-canvas</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。示例使用公开组件入口，样式由 Wake 自动加载。

Canvas 使用声明式图形节点描述场景，状态变化更新节点属性。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**绘制基本图形**

把 Rect 和 Circle 放入有明确尺寸的 Canvas。 Canvas 使用声明式图形节点描述场景，状态变化更新节点属性。 对照坐标与图形位置。 两种图形绘制在同一画布。

**用状态控制位置**

改变图形的 x 属性，并提供可读文字反馈。 Canvas 使用声明式图形节点描述场景，状态变化更新节点属性。 点击移动矩形。 矩形左右移动，文字同步描述位置。

</div>

## 常见问题

### 图形需要文字替代吗？

需要。画布本身不能替代可读内容；重要数据和操作应提供 DOM 文字与键盘入口。


## API

<API source="../_generated_api/rc-canvas.ts" symbol="CanvasProps" component="Canvas" />

## 更多示例

<details>
<summary>展开进阶示例（13 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

#### 基础图形

##### 基础图元 — 在 Canvas 中声明矩形与圆形，颜色支持 OKLCh / 十六进制，opacity 控制透明度。

##### 任意角度直线 — 直线在顶点着色器端挤出为带宽度的四边形，支持任意斜率与线宽。

##### 圆角矩形（SDF） — radius 0 时自动切换到 SDF 着色器，边缘与圆角均为亚像素级抗锯齿。

##### 整体透明度（opacity） — opacity 同时作用于 fill 和 stroke，从 1.0 到 0.1 均匀过渡，Line 的 color 同样受影响。

##### 文字渲染 — 文字经 OffscreenCanvas 生成字形位图并上传为 R8 纹理，在 GPU 端以 alpha mask 着色。

##### 文字对齐与多行 — textAlign（left / center / right）、textBaseline（top / middle / bottom）以及 \\n 换行和 maxWidth 自动词换行的综合演示。

##### 分组变换与嵌套 — Group 维护 TRS 矩阵栈，子孙坐标随父级平移 / 旋转 / 缩放叠加，支持任意层级嵌套。

#### 交互与变换

##### Draggable — Draggable 示例

##### hover 回调 — 通过 onMouseEnter / onMouseLeave 响应图元悬停事件，驱动颜色高亮与状态提示。三种图元均支持该回调。

##### Transformer — Transformer 示例

#### 场景与性能

##### 属性驱动动画 — 用 React state 逐帧更新图元 props，Canvas 内部 rAF 渲染循环自动重绘，演示流畅动画。

##### Infinite Canvas — Infinite Canvas 示例

##### Minimap — Minimap 示例

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-canvas/workbench/)

## 使用说明

使用 React 组件声明二维图形，由 WebGL 2 在 GPU 上绘制。支持矩形、圆形、线条、文字、图片和分组变换。


### 何时使用

- 需要绘制大量 2D 图元（矩形、圆、线、文字、图片）且追求 GPU 加速与流畅动画时。
- 需要以声明式 React 组件树组织画面，而非命令式操作 Canvas 2D API 时。
- 需要对图形做分组变换（平移 / 旋转 / 缩放）并保持嵌套坐标系时。

### 主要功能

- **WebGL 2 渲染**：通过内置着色器绘制图元。
- **声明式 API**：`<Canvas>` 内嵌套 `<Rect>` / `<Circle>` / `<Line>` / `<Image>` / `<Text>` / `<Group>`。
- **SDF 抗锯齿**：圆角矩形与圆形通过有符号距离场实现亚像素级平滑边缘。
- **任意角度直线**：顶点着色器端挤出（line extrusion），支持任意斜率与线宽。
- **分组变换**：`<Group>` 维护 TRS 矩阵栈，子孙坐标随父级叠加，支持任意层级嵌套。
- **OKLCh 颜色**：颜色解析支持 `oklch()`、十六进制与 `rgb()/rgba()`。
- **设备像素比自适应**：根据 `devicePixelRatio` 调整画布分辨率。

### 画布与阅读视口

**现有示例：** 固定绘图坐标放在有名称、可聚焦的局部滚动区域内。窄屏可横向查看完整画布，页面正文不会随绘图宽度扩张。画板中的显式配色服务于图元样张，不代表业务界面的语义主题；画板周围的焦点使用公共语义颜色。

**选择规则：** 要保持坐标和形状比例时使用局部滚动；要让画布本身随容器变化，可使用已有 `fillParent`，并为父容器提供明确尺寸。滚动区域的键盘可达不等于内部图元已具备名称、选择或拖拽的键盘替代，交互图形仍需单独实现和验收。



`CanvasImage.src` 支持字符串 URL 和 `ImageBitmap`。位图由调用者持有，替换或卸载后应调用 `close()`；Canvas 在最后一个消费者释放时删除图片和字形的 GPU 纹理及 CPU 缓存，不关闭调用者提供的位图。

更换图片时保留上一张已上传纹理，新图片上传成功后再替换；加载或上传失败调用 `onError`，保留原图。`Canvas.onRender` 在每次实际绘制完成后通知，可用于交接双缓冲画面；图片的 `onLoad` 仅表示上传完成，不表示画面已经绘制。使用双缓冲时应在旧场景移除后关闭其位图。

Canvas 真正卸载后主动释放 WebGL 上下文，避免虚拟列表反复挂载画布耗尽浏览器配额；StrictMode 的挂载检查保留正在使用的上下文。上下文意外丢失期间暂停绘制通知，浏览器恢复后重建绘图资源，重新上传仍在使用的图片和字形并重绘，无需重新加载业务数据。资源释放与恢复依据 [WebGL 最佳实践](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API/WebGL_best_practices)及 [Khronos 上下文丢失处理说明](https://wikis.khronos.org/webgl/HandlingContextLost)。
