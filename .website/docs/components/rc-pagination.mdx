+++
title = "Pagination 分页"
description = "切换页码、调整每页条数，并展示数据总量"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-pagination.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-pagination.js";

# Pagination 分页

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-pagination</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。示例使用公开组件入口，样式由 Wake 自动加载。

分页控件提供导航，数据切片或请求仍由业务负责。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**提供总量**

传入 total 和初始每页条数。 分页控件提供导航，数据切片或请求仍由业务负责。 点击下一页。 页码更新；此时尚未连接具体列表。

**连接列表切片**

用 current 计算 slice 范围，并连接 onChange。 分页控件提供导航，数据切片或请求仍由业务负责。 切换到第 2 页和最后一页。 第二页显示项目 6–10，最后一页只有 3 条。

</div>

## 常见问题

### 筛选后应保留原页码吗？

通常将页码重置为 1，避免新结果不足以覆盖原来的页数。


## API

<API source="../_generated_api/rc-pagination.ts" symbol="PaginationProps" component="Pagination" />

## 更多示例

<details>
<summary>展开进阶示例（8 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

#### 基础与外观

##### 基本用法 — 默认为非受控模式，提供 total 即可正常使用

##### 紧凑尺寸 — size=\"small\" 用于表格内联、抽屉底部等高密度场景

##### 禁用状态 — disabled 会整体锁定分页器，所有按钮与输入框不可交互

#### 页码控制

##### 受控模式 — 通过 current 与 onChange 完全托管页码状态

##### 大量数据 — 页数超过 7 时自动折叠为首尾 + 中部区间 + 省略号跳转

##### 快速跳转 — 开启 showQuickJumper 后，用户可直接输入目标页码并回车跳转

##### 显示总量 — showTotal 支持布尔值或自定义渲染函数，展示当前区间与总数

##### 每页条数选择 — 传入 showSizeChanger 以显示每页条数下拉；切换时会保持当前首条可见。

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-pagination/workbench/)

## 使用说明

切换页码、调整每页条数，并展示数据总量。


### 何时使用

- 列表 / 表格 / 卡片墙需要按页加载大量数据
- 后端分页或前端切片分页，向用户暴露页码导航
- 需要在不同密度场景（表格内联 / 独立页面 / 抽屉底部）切换尺寸

### 设计要点

- 根据页面密度选择尺寸，分页控件应靠近对应的数据区域。
- 页数较少时显示完整页码，较多时通过省略号和跳转控件缩短导航。
- 保留悬停、聚焦、按下和禁用反馈，并遵循减少动态效果的系统偏好。
