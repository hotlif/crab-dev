+++
title = "Tag 标签"
description = "标签，用于分类与标记"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-tag.js";

# Tag

标签用于进行标记和分类。

## 组件预览

<div hidden aria-hidden="true" data-docs-search-index="demos">

### Demo 搜索索引

#### 基础与外观

##### 基础用法 — 基础的标签展示，通过 `color` 属性设置不同颜色预设

##### 无边框 — 设置 `bordered=（false）` 可移除标签边框

##### 图标标签 — 通过 `icon` 属性在标签前添加图标

##### 标签尺寸 — 通过 `size` 属性设置标签尺寸

#### 交互与自定义

##### 可选中标签 — 通过 `CheckableTag` 实现可选中/取消选中的标签

##### 可关闭标签 — 添加 `closable` 属性使标签可关闭，配合 `onClose` 回调处理关闭逻辑

##### 自定义颜色与关闭图标 — 支持自定义颜色字符串与 `closeIcon`

</div>
<ComponentDemos demos={demos} />

## 何时使用

- 用于标记事物的属性和维度。
- 进行分类和筛选。
- 展示状态信息（成功、警告、错误等）。


我们提供了五种预设颜色。

- ⚪️ 默认标签：中性标记，适用于一般分类。
- 🔵 品牌标签：强调突出的重要标记。
- 🟢 成功标签：表示成功、完成状态。
- 🟡 警告标签：表示警告、待处理状态。
- 🔴 错误标签：表示错误、失败状态。

## 功能特性

- 支持预设颜色与自定义颜色字符串（如 `#1677ff`）。
- 支持 `icon` 前缀图标。
- 支持 `closable` 与 `onClose` 关闭交互。
- 支持 `closeIcon` 自定义关闭图标。
- 支持 `CheckableTag` 可勾选标签（`checked` / `onChange`）。



## API

<API source="../_generated_api/rc-tag.ts" symbol="TagPropsSearchIndex" component="Tag" />
