+++
title = "Realm 远程模块"
description = "加载 Module Federation 远程模块，支持 React 组件和生命周期两种接入协议"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-realm.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-realm.js";

# Realm 远程模块

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-realm</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。示例使用公开组件入口，样式由 Wake 自动加载。

Realm 按容器协议加载组件。这里注册本地容器模拟远程模块，不发送网络请求。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**挂载模拟模块**

在 effect 中注册独立 scope 的本地容器，卸载时清理。 Realm 按容器协议加载组件。这里注册本地容器模拟远程模块，不发送网络请求。 等待模拟模块显示。 Realm 通过 get 返回的模块渲染问候。

**传递业务属性**

通过 remoteProps 更新远程组件的业务输入。 Realm 按容器协议加载组件。这里注册本地容器模拟远程模块，不发送网络请求。 切换当前成员。 模块显示新的问候，不需要重新定义容器。

</div>

## 常见问题

### 如何接真实远程模块？

移除本地注册 effect，填写实际 entry、scope 与 module，并在应用中配置错误和加载反馈。不要在渲染过程中修改全局容器。


## API

<API source="../_generated_api/rc-realm.ts" symbol="RealmProps" component="Realm" />

## 更多示例

<details>
<summary>展开进阶示例（4 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 基础用法 — component 协议加载远程 React 组件：远程与宿主共享同一 React 实例, remoteProps 实时透传, 与本地组件表现无异。此例通过预注册容器演示（globalThis[scope] 已存在时跳过 script 注入, 零网络）。

##### 错误态与重试 — 远程模块首次加载必然失败（模拟弱网）：Realm 渲染 rc-alert 错误态并就近提供重试按钮；失败已自动使缓存失效, 点击重试即重走加载链路, 第二次成功。

##### mount 协议 + Shadow DOM 沙箱 — mount 协议适配跨框架 / 不同 React 版本的远程, 可开启 sandbox 做样式隔离：下方宿主与远程各有一个 .sandbox-demo-title 元素——远程标题被 styleSheets 染色, 宿主标题不受影响；主题令牌 CSS 变量仍穿透沙箱。

##### 真实 script 注入链路 — 从 URL 加载 remoteEntry.js（本包 public/remote/ 下的手写 var 格式容器）：script 注入 → init 收到含宿主 React 的 share scope → get('./Widget') → 渲染。远程组件用 init 时注入的宿主 React 实例创建元素, 端到端验证共享机制。

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-realm/workbench/)

## 使用说明

运行时加载 Module Federation 远程模块，完成入口加载、共享依赖初始化、模块获取和渲染。`component` 协议在宿主 React 树中渲染远程组件；`mount` 协议通过生命周期函数挂载远程内容。


### 主要功能

- 优先复用宿主的 `default` 共享作用域；宿主未使用 Module Federation 时，创建作用域并注册 React 相关模块。
- 通过 Promise 缓存合并并发加载请求；失败或超时后清除对应缓存，允许重新加载。
- 提供延迟加载反馈，以及就近显示的错误说明和重试入口。


### 两种接入协议

### component（默认）：远程导出 React 组件

远程与宿主共享同一 React 实例，在宿主 React 树中渲染。`remoteProps` 变化时更新组件属性，不重新加载模块。

```tsx
<Realm entry="https://cdn.example.com/remoteEntry.js" scope="orders" module="./OrderTable"
       remoteProps={{ tenantId }} />
```

### mount：远程导出生命周期对象

适配**跨框架**或**与宿主不同 React 版本**的远程。远程导出
`{ mount(container, props), update?(props), unmount?(container) }`：

- `mount` 应支持重复挂载和清理，并与 `unmount` 配对；可返回清理函数，卸载时先调用该函数，再调用 `unmount`。
- 提供 `update` 时，`remoteProps` 变化会触发增量更新；未提供时，Realm 卸载后重新挂载内容。

`mount` 协议可开启 `sandbox`，通过 Shadow DOM 隔离样式。`component` 协议不支持此选项：
注入 `document.head` 的普通样式无法直接应用到 Shadow DOM 内部。

### 远程应用的共享依赖配置

```js
// 远程应用的 webpack.config.js → ModuleFederationPlugin
shared: {
    react: { singleton: true, requiredVersion: '^19.0.0' },
    'react-dom': { singleton: true, requiredVersion: '^19.0.0' },
}
```

- React 共享依赖必须设置 `singleton: true`，以复用宿主已加载的实例，避免多个 React 实例引起 Hook 调用错误。
- 当前接入约定不支持 `strictVersion: true` 或 `eager: true`。请保持上面的共享配置，并验证远程应用与宿主的 React 版本兼容。

### 样式隔离边界

- `sandbox` 提供样式隔离，不隔离 JavaScript 执行环境。远程样式需要在 Shadow DOM 内生效，或通过 `styleSheets` 注入。
- CSS 自定义属性可从宿主继承到 Shadow DOM 内部，因此远程样式可以使用 `var(--token-semantic-*)`。
- Portal 弹层若挂载到 `document.body`，会离开 Shadow DOM 的样式范围。宿主位于原生模态对话框中时，应将弹层挂载到相应的 `dialog` 子树；可参考 Tooltip 和 DropdownContainer 的挂载处理。

### RealmError 错误码

| code | 阶段 |
|------|------|
| `ssr` | 非浏览器环境触发加载 |
| `script` | remoteEntry 网络加载失败（script onerror / module import 失败） |
| `container` | 加载成功但 `globalThis[scope]` 不存在或形状不符（scope 名错 / 非 MF remote） |
| `init` | `container.init(shareScope)` 抛错 |
| `factory` | `container.get(module)` 或模块工厂执行抛错 |
| `timeout` | 全程超过 `timeout`（默认 15s）期限 |
| `protocol` | 模块形态与所选协议不符（错误信息会指向另一协议） |
| `render` | 远程组件渲染期 / `mount()` 执行期抛错 |

加载失败或超时后，对应缓存会失效。点击错误提示中的“重试”可重新执行加载流程。

### 工具函数

以下具名导出函数可用于预加载或独立加载模块，与组件共享缓存：

```ts
import { loadRemoteModule, preloadRemote, clearRemoteCache } from '@crab-dev/rc-realm';

// 绕过组件直接取远程模块（共享同一套缓存）
const mod = await loadRemoteModule({ entry, scope, module: './util' });
// hover 预热（吞错, 失败留给正式加载重试）
preloadRemote({ entry, scope, module: './OrderTable' });
// 清缓存（测试隔离 / 热更新用, 生产慎用；不删除 globalThis[scope]）
clearRemoteCache(entry, scope);
```
