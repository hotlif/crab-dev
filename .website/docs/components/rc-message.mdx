+++
title = "Message 全局提示"
description = "以短暂的全局消息反馈操作结果，适用于轻量成功或失败提示。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-message.js";

# Message 全局提示

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-message</div>

<div id="基础示例" data-docs-example-anchor /><div id="全部示例" data-docs-example-anchor />

## 示例

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 基础用法 — 通过 `useMessage` Hook 创建消息实例，支持五种类型的消息提示

##### 自定义图标与持续时间 — 通过 `icon` 自定义图标，通过 `duration` 设置持续时间，设为 0 则不自动关闭

##### 消息堆叠 — 当消息数量超过 3 条时，旧消息会自动折叠。最新的一条在最前面，后面的消息缩小并淡出

</div>

<ComponentDemos demos={demos} />

[打开完整组件工作台](/components/rc-message/workbench/)

## 使用说明

在页面顶部展示简短的操作反馈。


### 何时使用

用于保存成功、复制完成或操作失败等简短反馈。消息在页面顶部居中显示，可按 `duration` 自动关闭。需要详细说明或后续操作时使用 Notification，需要持续展示的页面提示使用 Alert。

### 主要功能

- 支持五种类型：`success`、`info`、`warning`、`error`、`loading`。
- 顶部居中弹出，自动消失。
- 支持自定义 `duration` 控制显示时长。
- 鼠标悬停或内容中的操作获得键盘焦点时暂停倒计时，离开后继续剩余时间。
- 支持自定义图标 `icon`。
- 通过 `useMessage` Hook 创建实例。
- 多条消息堆叠，最多显示 3 条，仅最上层消息计时；旧消息恢复到最上层后继续剩余时间。
- 入场、退场和进度条均使用 CSS，自动适配减少动态效果偏好。`duration={0}` 的常驻消息不展示倒计时条。



`useMessage()` 的 open/success/error/warning/info/loading 均返回 `{ id, close, update }`。例如 `const handle = message.loading('正在保存', 0)`，保存完成后 `handle.update({ type: 'success', content: '已保存', duration: 2000 })`。update 保留 ID 并重新计时；close 重复调用只通知一次。实例也支持 `message.close(id)` 和 `message.update(id, patch)`。

默认 Message 组件通过 open/onExitComplete 控制显隐，duration 只控制进度动画；自动关闭与 onClose 回调由 useMessage 管理。

### 适用场景

操作结果无需保留在页面中，用户读到后可以继续。 需要通过 useMessage 在当前应用上下文中触发短消息。

必须阅读或处理的错误使用 Alert、Notification 或 Dialog；表单字段错误放在字段附近。

### 使用要点

- 一句话说明结果，避免把原因、详情和多个动作都塞进去。
- duration 要给用户足够阅读时间，重要消息不要自动过快消失。
- 通过上下文 holder 保持主题和挂载位置，不要在任意模块创建孤立实例。

### 无障碍

- 根据重要程度选择礼貌或紧急通告，避免连续消息让读屏队列失控。
- 消息关闭后不移动当前焦点；仅靠颜色不能表达成功或失败。

## 常见问题

### 消息为什么没有出现？

检查是否渲染了 useMessage 返回的 contextHolder，并确认事件实际执行。

## API

<API source="../_generated_api/rc-message.ts" symbol="MessageProps" component="Message" />
