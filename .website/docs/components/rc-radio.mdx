+++
title = "Radio 单选框"
description = "从一组互斥且可见的选项中选择一个值"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-radio.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-radio.js";

# Radio 单选框

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-radio</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。示例使用公开组件入口，样式由 Wake 自动加载。

单选组展示少量互斥选项，组值用于业务提交。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**设置选项组**

用 RadioGroup 包裹 Radio，并设置不同 value。 单选组展示少量互斥选项，组值用于业务提交。 在每天和每周之间切换。 同一时刻只有一个选项被选中。

**读取组值**

通过组级 value 与 onChange 管理选中值。 单选组展示少量互斥选项，组值用于业务提交。 选择每天，查看提交值。 输出变为 daily，其他选项取消选中。

**视觉状态与明暗主题**

对照圆环、圆点、交互状态层和三种视觉尺寸。 参考 Material Design 3，以品牌色圆环和同色圆点表达选中，交互反馈不改变布局。 切换紫、蓝、绿品牌色；在两种主题下悬停、按压，并用 Tab 和方向键操作。 圆点与圆环跟随主题，焦点可见，禁用选项不响应，切换前后位置和尺寸稳定。

</div>

## 常见问题

### 选项很多时用什么？

选项较多时用 Select；Radio 适合直接比较少量选项。


## API

<API source="../_generated_api/rc-radio.ts" symbol="RadioProps" component="Radio" />

## 更多示例

<details>
<summary>展开进阶示例（4 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 基础用法 — 最简单的 Radio 用法

##### 禁用状态 — 添加 `disabled` 属性即可让单选框处于禁用状态

##### 单选框组 — 使用 `RadioGroup` 管理一组单选框的状态, 实现互斥选择

##### 单选框尺寸 — 通过 `size` 属性设置单选框尺寸, 支持 `large`、`middle`、`small` 三种

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-radio/workbench/)

## 使用说明

从一组互斥且可见的选项中选择一个值。


### 何时使用

- 从少量互斥选项中选择一个值，例如支付方式或配置类型。
- 需要让用户直接比较全部选项时使用 Radio；选项较多或空间有限时使用 Select。

### 视觉状态与验证

参考 [Material Design 3 Radio](https://m3.material.io/components/radio-button/overview) 的圆环、圆点与圆形状态层。默认 20px 圆环采用恒定 2px 描边，选中时圆环和圆点使用品牌色，内部保持透明；small / large 分别为 16 / 24px。颜色通过 L3 消费现有 L2，自动适配明暗主题与单色品牌配置。

40px 圆形交互区承载悬停 8%、聚焦和按压 12% 的状态层，悬停文字也会触发反馈；键盘焦点另有独立轮廓。圆点和状态层仅使用 CSS 过渡。禁用时沿用 Crab 的禁用透明度，圆环、圆点和文字使用中性色，不响应交互反馈。

原生输入负责组内单选和键盘行为。使用 `aria-label` 或 `aria-labelledby` 为 RadioGroup 命名；校验提示通过 Radio 的 `aria-invalid` 与 `aria-describedby` 关联。强制颜色下以系统圆环和圆点表达选中。

选中圆点常驻并绝对定位，只以透明度和缩放过渡显示；圆框采用固定盒模型，外层固定行内对齐。切换选项不会改变圆框尺寸、文字位置或相邻控件的间距。

RadioGroup 未指定 `name` 或传入空字符串时，会生成稳定的组内名称，让方向键在当前组循环，避免跳入页面中的其他组。显式 `name` 保持原值；不同独立组不应复用同一个名称。组内不要再给单个 Radio 设置不同的 `name`。

**原生表单迁移**：自动名称也属于原生表单字段。使用 FormData 的页面应明确设置例如 `name="notificationFrequency"` 的业务名称，不依赖自动 ID，也不依赖未命名组原先不进入 FormData 的行为。

粗指针环境下，圆形控件的命中区扩展为 48×48 CSS px，圆环和圆点仍保持指定的视觉尺寸。长标签采用正文行高并允许换行；紧密组合需检查目标区域没有相互重叠。

键盘焦点使用公共 L2 焦点角色，通过 L3 覆盖可调整；减少动态效果时移除非必要过渡。实际浏览器应逐项检查 Tab / Shift+Tab、原生激活方式、两种主题、强制颜色、长标签与粗指针布局。当前源码修复与自动检查状态见质量审查记录，不能把 CSS 中存在媒体查询等同于设备验收通过。

规则依据：[状态系统](/design/interaction)、[无障碍与键盘](/design/accessibility)、[布局与密度](/design/layout)。
