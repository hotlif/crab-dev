+++
title = "Segmented 分段控制器"
description = "分段控制器, 用于在一组互斥选项中展示并选择单个选项"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-segmented.js";

# Segmented

分段控制器, 用于在一组互斥选项中展示并选择单个选项。

## 组件预览

<div hidden aria-hidden="true" data-docs-search-index="demos">

### Demo 搜索索引

##### 基础用法 — 通过 options 传入选项, 默认选中第一个可用项。

##### 撑满容器 — 设置 `block` 让控制器铺满父容器, 各选项等宽分布。

##### 受控模式 — 传入 value 与 onChange 由外部状态托管选中值。原始值可作为 options 简写。

##### 禁用状态 — 整体 `disabled` 或对单个选项设 `disabled`, 键盘方向键会自动跳过禁用项。

##### 尺寸 — 通过 `size` 设置 `large`、`middle`、`small` 三档尺寸。

##### 带图标 — 选项可搭配 icon; 纯图标选项须提供 aria-label 保证可访问性。

</div>
<ComponentDemos demos={demos} />

## 何时使用

- 用于在一组互斥选项间快速切换, 且希望所有选项一屏可见、方便横向比较
- 与 Radio 的区别是外观更紧凑、连体, 常用于视图 / 时间粒度 / 筛选维度的切换
- 与 Tabs 的区别是它只负责"选值", 不承载被切换的内容面板
- 选项数量不宜过多（建议 2–5 个）, 过多时应改用 Select




## API

<API source="../_generated_api/rc-segmented.ts" symbol="SegmentedPropsSearchIndex" component="Segmented" />
