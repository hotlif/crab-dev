+++
title = "Table 表格"
description = "展示行列数据的表格组件"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-table.js";

# Table

表格用于展示结构化数据。

## 组件预览

<div hidden aria-hidden="true" data-docs-search-index="demos">

#### 基础、汇总与性能

##### 基础表格 — 一个简单的表格信息，展示基础的表格功能

##### 空状态 — 当 rows 为空数组时，表格 body 区域自动显示空状态。默认使用内置 Empty 组件；可通过 empty prop 传入自定义内容，或传 null 禁用。

##### 关键字高亮 — 通过 highlightKeyword 高亮单元格中的匹配文本，activeMatchIndex 控制当前活动匹配（橙色）并同时滚动到对应行列，效果类似浏览器 Ctrl+F。默认 render 自动处理；自定义 render 可通过 keyword 参数配合 highlightText 工具函数手动处理。

##### 底部汇总行 — 通过 showSummary 开启底部固定汇总行，各列由 summaryRender 提供合计 / 平均等聚合内容；汇总行始终贴住底部，固定列横向同步固定。

##### 大规模数据 — 1000 列 × 1000 行，验证虚拟滚动在大数据量下的滚动性能

#### 列与表头

##### 拖拽调整列顺序（固定列 + 分组表头） — 左侧「序号」「姓名」、右侧「邮箱」固定；拖拽分组表头可整体调整分组顺序，拖拽分组内的子列可在分组内部重排。

##### 列宽拖拽调整 — 在表头右边缘拖拽可调整列宽。设置 Table resizable 全局开启，也可通过 ColumnType.resizable 逐列控制。

##### 过滤栏 — 过滤栏只负责收集条件，筛选逻辑在外部实现

##### 表头合并 — 通过 children 配置实现多级表头（头部合并）

##### 列排序 — 点击列头切换升序 / 降序 / 无序；按住 Shift 可追加多列排序。`sortable: true` 启用单列，`sorter` 可自定义比较逻辑。

#### 行与结构

##### 动态行高 — 使用原生 DOM 动态计算行高，并演示 row.height 的优先级

##### 合并单元格 — 按连续相同值自动合并单元格，展示更真实的业务台账数据

##### 行展开（详情面板） — 通过 `expandedRowRender` 在行下方插入自定义详情区，点击行首图标展开或收起。展开内容跨所有列、随表格一起横向滚动；当详情高度超过 `expandedRowHeight` 时面板内部可独立纵向滚动（滚轮 / 触控板）。区别于树形数据：这里展示的是异构详情而非同构子行。

##### 行分组 — 通过 groupBy 按列名进行多级分组。分组 banner 渲染在对应列的位置，叶子行中已分组的列保持空白以避免信息重复（与 react-data-grid 行为一致）。

##### 行序号 — 通过 `showRowNumber` 开启行序号列，序号从 1 开始、反映当前渲染顺序（排序后随之重排）。分组 banner 行不计入序号，数据行始终连续编号。`rowNumberColumnWidth` 控制列宽（默认 50），`rowNumberColumnFixed` 控制是否固定到左侧（默认 true）。

##### 行选中 — 通过 `rowSelection` prop 启用行选中功能。`type: 'checkbox'` 为多选（表头显示全选），`type: 'radio'` 为单选。支持受控与非受控两种用法，可通过 `getDisabled` 禁用特定行。

##### 行状态（新增 / 修改 / 删除） — 通过 row.state 标记行的变更状态，结合自定义渲染为不同状态的行呈现不同视觉样式。

##### 树形数据 — 通过 `treeData` 和 `getChildRows` 展示层级关系，点击行首箭头可展开或收起子行。

#### 编辑与交互

##### 复制单元格 — 选中单元格后按 Ctrl/⌘+C，触发 onCopy 回调并将选区数据写入剪贴板（TSV 格式，可直接粘贴到 Excel）。

##### 编辑单元格 — 双击单元格进入编辑，支持回车提交与失焦提交

##### 行编辑 — 双击任意行进入行编辑模式，整行同时展示编辑器；在右侧工具栏确认或取消，按 Esc 快速退出

##### 行点击与行双击 — 通过 `onRowClick` / `onRowDoubleClick` 监听行事件。传入后行即成为可点击目标（pointer 光标 + hover 反馈），也可用键盘触发：点选行内任一单元格后按 Enter。点在复选框、展开图标、操作按钮等控件上时不会触发行事件；单元格拖选之后的那次抬起也不算点击。

##### 单元格选择 — 类似 Excel 的单元格选区：点击选中、按住拖拽框选、Shift 扩选、Ctrl/⌘ 切换单格

</div>
<ComponentDemos demos={demos} />

## 何时使用

当需要清晰展示大量信息、进行数据对比，或在行列维度上进行查看与操作时，使用表格。


表格常见能力包括：

- 📄 展示多列结构化数据
- 🔎 支持排序、筛选等数据处理
- ✅ 支持行选择与批量操作
- 📌 支持固定表头与固定列
- 🧩 支持自定义单元格渲染

## 无障碍

- 为每个 Table 提供描述数据用途的 `aria-label`，例如“本月订单”；不要只写“表格”。
- 列标题应是简短的纯文本；自定义单元格中的按钮、复选框和输入框必须分别拥有上下文名称。
- 排序、展开、选中和编辑状态不能只靠颜色表达，并应保留组件提供的键盘操作。
- 虚拟滚动场景仍需传入准确的数据总数；空、加载和错误状态要有可读文本和稳定焦点位置。

更多通用要求见[无障碍使用指南](/guides/accessibility)。



## API

<API source="../_generated_api/rc-table.ts" symbol="TablePropsSearchIndex" component="Table" />
