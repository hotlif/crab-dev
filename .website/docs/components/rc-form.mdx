+++
title = "Form 表单"
description = "组织字段、收集表单值并执行校验，集中管理提交反馈。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-form.js";

# Form 表单

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-form</div>

<div id="基础示例" data-docs-example-anchor /><div id="全部示例" data-docs-example-anchor />

## 示例

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 基础用法 — 使用 rc-line-edit 作为编辑器，演示 getFieldValue / setFieldValue / resetFields 等实例方法。

##### 搭配 crab-dev 编辑器 — 将 rc-form 与同体系的 rc-line-edit / rc-select / rc-checkbox / rc-radio / rc-switch / rc-slider / rc-button 组合，演示生态内循环。

##### 字段校验 — 使用 required 与自定义 rules 组合 rc-line-edit / rc-select，演示同步与异步校验。

</div>

<ComponentDemos demos={demos} />

[打开完整组件工作台](/components/rc-form/workbench/)

## 使用说明

管理字段值、校验规则和提交流程，并为字段布局与错误反馈提供统一结构。


### 何时使用

- 收集用户信息、创建记录或编辑配置。
- 集中管理字段值、校验规则、错误提示和提交结果。

### 无障碍

- 每个字段都使用可见 `<label>`；关联 ID 由 `useId` 生成，不使用占位符代替标签。
- 帮助文本和错误信息通过 `aria-describedby` 关联，错误控件同时设置 `aria-invalid`。
- 必填、格式和范围限制应在提交前说明；错误摘要需要能把焦点带回对应字段。
- 提交期间设置 `aria-busy` 并保留明确反馈，避免仅禁用按钮而没有解释。

更多通用要求见[无障碍使用指南](/guides/accessibility)。



`defaultValue` 只在挂载时读取；加载另一条记录时调用 `form.reinitialize(record)`，该记录也成为 `resetFields()` 的新基准。`useForm()` 的实例引用保持稳定。

`Item` 默认绑定 value/onChange。原生输入与 LineEdit 使用 `binding="event"`，Switch / Checkbox 使用 `binding="checked"`，日期选择器使用 `binding="valueChange"`；原有子组件事件会继续执行。特殊协议可配置 valuePropName、trigger、getValueFromEvent，或使用函数 children 接收字段状态和 onChange。

`await form.submit()` 等待校验及业务回调，返回 success / invalid 两种结果。`validateFields()` 失败时抛出 `FormValidationError`，通过 values 获取数据快照、fields 获取字段 errors / warnings。WARNING 不阻止提交。嵌套字段使用元组路径，setter 会检查对应的值类型。

### 适用场景

多个字段需要一起校验和提交。 字段之间存在依赖、动态显隐或统一错误处理。

只有一个独立开关或搜索框时不必引入完整表单状态；服务器数据缓存也不应塞进表单。

### 使用要点

- 为每个字段使用稳定 name，initialValues 只负责初始值。
- rules 描述验证规则，提交失败时把用户带到第一个可修复问题。
- 编辑器组件通过 value/onChange 与 Form 连接，避免同时维护第二份本地值。

### 无障碍

- label、帮助文字和错误必须通过 ID 关联到实际输入控件。
- 错误不能只显示红色；提交后保留用户输入，并提供错误摘要或准确焦点。

## 常见问题

### 为什么不能直接使用 DOM onChange？

Form 字段需要值回调；LineEdit 返回输入事件，所以用适配器提取 event.target.value。

## API

<API source="../_generated_api/rc-form.ts" symbol="FormProps" component="Form" />
