+++
title = "Hooks 通用钩子"
description = "复用键盘事件、受控状态、尺寸测量和定时器等 React 逻辑"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-hooks.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-hooks.js";

# Hooks 通用钩子

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-hooks</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。示例使用公开组件入口，样式由 Wake 自动加载。

Hook 复用行为；useControllableValue 将受控与非受控约定收敛到同一个状态入口。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**内部管理状态**

用 defaultValue 提供内部状态初值。 Hook 复用行为；useControllableValue 将受控与非受控约定收敛到同一个状态入口。 增加两次数量。 Hook 内部保存数量 2。

**交给业务管理**

传入 value 与 onChange，让外部成为唯一数据来源。 Hook 复用行为；useControllableValue 将受控与非受控约定收敛到同一个状态入口。 增加数量后点击外部归零。 同一套行为可由父级状态控制。

</div>

## 常见问题

### 所有状态都应使用这个 Hook 吗？

不需要。简单页面状态直接 useState；需要同时支持受控与非受控的可复用能力再使用它。


## 更多示例

<details>
<summary>展开进阶示例（2 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### useDragResize 拖拽调整尺寸 — 把 handleProps 铺到分隔条上即可；edge 决定把手在目标的哪一缘（方向系数），dragging 可用于拖拽期间关闭过渡动画

##### Use Key Down — Use Key Down 示例

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-hooks/workbench/)

## 使用说明

提供可复用的 React Hook，覆盖键盘事件、受控状态、尺寸测量、定时器和拖拽调整等逻辑。


### 主要功能

- 键盘与事件：`useKeyDown`、`useEventCallback`。
- 受控状态：`useControllableValue`、`useControllableOpen`。
- 尺寸与布局：`useResizeObserver`、`useSize`、`useDragResize`。
- 定时任务：`useTimeout`、`useInterval`。
- 环境与历史值：`useMediaQuery`、`usePrevious`、`useIsomorphicLayoutEffect`。

Hook 均为具名导出，使用时从 `@crab-dev/rc-hooks` 按需导入。
