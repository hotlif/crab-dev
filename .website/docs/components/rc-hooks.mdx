+++
title = "Hooks 通用钩子"
description = "复用状态管理与浏览器相关逻辑，为组件和业务交互提供基础能力。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-hooks.js";

# Hooks 通用钩子

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-hooks</div>

<div id="基础示例" data-docs-example-anchor /><div id="全部示例" data-docs-example-anchor />

## 示例

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### useDragResize 拖拽调整尺寸 — 把 handleProps 铺到分隔条上即可；edge 决定把手在目标的哪一缘（方向系数），dragging 可用于拖拽期间关闭过渡动画

##### Use Key Down — Use Key Down 示例

</div>

<ComponentDemos demos={demos} />

[打开完整组件工作台](/components/rc-hooks/workbench/)

## 使用说明

提供可复用的 React Hook，覆盖键盘事件、受控状态、尺寸测量、定时器和拖拽调整等逻辑。


### 主要功能

- 键盘与事件：`useKeyDown`、`useEventCallback`。
- 受控状态：`useControllableValue`、`useControllableOpen`。
- 尺寸与布局：`useResizeObserver`、`useSize`、`useDragResize`。
- 定时任务：`useTimeout`、`useInterval`、支持暂停后继续剩余时间的 `useCountdown`。
- CSS 退场：`usePresence` 保留节点直到自身过渡结束，重新打开会取消旧的卸载流程。
- 环境与历史值：`useMediaQuery`、`usePrevious`、`useIsomorphicLayoutEffect`。

Hook 均为具名导出，使用时从 `@crab-dev/rc-hooks` 按需导入。

`usePresence(open, onExitComplete?)` 返回 `present`、`ref` 和 `state`。用 `present` 控制挂载，将 `ref` 绑定到执行退场动画的元素，用 `data-state={state}` 区分 `open` / `closed`，并通过 CSS `@starting-style` 定义入场。它只观察动画完成，不计算动画帧；减少动态效果偏好下没有过渡时立即完成卸载。不要将它绑定到无限动画的 spinner 上。

`useCountdown(duration, paused, onFinish?)` 使用毫秒计时，返回最近一次暂停或恢复时的剩余时间快照，不逐帧刷新。`duration <= 0` 时不自动结束；修改时长会重新计时，卸载时清理计时器。

### 适用场景

多个组件需要共享同一套交互状态机。 浏览器事件、订阅或测量逻辑需要安全地处理挂载和卸载。

只在一个组件中使用的简单状态保留在组件内部；不要用 Hook 隐藏不可见的全局副作用。

### 使用要点

- 先读清输入、返回值和生命周期，再把 Hook 接到组件。
- 受控/非受控逻辑要明确默认值、外部值和变更回调的优先级。
- 事件监听、计时器和观察器必须在卸载时清理。

### 无障碍

- 键盘 Hook 只是能力，最终元素仍需正确角色、名称和焦点管理。
- 拖拽或动画 Hook 必须提供键盘替代操作并尊重减少动态效果。

## 常见问题

### 所有状态都应使用这个 Hook 吗？

不需要。简单页面状态直接 useState；需要同时支持受控与非受控的可复用能力再使用它。
