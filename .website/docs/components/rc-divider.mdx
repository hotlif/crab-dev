+++
title = "Divider 分割线"
description = "分割线, 用于在内容之间划分区隔并表达从属边界"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-divider.js";

# Divider

分割线, 用于在内容之间划分区隔并表达从属边界。

## 组件预览

<div hidden aria-hidden="true" data-docs-search-index="demos">

### Demo 搜索索引

##### 基础用法 — 默认渲染一条语义分隔线（role=separator）, 用于切分上下两段内容。

##### 语义线与装饰线 — 默认线会被读屏播报为「分隔线」; 当线只是重复了已有的视觉分组时, 用 decorative 把它移出无障碍树, 避免噪声反馈。

##### 留白档位 — 线两侧的留白决定分组强度（格式塔接近性）: 留白越大, 两段内容「离得越远」。

##### 带文字 — 传入 children 即成为分节标题。textAlign 控制文字落点, plain 让文字退回正文字重。

##### 线型 — solid 用于正式分区; dashed / dotted 语义更轻, 常用于表达「可选」或「临时」的边界。

##### 竖向分割线 — direction=vertical 用于行内切分: 按钮组、状态栏、面包屑。高度跟随当前字号。

</div>
<ComponentDemos demos={demos} />

## 何时使用

- 需要把一段连续内容切成若干可辨认的分组时（表单分节、列表分段、卡片内分区）
- 需要在行内切分并列的操作或状态时（按钮组、面包屑、状态栏）用 `direction="vertical"`
- 需要给分组加一个轻量小标题时, 直接把文字作为 `children` 传入
- 若分组已由留白、标题或卡片边框表达清楚, 则不必再画线——线越少, 每条线越有意义


## 无障碍

分割线是少数「画不画都不影响功能, 但画错就制造噪声」的组件, 因此它的无障碍取决于一个判断：
**这条线是否承载了视觉之外的信息？**

- **承载**（默认）：渲染为 `role="separator"`, 读屏会播报「分隔线」, 帮助盲用户建立与视觉一致的分组模型
- **不承载**：设 `decorative`, 渲染为 `role="none"` + `aria-hidden`, 读屏完全跳过

带文字的分割线始终是语义线：静态 `separator` 的子节点在 ARIA 中按 presentational 处理, 文字不会
被自动读出, 因此组件用 `aria-labelledby` 回指文字, 使其播报为「分组标题 分隔线」。也正因如此,
类型层禁止在带文字的线上声明 `decorative`——不得把可读内容藏出无障碍树。




## API

<API source="../_generated_api/rc-divider.ts" symbol="DividerPropsSearchIndex" component="Divider" />
