+++
title = "Divider 分割线"
description = "分隔相邻内容与区块，帮助识别信息结构和阅读顺序。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-divider.js";

# Divider 分割线

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-divider</div>

<div id="基础示例" data-docs-example-anchor /><div id="全部示例" data-docs-example-anchor />

## 示例

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 基础用法 — 默认渲染一条语义分隔线（role=separator）, 用于切分上下两段内容。

##### 语义线与装饰线 — 默认线会被读屏播报为「分隔线」; 当线只是重复了已有的视觉分组时, 用 decorative 把它移出无障碍树, 避免噪声反馈。

##### 留白档位 — 线两侧的留白决定分组强度（格式塔接近性）: 留白越大, 两段内容「离得越远」。

##### 带文字 — 传入 children 即成为分节标题。textAlign 控制文字落点, plain 让文字退回正文字重。

##### 线型 — solid 用于正式分区; dashed / dotted 语义更轻, 常用于表达「可选」或「临时」的边界。

##### 竖向分割线 — direction=vertical 用于行内切分: 按钮组、状态栏、面包屑。高度跟随当前字号。

</div>

<ComponentDemos demos={demos} />

[打开完整组件工作台](/components/rc-divider/workbench/)

## 使用说明

分隔内容或行内操作，明确分组边界。


### 何时使用

- 分隔表单章节、列表分组或卡片内的内容区域。
- 使用 `direction="vertical"` 分隔行内并列操作。
- 将分组标题作为 `children` 传入。

已有留白、标题或边框能够清楚表达分组时，无需额外添加分割线。

### 无障碍

默认使用 `role="separator"` 表达内容分组。纯装饰线可设置 `decorative`，使其通过 `role="none"` 和 `aria-hidden` 从无障碍树中隐藏。

带文字的分割线通过 `aria-labelledby` 关联标题。此时不能设置 `decorative`，以免隐藏分组名称。

### 适用场景

列表、菜单或内容区中相邻组需要轻量边界。 水平或垂直布局需要清楚但不过度强调的结构分隔。

仅靠间距已经能表达分组时无需增加线条；不要让每个元素四周都出现 Divider。

### 使用要点

- orientation 要与布局方向匹配，垂直分隔必须有可计算高度。
- variant 与 inset 用来对齐内容边缘，而不是随意装饰。
- 带文字的分隔要确保文字确实是分组标题而非普通句子。

### 无障碍

- 纯装饰分隔隐藏语义；确实表示主题断点时保留 separator 角色和正确方向。
- 不要只用 Divider 表达不可见的关系，标题与 DOM 结构仍要正确。

## 常见问题

### 带标题时能设置 decorative 吗？

不能。标题提供分组名称，不应被移出无障碍树。

## API

<API source="../_generated_api/rc-divider.ts" symbol="DividerProps" component="Divider" />
