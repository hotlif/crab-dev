+++
title = "ConfigProvider 全局配置"
description = "通过 React Context 注入主题、语言和尺寸，复用 L2 CSS 变量切换主题"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-config-provider.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-config-provider.js";

# ConfigProvider 全局配置

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-config-provider</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

了解 React 属性与 useState，并安装示例使用的 Crab UI 组件。应用入口需加载 rc-theme CSS。

通过 L2 CSS 变量切换主题，并为后代注入语言和尺寸；嵌套区域可以单独覆盖配置。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**配置应用默认值**

为卡片内的组件统一提供深色主题、英文和大尺寸。 配置集中在内容根部，后代组件按各自支持的能力消费。 观察卡片背景、Empty 英文文案和按钮尺寸。 深色卡片显示 No data，按钮使用 large 尺寸。

**交互切换与局部覆盖**

分别切换主题、语言和尺寸，比较全局继承和局部覆盖。 嵌套 Provider 按字段合并配置，组件显式属性优先。 选择 dark、en-US 和 large，再切回 light；也可使用 Tab 与空格操作。 预览实时更新，局部 Light 区域始终浅色，固定小尺寸按钮保持不变。

</div>

## 常见问题

### 哪些组件已自动接入？

主题作用于所有消费 L2 变量的 DOM 后代；尺寸首批接入 Button / ButtonGroup，语言首批接入 Empty。其他组件可通过 useConfig 接入。

### Portal 浮层如何继承主题？

将浮层放在主题容器内，或在 Portal 内再包一层无配置属性的 ConfigProvider，从 React Context 继承配置并重建 CSS 边界。


## API

<API source="../_generated_api/rc-config-provider.ts" symbol="ConfigProviderProps" component="ConfigProvider" />

## 更多示例

<details>
<summary>展开进阶示例（1 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 嵌套配置与主题边界 — 内层仅覆盖指定配置，未指定的语言和尺寸继续继承父级。

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-config-provider/workbench/)

## 使用说明

为应用或局部区域提供可嵌套的默认配置。




### 使用

```tsx
import ConfigProvider from '@crab-dev/rc-config-provider';
import '@crab-dev/rc-theme/css/index.css';

<ConfigProvider theme="dark" locale="en-US" size="large">
    <App />
</ConfigProvider>
```

主题样式入口需在应用中加载一次。Provider 输出一个普通 div，设置 `data-theme` 和 `lang`，
由 rc-theme 覆盖 L2 `--token-semantic-*` 变量；没有内联样式、运行时样式插入或组件层颜色分支。
Provider 本身不绘制背景；后代通过自己的语义令牌消费主题。
应用应把 Provider 放在需要统一配置的内容根部；额外 div 会参与布局。

### 配置与继承

| 属性 | 值 | 根级默认值 |
| --- | --- | --- |
| theme | light / dark | light |
| locale | zh-CN / en-US | zh-CN |
| size | small / middle / large | middle |

嵌套 Provider 按字段继承父级，显式属性覆盖父级，重新传入 undefined 恢复继承。
原生 div 属性和 React 19 ref 可直接传入；语言由 locale 统一设置。
使用具名导出的 `useConfig()` 读取当前有效配置，自定义组件可由此接入。
无 Provider 时 Hook 返回上述默认值。

主题适用于所有消费 L2 变量的后代。当前尺寸自动接入 Button / ButtonGroup，
优先级为 Button.size > ButtonGroup.size > ConfigProvider.size；语言自动接入 Empty 三种预置文案。
Empty 显式 title / description（包括 null）优先于语言默认值。
其余组件的尺寸与文案仍遵循原有 API，可通过 useConfig 逐步接入。

### 嵌套与浮层

允许 dark 内嵌 light，或并列多个独立主题。CSS 同时保留强制颜色支持；主题切换不添加动画。
Provider 不访问 document、存储或系统主题，也不修改 html / body；如需持久化或跟随系统，请由应用传入结果。

React Portal 保留 Context，但不会继承原 DOM 祖先的 CSS 变量。
将浮层挂载在主题容器内，或在 Portal 内容外再包一层不带配置属性的 ConfigProvider，
即可从 Context 继承配置并重新建立 CSS 边界。不会自动修改其他浮层或全局挂载点。

```tsx
createPortal(<ConfigProvider><Overlay /></ConfigProvider>, container)
```

局部主题边界会重新声明 L2 变量；业务变量覆盖应施加到目标边界或其后代。
