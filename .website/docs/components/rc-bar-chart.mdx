+++
title = "BarChart 柱状图"
description = "以柱形比较分类数值，便于观察差异、排名与数据分布。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-bar-chart.js";

# BarChart 柱状图

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-bar-chart</div>

<div id="基础示例" data-docs-example-anchor /><div id="全部示例" data-docs-example-anchor />

## 示例

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 数值标签与参考线 — showValues 在柱端标注数值（空间不足以容纳的自动省略；堆叠模式标注类目合计）；referenceLines 绘制均值 / 目标虚线，参考值自动纳入值轴刻度域。

##### 基础用法 — 单系列柱状图：单系列不出现图例，悬停任意类目列即可读取数值，完整数据同时以隐藏数据表提供给辅助技术。

##### 横向条形与自适应宽度 — 横向条形让类目沿纵轴排列；设置 width 为 auto，通过 rc-auto-sizer 跟随父容器宽度。调整窗口宽度可查看布局变化。

##### 入场与数据更新动画 — 首次挂载时柱体从零值基线逐类目生长；手动切换数据集时从旧值过渡到新值。系统偏好「减弱动态」时直接呈现结果。

##### 多系列分组 — 多系列在类目内并列分组，颜色按分类色板顺序分配并跟随系列；提供 onBarClick 后柱子呈现 pointer 光标。

##### 堆叠模式 — stacked 开启堆叠：正值向上、负值向下分别累计，段与段之间保持 2px 表面留白，仅最外侧段带数据端圆角。

</div>

<ComponentDemos demos={demos} />

[打开完整组件工作台](/components/rc-bar-chart/workbench/)

## 使用说明

基于 [Canvas](/components/rc-canvas) 的 WebGL 2 柱状图。使用 Props 配置数据、坐标轴和展示方式；图例、悬浮提示与无障碍数据表由 HTML 元素呈现。


### 何时使用

- 比较一组类目下的数值大小（单系列），或多个系列在相同类目上的对比（分组）。
- 展示各类目的构成结构（堆叠，正值向上、负值向下分别累计）。
- 数据量大或与其他 rc-canvas 图形同屏、追求 GPU 渲染性能时。

### 主要功能

- **展示方式**：支持单系列、分组、正负值堆叠，以及 `orientation="horizontal"` 横向条形。
- **尺寸与动画**：`width="auto"` 跟随父容器宽度；`animate` 控制入场和更新动画，减少动态效果偏好下直接显示结果。
- **数值与参考线**：`showValues` 显示数值标签，空间不足时省略；`referenceLines` 配置目标值等参考线，并计入值轴范围。
- **系列颜色**：内置 8 色分类色板，也可通过 `series[].color` 覆盖；超过 8 个系列时不渲染，并在开发环境提示。
- **图例筛选**：点击图例切换系列显隐，数据表仍保留全部数据。
- **交互提示**：悬停、键盘聚焦或触屏点按可查看类目数据；Enter 或 Space 触发 `onBarClick`。
- **无障碍数据**：绘制层对辅助技术隐藏，完整数据通过视觉隐藏的表格提供，`aria-label` 用作表格标题。
- **轴标签**：按实际文本宽度计算留白，支持通过 `formatValue` 格式化数值。

### 容器与尺度

**现有实现：** `width="auto"` 使用现有 AutoSizer 跟随父容器，工作台的六个示例采用这一方式；公共默认宽度仍为 600px。父容器需要明确可用宽度，并允许子项收缩。

**选择规则：** 对比数据通常优先随容器适配；类别较多时应同时评估标签密度、横向条形和数据表入口，不能把缩小后的图形作为全文获取方式。显式数值宽度用于有固定绘图区的场景，应由业务提供局部查看区域。

### 适用场景

比较不同类别的数量、金额、排名或完成率。 用堆叠柱展示总量及其组成，并为每个系列提供图例。

类别过多、标签很长或必须查看精确明细时，表格通常更清楚；时间连续趋势可考虑折线图。

### 使用要点

- 数据轴必须从合理基线开始，避免用截断尺度夸大差异。
- series、category 与 value 是三种不同概念，先整理数据再配置视觉。
- 实时更新要保持对象身份和过渡可理解，不能让柱形不断闪烁重排。

### 无障碍

- 颜色之外还要提供图例、标签或可读数据摘要，并确保键盘能取得关键数据。
- 动画应响应减少动态效果偏好；悬停提示也必须能通过键盘焦点获得。

## 常见问题

### 为什么数据对不上分类？

每个系列的 data 长度和顺序必须对应 categories。图表使用 WebGL，业务仍应保留文字或表格替代。

## API

<API source="../_generated_api/rc-bar-chart.ts" symbol="BarChartProps" component="BarChart" />
