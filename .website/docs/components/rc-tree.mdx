+++
title = "Tree 树形控件"
description = "以可展开的层级结构展示数据，适用于目录、组织与资源列表。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-tree.js";

# Tree 树形控件

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-tree</div>

<div id="基础示例" data-docs-example-anchor /><div id="全部示例" data-docs-example-anchor />

## 示例

<div hidden aria-hidden="true" data-docs-search-index="demos">

#### 基础展示

##### 基础用法 — 最基本的树形组件用法，展示节点的展开、收起和选择功能。

##### 全部展开 / 折叠 — 通过 `TreeDataUtil` 提供的 `expandAll` 与 `collapseAll` 方法，一键展开或折叠所有 FOLDER 节点。

##### 显示连接线 — 通过 `showLine` 属性展示树节点之间的连接线。

##### 图标插槽与禁用节点 — 通过 `icon` 字段为节点设置前置图标；`disabled` 字段禁用节点，禁用节点不可点击、不可拖拽、样式置灰。

#### 选择与筛选

##### 复选框 checkable — 通过 `checkable` 开启复选框模式。选中父节点自动级联选中所有子节点；取消选中子节点时，父节点自动变为半选状态。`checkedKeys` 与 `onCheck` 实现受控。

##### 搜索过滤 filterTreeNode — 通过 `filterTreeNode` prop 过滤节点。返回 `true` 的节点及其所有祖先节点均会保留显示，其余节点被隐藏。结合展开所有匹配路径，可实现完整的搜索体验。

#### 编辑与交互

##### 拖拽排序 — 通过 `draggable` 属性启用拖拽功能，支持节点的拖拽排序和层级调整。

##### 拖拽限制 allowDrop — 通过 `allowDrop` 回调控制节点的放置规则。本示例只允许同级排序：禁止 INSIDE（移入子节点），也禁止跨层级的 DOWN/UPWARD（那样会改变父节点）。

##### Inline 节点编辑 — 双击节点标题进入 inline 编辑模式。默认使用内置 ` input `；通过 `renderEditInput` 提供自定义编辑器——本例演示带字数限制与实时校验的自定义输入框。`onCommit(value)` 提交，`onCancel()` 取消。

##### 键盘导航 — 点击树组件后可使用键盘操作：`↑↓` 移动焦点，`→` 展开文件夹，`←` 折叠文件夹或跳转到父节点，`Enter` 选中/取消选中当前节点。

</div>

<ComponentDemos demos={demos} />

[打开完整组件工作台](/components/rc-tree/workbench/)

## 使用说明

展示层级数据，支持展开、选择、搜索和拖拽。


### 何时使用

用于文件目录、组织架构等层级数据的浏览和管理。通过展开、收起和选择定位节点，并按需启用搜索、复选、行内编辑或拖拽。

### 无障碍

- 为树所在区域提供可见标题，搜索框必须使用 `<label>` 建立可访问名称。
- 保留方向键浏览、Enter 选择和清晰的 `:focus-visible` 焦点环；不要在业务层拦截这些按键。
- 节点标题中的图标只作装饰时应从无障碍树隐藏，加载、禁用、展开和选中状态不能只靠颜色表示。
- `aria-label` / `aria-labelledby` 标记实际的键盘焦点容器；节点暴露 `treeitem`、选中、展开和禁用状态。关闭拖拽不会禁用节点选择，键盘选中项会自动滚动到可见区域。
- 拖拽和右键菜单必须提供键盘可操作的等价入口，并在操作后说明结果。

更多通用要求见[无障碍使用指南](/guides/accessibility)。

### 容器与窄屏

**现有实现：** `width` 和 `height` 接收数值，单位为 CSS px。工作台示例通过 [AutoSizer](/components/rc-auto-sizer) 测量父容器，再传入 `Math.min(width, 示例目标宽度)`；父容器必须允许收缩，不能直接传入 `width="100%"`。多选示例在空间不足时把结果摘要换到下一行。

**设计要求：** 长标题应提供全文获取入口；宽度适配不能替代标题可读性、层级键盘导航及拖拽替代操作的验收。


### 节点数据

通过 `treeData` 传入节点数组。`Node` 类型包含唯一标识 `id`、标题 `title`、父节点 `parent`、节点类型 `type` 和加载状态 `loadState`。

- 根节点的 `parent` 为 `null`，子节点通过 `parent` 建立层级关系。
- `type` 使用 `NodeType`，`loadState` 使用 `LoadStateType`；两者均从组件包具名导出。
- 可通过 `disabled` 禁用节点，通过 `icon` 设置前置图标，通过 `height` 指定节点高度（单位为 px）。

### 适用场景

目录、组织架构或资源拥有清楚的父子关系。 用户需要逐层展开，并可能移动、重命名或批量勾选节点。

平面选项使用 List/Select；层级很深且需要精确比较字段时考虑 Tree table。

### 使用要点

- 每个节点使用稳定 key，children 表示真实父子关系。
- expandedKeys、selectedKeys 与 checkedKeys 分别管理不同状态。
- 拖拽 allowDrop 只声明合法位置，数据更新仍由外层完成。

### 无障碍

- 实现 tree/treeitem 层级、展开、选中和集合位置信息，并支持方向键模型。
- 拖拽、内联编辑和筛选都有键盘替代路径，焦点节点在数据更新后仍可定位。

## 常见问题

### 展开和选中为什么要分开？

展开控制可见子节点，选中表示当前操作对象。异步加载时还需要维护节点 loadState。

### 为什么提供树外的操作按钮？

外部按钮演示业务操作如何更新受控的 expandedKeys 和 selectKeys，不是节点交互的替代入口。关闭拖拽不会禁用节点；Tab 可聚焦树，上下方向键选择节点，左右方向键展开、收起或返回父节点，Enter 切换当前节点的选择状态。只有 node.disabled 为 true 时节点才会被禁用。

## API

<API source="../_generated_api/rc-tree.ts" symbol="TreeProps" component="Tree" />
