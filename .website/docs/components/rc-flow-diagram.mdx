+++
title = "FlowDiagram 流程图"
description = "基于 Canvas 绘制流程图，支持自动布局、连线路由和视口控制"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-flow-diagram.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-flow-diagram.js";

# FlowDiagram 流程图

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-flow-diagram</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。示例使用公开组件入口，样式由 Wake 自动加载。

节点与边是数据，布局结果给出节点矩形和连线路径，再交给图形组件绘制。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**连接两个节点**

声明 nodes 与 edges，再把布局结果传给 FlowNode 和 FlowEdge。 节点与边是数据，布局结果给出节点矩形和连线路径，再交给图形组件绘制。 对照节点与连接方向。 两个节点从上到下连接。

**调整流程方向**

通过 elk.direction 切换布局方向，业务节点保持不变。 节点与边是数据，布局结果给出节点矩形和连线路径，再交给图形组件绘制。 切换布局方向。 节点重排，连接关系仍保持。

</div>

## 常见问题

### 为什么需要文字流程？

流程图不应只靠画布传达业务关系。保留文字步骤，让辅助技术也能读取流程。


## API

<API source="../_generated_api/rc-flow-diagram.ts" symbol="FlowDiagramProps" component="FlowDiagram" />

## 更多示例

<details>
<summary>展开进阶示例（3 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### FlowEdge 流动动效 — flowSpeed 让虚线沿边流动，表达数据流向：正值顺流、负值逆流、数值越大越快。流动相位按累计弧长逐段衔接，拖拽节点改变走线后，图案跨拐角与交叉缺口依旧连续；系统开启「减弱动态效果」时自动降级为静态虚线。

##### FlowEdge 样式 — FlowEdge 支持实线 / 虚线、单向 / 双向箭头、自定义颜色。节点可拖，边走线自动绕开节点。

##### ELK 自动图布局 — FlowDiagram 内置 ELK 布局 + 正交走线。节点可拖拽，边自动绕开节点。切换算法 / 方向后自动重新布局并适应视图。

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-flow-diagram/workbench/)

## 使用说明

基于 `rc-canvas` 的流程图组件，使用 ELK.js 计算布局，并提供正交连线路由。
调用方传入节点和边数据，通过子渲染函数使用布局结果，自定义节点与连线的内容。


### 何时使用

- 展示有向流程图或架构图，并自动计算节点位置。
- 使用正交折线连接节点，或手动指定部分连线路径。
- 拖拽节点、缩放和平移画布，或通过 `fitView` 将内容适配到视口。

### 主要功能

- **ELK 自动布局**：内置 Web Worker + ELK.js，支持 `layered`（层次）、`mrtree`（树形）、`stress`（力导向）等算法。
- **正交边路由**：基于 A\* 的障碍物避让，自动分配端口、生成折线，支持虚线与箭头方向。
- **Render Prop 模式**：`<FlowDiagram>` 提供 `children({ nodeRects, routes, zoom, loading, controls })`，节点 / 边的渲染完全由消费方控制。
- **画布封装**：内部使用 `@crab-dev/rc-canvas`，常规接入无需直接操作 WebGL。
- **节点拖拽**：`FlowNode` 的 `draggable` + `onDrag(dx, dy)` 支持拖拽覆盖布局位置。
- **手动走线**：通过 `manualRoutes` 对单条边指定端口与途经点，其余边仍自动路由。
- **无限网格 + 视口控制**：内置 `InfiniteGrid` 背景网格，支持 `useFlowDiagramControls()` 程序化缩放 / 平移 / fitView。

### 窄屏查看

**现有示例：** 节点和连线保留二维布局，外层提供有名称、可聚焦的局部横向滚动区域。视口变窄时，可查看完整画布，不把整个文档页面撑宽。

**设计要求：** 局部滚动与图内平移是不同操作。滚动区域获得焦点后可用方向键查看；图内拖拽、缩放和节点编辑仍需各自的键盘替代及状态反馈，不能仅凭外层可聚焦就认定流程图满足全部无障碍要求。
