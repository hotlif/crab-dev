+++
title = "Button 按钮"
description = "用于提交、确认或触发即时操作的按钮"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-button.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-button.js";

# Button 按钮

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-button</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

了解 JSX 和 useState 即可。先完成快速开始中的安装与样式接入。

用清晰的按钮文字表达操作，用外观区分操作优先级。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**渲染主操作**

让“保存资料”成为主操作 将 appearance 设为 primary，让用户快速识别当前最重要的操作。 聚焦按钮，观察鼠标悬停与键盘焦点反馈。 保存操作以主按钮展示；本步尚未接入保存逻辑。

**连接交互状态**

让按钮响应点击，并切换外观 用 React 状态分别管理保存次数和按钮外观，让两种变化互不干扰。 点击保存两次，再切换按钮外观。 保存次数变为 2；外观变化不会清空计数。

</div>

## 常见问题

### 链接外观会自动导航吗？

不会。appearance="link" 只改变外观；需要导航时还应传入 href。


## API

<API source="../_generated_api/rc-button.ts" symbol="ButtonProps" component="Button" />

## 更多示例

<details>
<summary>展开进阶示例（12 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

#### 基础与外观

##### 基础配置 — 在属性控件中组合文字、外观、尺寸与常用状态

##### 外观设置 — 通过 `appearance` 属性设置按钮外观

##### 圆形按钮 — `shape=\"circle\"` 搭配纯图标（无 children）适用于工具栏操作图标

##### 危险操作 — 使用 `appearance=\"danger\"` 标识删除、清空等破坏性操作

##### 图标按钮 — 通过 `icon` 属性设置按钮图标

##### 图标位置 — `icon` 在文字左侧，`iconAfter` 在文字右侧，可同时使用

##### 链接按钮 — 传入 `href` 时渲染为 ` a ` 元素，保留所有 Button 样式与交互

##### 按钮尺寸 — 通过 `size` 属性设置按钮尺寸

#### 状态与反馈

##### 禁用状态 — 添加 `disabled` 属性即可让按钮处于禁用状态

##### 加载中 — 添加 `loading` 属性即可让按钮处于加载状态

##### 选中状态 — `isSelected` 用于工具栏过滤器、视图切换等 toggle 场景

#### 组合

##### 按钮组 — `ButtonGroup` 统一管理子按钮的 `size` 和 `appearance`，适用于工具栏场景

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-button/workbench/)

## 使用说明

### 何时使用

用于保存、提交、取消等操作。一个操作区域应突出一个主要操作，其余按钮按重要程度选择外观。

`appearance` 提供六种外观：

| 值 | 用途 |
| --- | --- |
| `primary` | 强调主要操作 |
| `subtle` | 常规或次要操作 |
| `dashed` | 添加等辅助操作 |
| `text` | 低强调度的文字操作 |
| `link` | 链接样式的操作 |
| `danger` | 删除、清空等危险操作 |

传入 `href` 才会渲染为链接元素；`appearance="link"` 只设置外观。

### 无障碍

- 优先使用清晰的可见文字；只有图标的按钮必须传入描述操作目的的 `aria-label`。
- 普通按钮传入 `isSelected` 时会同步默认 `aria-pressed`；未传入时仍是普通动作。显式 `aria-pressed` 优先，适合混合状态等情形。链接和显式 tab 等其他角色应使用对应的 current / selected 语义。
- 异步操作使用 `loading` 提供 `aria-busy` 与防重复提交反馈；不要仅用颜色表示危险或选中状态。
- 保持原生按钮的键盘行为和可见焦点，不要用普通元素模拟按钮。

更多通用要求见[无障碍使用指南](/guides/accessibility)。


### 视觉状态与验证

加载时保留可读的背景与前景，默认 opacity 为 1；已有 `--button-opacity-loading` 覆盖继续有效。点击和捕获处理函数在 loading 时阻止激活及表单提交；仅使用 loading 时按钮保留焦点，同时设置 disabled 则遵循原生禁用行为并退出键盘顺序。业务仍需用 pending 状态管理整个请求周期，Promise 点击锁不能代替提交状态。禁用链接不应用悬停或按压外观，虚线和普通按钮保留边框占位以避免宽度跳动。

粗指针环境下，独立操作的命中区至少为 44×44 CSS px。按钮通过最小尺寸扩展操作区域。长标签采用正文行高；紧密组合需检查目标区域没有相互重叠。此规则不新增 density 属性，也不把全部组件改成同一视觉高度。

键盘焦点使用公共 L2 焦点角色，通过 L3 覆盖可调整；减少动态效果时移除非必要过渡。实际浏览器应逐项检查 Tab / Shift+Tab、原生激活方式、两种主题、强制颜色、长标签与粗指针布局。当前源码修复与自动检查状态见质量审查记录，不能把 CSS 中存在媒体查询等同于设备验收通过。

规则依据：[状态系统](/design/interaction)、[无障碍与键盘](/design/accessibility)、[布局与密度](/design/layout)。
