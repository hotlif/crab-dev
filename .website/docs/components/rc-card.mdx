+++
title = "Card 卡片"
description = "将相关内容和操作组织为独立容器，建立清晰的信息分组。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-card.js";

# Card 卡片

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-card</div>

<div id="基础示例" data-docs-example-anchor /><div id="全部示例" data-docs-example-anchor />

## 示例

<div hidden aria-hidden="true" data-docs-search-index="demos">

#### 基础与布局

##### 基础用法 — title 与 extra 组成标题区, 裸内容自动落入内容区。

##### 自由拼装 — 传入 Card.Cover / Header / Body / Footer 结构子组件时切换为自由模式, 完全掌控区块次序。

##### 封面与悬停反馈 — cover 出血铺满顶部；hoverable 显示状态层并提升一级阴影，封面保持原尺寸。

##### 三档尺寸 — size 同步缩放内边距 / 圆角 / 标题字号, 与其他组件的尺寸档位对齐。

##### 三种变体 — elevated 微投影（默认）/ outlined 描边 / filled 弱灰底, 按承载面的层次选用。

#### 交互与状态

##### 整卡可点击 — clickable 赋予按钮语义：浮起反馈、键盘激活与焦点环; extra 与 actions 的点击自动与整卡隔离。

##### 交互状态 — 三种变体保留各自的表面色和层级，支持鼠标、键盘及禁用状态。

##### 加载骨架 — loading 时以骨架占位并标注 aria-busy, 结构与真实内容对应, 完成后无跳动切换。

</div>

<ComponentDemos demos={demos} />

[打开完整组件工作台](/components/rc-card/workbench/)

## 使用说明

将标题、封面、正文和操作组织为独立的信息单元。


### 何时使用

- 将标题、媒体、正文和操作组合为独立的信息单元。
- 在列表或网格中展示产品、文章、项目等内容。
- 通过 `clickable` 将整张卡片设为操作入口，或通过 `hoverable` 提供悬停反馈。

### 两种使用形态

**属性配置**：使用 `title`、`extra`、`cover` 和 `actions` 描述常规结构，直接传入的 `children` 放入内容区。

```tsx
<Card title="标题" extra={<Button appearance="link">更多</Button>} actions={[<Button key="ok">确认</Button>]}>
    正文内容
</Card>
```

**组合子组件**：`children` 中包含 `Card.Cover`、`Card.Header`、`Card.Body` 或 `Card.Footer` 时，
卡片按传入顺序组织区块。`Card.Meta` 组合头像、标题和描述，可用于两种模式。

```tsx
<Card hoverable>
    <Card.Header title="标题" />
    <Card.Body>正文</Card.Body>
    <Card.Cover><img src="..." alt="..." /></Card.Cover>
    <Card.Footer><Button>确认</Button></Card.Footer>
</Card>
```

### 适用场景

多个同级内容单元需要重复排列，例如项目、文章或商品摘要。 一组内容需要自己的标题、媒体、正文和少量就近动作。

内容天然属于同一连续页面时优先用标题、留白和 Divider；不要层层嵌套卡片。

### 使用要点

- 先确定 elevated、filled 或 outlined 变体，再保持同一列表中的层级一致。
- 可点击卡片需要一个清楚主动作，次要按钮不能与整卡点击冲突。
- loading 骨架要尽量保持最终内容结构，避免加载完成后大幅跳动。

### 无障碍

- 整卡可点击时使用真实链接或按钮语义，并给内部独立操作清楚的焦点顺序。
- 标题层级、图片替代文本和状态信息不能因为放进 Card 就省略。

## 常见问题

### 什么时候使用结构子组件？

当内容区块顺序需要自定义时，组合 Card.Header、Card.Body 等子组件。

## API

<API source="../_generated_api/rc-card.ts" symbol="CardProps" component="Card" />
