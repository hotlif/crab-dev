+++
title = "Drawer 抽屉"
description = "抽屉组件：从屏幕边缘滑出的浮层面板"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-drawer.js";

# Drawer

从屏幕边缘滑出的浮层面板，承载二级任务、表单或详情内容。

## 组件预览

<div hidden aria-hidden="true" data-docs-search-index="demos">

### Demo 搜索索引

##### 基础用法 — 通过 `open` 控制显示；点击遮罩、关闭按钮或按 `Esc` 均可关闭。

##### 嵌套打开 — 支持层层嵌套打开（示例共 10 层）；每层独立维护开关状态，关闭后不影响下层。

##### 四个方向 — 通过 `placement` 控制抽屉从哪个方向滑出。

##### 底部操作区 — 通过 `footer` 传入操作按钮，适合承载表单提交场景。

</div>
<ComponentDemos demos={demos} />

## 何时使用

- 需要进行较长的表单输入、查看详情，且不希望跳转页面打断主流程时
- 在当前页面之上临时呈现辅助信息，结束后可快速返回主界面
- 作为 Dialog 的侧栏版本：承载更多内容，尤其适合宽窄比不对称的场景



## API

<API source="../_generated_api/rc-drawer.ts" symbol="DrawerPropsSearchIndex" component="Drawer" />
