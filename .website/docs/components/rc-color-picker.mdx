+++
title = "ColorPicker 颜色选择器"
description = "选择和编辑颜色，支持格式切换、预设色板与透明度调整"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-color-picker.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-color-picker.js";

# ColorPicker 颜色选择器

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-color-picker</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。完整代码包含公开导入和样式入口。

颜色值使用 OKLCH 对象；格式切换属于展示，业务仍保存同一种数据结构。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**选择颜色**

提供 OKLCH 格式的 defaultValue。 颜色值使用 OKLCH 对象；格式切换属于展示，业务仍保存同一种数据结构。 打开色板，调整色相。 触发器显示所选颜色。

**读取颜色值**

通过 value 与 onValueChange 保存颜色对象。 颜色值使用 OKLCH 对象；格式切换属于展示，业务仍保存同一种数据结构。 修改颜色，再恢复蓝色。 数值与色板同步恢复。

</div>

## 常见问题

### 需要保存十六进制字符串吗？

组件的受控值使用 OKLCHValue；需要其他格式时在业务边界显式转换。


## API

<API source="../_generated_api/rc-color-picker.ts" symbol="ColorPickerProps" component="ColorPicker" />

## 更多示例

<details>
<summary>展开进阶示例（2 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 颜色选择器 — 可视化取色:透明度、文本输入、格式切换、预设色板、吸管取色、受控/非受控、尺寸与禁用。

##### 颜色面板 — 内嵌式颜色面板:亮度 / 色度 / 色相 / 透明度 + 文本输入 + 预设色板。

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-color-picker/workbench/)

## 使用说明

选择和编辑颜色，支持格式切换、预设色板与透明度调整。


### 何时使用

- 为文字、图形或背景选择颜色。
- 通过预设色板限制常用颜色，或通过输入框精确编辑色值。
- 需要切换颜色显示格式或调整透明度。

格式切换只改变显示方式，输出统一为 OKLCh。吸管取色依赖浏览器的 EyeDropper API。
