+++
title = "Prose 长文本排版"
description = "为 Markdown、MDX 或富文本生成的内容提供统一排版样式"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-prose.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-prose.js";

# Prose 长文本排版

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-prose</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。完整代码包含公开导入和样式入口。

Prose 为已经生成的 HTML 或 React 内容提供样式，不负责解析 Markdown。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**包裹正文**

将语义化标题、段落和列表作为 children。 Prose 为已经生成的 HTML 或 React 内容提供样式，不负责解析 Markdown。 比较各层级文字与间距。 正文形成统一的排版结构。

**调整阅读尺寸**

通过 size 控制排版密度。 Prose 为已经生成的 HTML 或 React 内容提供样式，不负责解析 Markdown。 切换阅读尺寸。 标题、正文和间距一起变化。

</div>

## 常见问题

### 直接传 Markdown 字符串会解析吗？

不会。先由 Markdown/MDX 工具解析，再将结果放入 Prose。


## API

<API source="../_generated_api/rc-prose.ts" symbol="ProseProps" component="Prose" />

## 更多示例

<details>
<summary>展开进阶示例（4 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 基础用法 — 默认 `size=base`，包含标题、段落、链接、粗体等基础排版元素

##### 语义标签 — 通过 `as` 属性指定根元素标签：`div`（默认）、`article`、`section`、`main`

##### 尺寸变体 — 通过 `size` 属性设置排版尺寸：`sm`、`base`（默认）、`lg`、`xl`

##### 表格与定义列表 — 展示表格与定义列表的排版效果

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-prose/workbench/)

## 使用说明

为 Markdown、MDX 或富文本生成的内容提供统一排版样式。


### 何时使用

- 渲染 Markdown / MDX 内容时，提供一致的排版体验。
- 文章页面、文档页面、博客内容的排版。
- 需要对标题、段落、列表、代码块、表格等元素统一样式。

### 主要功能

- 零运行时，基于 Crab CSS 编译为静态 CSS。
- 支持四种尺寸变体：`sm`、`base`、`lg`、`xl`。
- 支持 `invert` 暗色排版模式。
- 支持 `as` 语义标签：`div`、`article`、`section`、`main`。
- 完整的排版规则覆盖：标题、段落、链接、列表、引用、代码、表格、媒体、定义列表。
- 三层设计令牌架构，支持主题覆写。
