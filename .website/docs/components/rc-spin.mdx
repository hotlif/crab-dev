+++
title = "Spin 加载中"
description = "标识正在加载或处理的区域，让用户了解当前等待状态。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-spin.js";

# Spin 加载中

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-spin</div>

<div id="基础示例" data-docs-example-anchor /><div id="全部示例" data-docs-example-anchor />

## 示例

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 基础用法 — 默认即处于加载中, 渲染一枚匀速旋转的指示环, 并以 role=status 向读屏播报。

##### 延迟显示 — 秒回的请求不该闪一下转圈。delay 内完成的操作全程无指示器; 超出才说明它确实耗时, 此时才给反馈。

##### M3 Expressive 加载指示器 — 需要更醒目的品牌反馈时使用 expressive 形变；紧凑控件内继续使用默认 circular 指示环。

##### 自定义指示器 — indicator 替换默认的旋转环; 无障碍语义（role=status / aria-label）仍由 Spin 统一兜底。

##### 尺寸 — 通过 size 设置 small / middle / large 三档; 指示器与提示文案会一同缩放。

##### 提示文案 — tip 说明「正在做什么」, 比一枚沉默的转圈更能安抚等待。它同时成为读屏播报的内容。

##### 包裹内容 — 传入 children 即笼罩该区域: 内容变淡并被 inert 阻断——鼠标点不到, 键盘 Tab 也进不去。

</div>

<ComponentDemos demos={demos} />

[打开完整组件工作台](/components/rc-spin/workbench/)

## 使用说明

显示耗时操作的加载状态，支持延迟显示和区域加载遮罩。


### 何时使用

- 请求或计算需要等待时，显示操作仍在进行。
- 局部区域刷新时，使用包裹模式标记加载范围。
- 内容结构已知时，可使用 Skeleton 保留布局；进度可计算时，应展示明确的进度。

### 延迟显示

`delay` 控制指示器出现前的等待时间，避免短请求导致指示器频繁闪现。例如 `delay={300}` 表示加载持续超过 300 ms 后才显示。

延迟只影响出现时机。`spinning` 变为 `false` 时，指示器立即隐藏。

### Material 3 指示器

默认的 `variant="circular"` 适合按钮、表格和紧凑区域。需要更醒目的品牌化等待反馈时，可使用 `variant="expressive"`；它通过形状变化表达持续活动，并遵循 Material 3 Expressive 的形状与动态设计语言。

两种指示器都只表达“正在进行”，不表示可计算的进度。能够确定完成比例时，应使用进度条或进度环。

### 复用 SpinIndicator

`Spin` 提供 `role="status"` 和 `aria-live`。宿主已提供加载语义时，例如按钮已设置 `aria-busy`，可改用具名导出的 `SpinIndicator`，避免重复播报。

`SpinIndicator` 和 `ExpressiveSpinIndicator` 都使用 `aria-hidden` 隐藏纯视觉图形，加载语义由宿主负责。尺寸与配色可通过 CSS 变量调整：

```tsx
import { css } from '@crab-dev/css';
import { SpinIndicator, TokenVars as spinVars } from '@crab-dev/rc-spin';

// 指示环随宿主字号缩放，并使用宿主前景色。
const style = css`
    --rc-spin-size: 1em;
    ${spinVars['ring.indicator.stroke']}: currentColor;
    ${spinVars['ring.track.stroke']}: transparent;
`;
```

`rc-button` 使用 `SpinIndicator`，`rc-table-pro` 使用 `Spin`，共同复用加载指示样式。

### 无障碍

- 使用 `role="status"` 和 `aria-live="polite"` 提供加载状态播报。
- `tip` 提供具体的操作说明；未设置时使用 `label`，默认值为“加载中”。
- 包裹模式为容器设置 `aria-busy`，并通过 `inert` 阻止内容区的指针交互和键盘聚焦。
- 在 `prefers-reduced-motion` 下，将旋转改为低频透明度变化，保留加载反馈。

### 适用场景

局部请求或计算没有可靠完成比例。 内容需要暂时阻止重复操作，并用 tip 解释正在做什么。

页面结构可预测时 Skeleton 更稳定；有真实进度值时使用 determinate Progress。

### 使用要点

- spinning 控制状态，delay 避免极短任务闪一下。
- wrapper 模式只覆盖相关区域，不应冻结整个应用。
- tip 使用具体动词，如“正在保存报表”，不要只写“加载中”。

### 无障碍

- 容器设置 busy 状态并提供简短 status 文本，完成后及时移除。
- 减少动态效果时保留静态进度提示；不要让装饰指示器进入 Tab 顺序。

## 常见问题

### 示例会请求后端吗？

不会。这里的 700 ms 定时等待模拟请求；接入业务时替换为真实异步函数。

## API

<API source="../_generated_api/rc-spin.ts" symbol="SpinProps" component="Spin" />
