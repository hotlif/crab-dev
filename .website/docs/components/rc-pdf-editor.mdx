+++
title = "PdfEditor PDF 编辑器"
description = "在浏览器本地修改 PDF 文字对象、图片与页面"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-pdf-editor.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-pdf-editor.js";

# PdfEditor PDF 编辑器

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-pdf-editor</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

接入项目需要部署 Worker 与 WASM 静态资源。初始业务文档通过 initialDocument 提供，后续通过 ref.open 切换。修改文字需提供覆盖目标字符的 TTF 字体。

本地打开 PDF，按对象编辑文字、图片，整理页面并保存真实 PDF。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**打开并编辑文档**

连接本地运行时，打开 PDF 并编辑对象。 独立 Worker 处理 PDF，Canvas 显示文字和图片，自有属性控件提交修改。 打开工作台，选择 PDF；配置 TTF 字体后从对象列表选择文字，编辑内容并应用修改。页面操作位于顶部工具栏。 修改显示在当前页；可以撤销、重做并保存 PDF。

**启用只读浏览**

在审核场景中保留查看和导出。 同一组件支持只读入口，无需维护另一套 PDF 预览界面。 设置 readOnly，打开 PDF 后翻页、缩放或查看对象属性。 编辑按钮不可用，页面导航、缩放、提取和保存仍可使用。

**配置图标与业务动作**

按业务需要组合编辑器入口，并追加自己的按钮。 稳定的动作 ID 将展示配置与文档命令分开，新增业务动作不需要修改组件内部。 在工作台逐项或分组勾选图标，切换只读模式，再点击工具栏末尾的文档摘要。 图标立即显示或消失，文档不会重载；扩展按钮输出当前文档摘要。

**接入跨页区域 OCR 插件**

采集相邻两页交界区域并交给自有识别引擎。 选区与原始图像采集由编辑器负责，业务插件独立管理识别和结果处理。 打开跨页 Demo，点击采集两页交界样例或手动框选，查看拼接 PNG 和页坐标；切换安装开关验证插件卸载。 两页区域去掉页间留白后拼接，返回坐标映射；默认适配器只展示图像，不执行文字识别。

</div>

## 常见问题

### 为什么有些文字不能修改？

转曲文字、复杂嵌套、裁剪或无法识别 Unicode 的对象保留原样。对象属性会给出原因。

### 没有配置字体可以使用吗？

可以浏览、整理页面、编辑图片，以及变换原文字。修改文字内容和新增文字需要覆盖相应字符的 TTF 字体。

### 文件会上传到服务器吗？

默认不会。PDF 编辑和导出在浏览器的 Worker 中执行；提供 onSave 或 OCR recognize 适配器后，是否上传由宿主实现决定。

### 插件是否内置 OCR 模型？

没有。组件提供跨页选区、PNG 采集、坐标映射与取消信号；使用方通过 recognize 注入浏览器模型或服务端识别接口。


## API

<API source="../_generated_api/rc-pdf-editor.ts" symbol="PdfEditorProps" component="PdfEditor" />

## 更多示例

<details>
<summary>展开进阶示例（4 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 本地编辑 — 打开 PDF，上传项目提供的 TTF 字体后修改文字；图片与页面操作无需额外字体。

##### 只读浏览 — 保留打开、翻页、缩放、对象查看、提取和导出，禁用编辑。

##### 跨页区域与 OCR 插件 — 框选相邻页交界区域，按页面坐标裁切并拼接为真实 PNG；通过 recognize 回调接入自己的 OCR 引擎。示例适配器只展示输入图像，不执行文字识别。

##### 图标显隐与动作扩展 — 逐项或分组控制全部内置图标，实时预览配置，并追加一个业务动作。隐藏入口不会限制快捷键和 ref。

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-pdf-editor/workbench/)

## 使用说明

基于自有 Canvas 与表单控件，使用独立 Worker 中的 PDFium 编辑和保存真实 PDF 对象。


### 何时使用

需要在桌面浏览器中修改既有文字对象、调整图片，或整理文档页面时使用。默认在浏览器本地处理文件；接入保存或 OCR 服务时，是否上传由宿主实现决定。

### 资源与字体

部署包内 `@crab-dev/rc-pdf-editor/worker` 与 `@crab-dev/rc-pdf-editor/wasm` 导出的文件，通过必填 `runtime` 指定地址；也可以提供 `wasmBinary`。没有默认 CDN。修改文字内容和新增文字需提供覆盖相应字符的 TTF 字体，示例允许从本地选择字体。

### 编辑范围

文字按对象处理，不进行段落重排；图片支持 PNG、JPEG、WebP。页面支持插入、删除、旋转、前后移动、合并与提取。转曲文字、嵌套组合、裁剪及描边文字保留原样，并在对象属性中显示原因。

中央预览将各页纵向连续排列，使用滚轮或滚动条浏览，不显示分页控件。点击左侧缩略图定位页面，滚动时同步当前页和对象树；底部保留缩放与适应页面。预览区支持 PageUp/PageDown、Home/End，普通滚轮不改变缩放倍率。

按住 Ctrl（macOS 也可使用 Command）加滚轮，以鼠标位置为中心缩放，也可在底部直接输入百分比。选择对象、切换侧栏和调整窗口大小保留倍率；首次打开文档或主动点击“适应页面”时才重新适应。

底栏使用紧凑百分比输入框，右侧四角框图标用于适应页面；悬停或键盘聚焦时显示中文提示。

Ctrl/Cmd+Z 撤销，Ctrl/Cmd+Shift+Z 或 Ctrl/Cmd+Y 重做，Delete 删除，方向键移动，Shift+方向键移动 10 pt，Escape 取消选择。文字输入和中文输入法组合期间不接管这些快捷键。对象列表提供画布之外的键盘入口。



通过 `toolbar.visibility` 按动作 ID 实时显示或隐藏图标，例如 `{ addText: false, deletePage: false }`。未配置的动作默认显示；显隐不会重载文档，也不会改变快捷键或编辑权限。

`toolbar.extraActions` 可向 `main`、`tools`、`status` 追加业务图标，接收公开命令接口，并共用忙碌状态及 `onError`。`PDF_EDITOR_ACTIONS` 导出完整只读目录；配置语义与扩展示例见包内 `docs/TOOLBAR.md`。

`plugins` 接入带安装、卸载、取消生命周期的业务插件；`createRegionOcrPlugin({ id, recognize, onResult })` 将框选、图像采集与自有 OCR 引擎组合。`selectRegion()` 支持同一 PDF 相邻两页的交界区域，`captureRegion()` 从 PDF 内容渲染 PNG 并去掉页间界面空隙，返回逐页坐标映射。只读模式同样可用。工作台 Demo 展示真实采集图像，默认不执行识别；完整接口见包内 `docs/PLUGINS.md`。

初始文件通过 `initialDocument={{ id, source, fileName? }}` 提供，后续文件切换调用 `ref.open()`，清空调用 `ref.close()`。`runtime` 与初始文档只在挂载时读取。

宿主保存按钮调用 `ref.save()`，与工具栏共用 `onSave` 持久化流程。`exportPdf()` 和 `extractPages()` 返回包含 Blob、文档身份、会话和版本的快照，不自动下载或清除未保存状态。完整迁移说明见包内 `docs/API-REDESIGN.md`。



### 设计参考

标准配色、顶部停靠工具栏依据 M3 的桌面布局说明扩展为文档工作区。Material Web 未提供 Toolbar，实现复用本库 Button 等控件。详细资源部署、接口、限制及设计来源见包内 `docs/README.md`。
