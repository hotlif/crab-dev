+++
title = "CronPicker Cron 表达式选择器"
description = "Cron 表达式选择器, 支持五段式表达式的可视化编辑、手动输入解析与下次执行时间预览"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-cron-picker.js";

# CronPicker

Cron 表达式选择器:五段式(分 时 日 月 周)表达式的可视化编辑与手动输入。

## 组件预览

<div hidden aria-hidden="true" data-docs-search-index="demos">

### Demo 搜索索引

##### 基础用法 — 点击输入框打开可视化面板;也可直接手输表达式,回车提交(非法输入会以 error 边框提示并在失焦时回退)

##### 受控与常用预设 — 受控模式配合快捷预设:外部一键切换常用调度周期,面板与输入框同步跟随

##### 尺寸与状态 — 三档尺寸、禁用态与外部校验状态;预览条数可通过 previewCount 调整或关闭

</div>
<ComponentDemos demos={demos} />

## 何时使用

- 定时任务、调度策略、报表周期等需要用户配置 cron 表达式,而用户不一定熟悉 cron 语法时
- 需要"所见即所得"的反馈:面板实时回显表达式、自然语言描述与接下来的执行时间
- 高级用户希望直接手输表达式(支持 `JAN-DEC` / `SUN-SAT` 名字、`7` 表示周日、`1,2-5`
  等复合形态,提交时自动归一化为标准写法)

解析 / 格式化 / 求下次执行时间的纯函数(`parseCron` / `formatCron` / `describeCron` /
`nextOccurrences`)均以具名导出,可在表单校验等场景独立使用。




## API

<API source="../_generated_api/rc-cron-picker.ts" symbol="CronPickerPropsSearchIndex" component="CronPicker" />
