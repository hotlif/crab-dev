+++
title = "CronPicker 定时规则"
description = "通过可视化配置生成 Cron 表达式，设置任务的执行时间。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-cron-picker.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-cron-picker.js";

# CronPicker 定时规则

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-cron-picker</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。完整代码包含公开导入和样式入口。

通过可视化配置生成 Cron 表达式，设置任务的执行时间。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**认识规则**

以每天上午 9 点的五段表达式开始。 Cron 表达式描述重复规则，预览帮助检查它是否符合业务时间约定。 查看分钟、小时与日期字段。 规则和后续执行时间一起显示。

**保存表达式**

连接 value 和 onChange，再用业务按钮设置工作日规则。 Cron 表达式描述重复规则，预览帮助检查它是否符合业务时间约定。 点击仅工作日，再修改小时。 表达式随编辑更新；真正执行仍需业务调度器。

</div>

## 常见问题

### 选择规则后任务会执行吗？

不会。组件只编辑表达式；服务端应负责时区、持久化与调度。


## API

<API source="../_generated_api/rc-cron-picker.ts" symbol="CronPickerProps" component="CronPicker" />

## 更多示例

<details>
<summary>展开进阶示例（3 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 基础用法 — 点击输入框打开可视化面板;也可直接手输表达式,回车提交(非法输入会以 error 边框提示并在失焦时回退)

##### 受控与常用预设 — 受控模式配合快捷预设:外部一键切换常用调度周期,面板与输入框同步跟随

##### 尺寸与状态 — 三档尺寸、禁用态与外部校验状态;预览条数可通过 previewCount 调整或关闭

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-cron-picker/workbench/)

## 使用说明

支持五段式 Cron 表达式的可视化编辑、手动输入和执行时间预览。


### 何时使用

- 为定时任务、报表或调度策略配置执行周期。
- 通过面板编辑表达式，并查看中文说明与后续执行时间。
- 直接输入表达式；支持月份和星期英文缩写、以 `7` 表示周日，以及 `1,2-5` 等组合写法，提交时规范化输出。

表达式采用五段式，依次表示分、时、日、月、周。`parseCron`、`formatCron`、`describeCron` 和 `nextOccurrences` 均为具名导出的纯函数，可用于独立校验与预览。
