+++
title = "Select 选择器"
description = "从下拉列表中选择值，通过搜索快速定位较多选项。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-select.js";

# Select 选择器

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-select</div>

<div id="基础示例" data-docs-example-anchor /><div id="全部示例" data-docs-example-anchor />

## 示例

<div hidden aria-hidden="true" data-docs-search-index="demos">

#### 基础与状态

##### 基础用法 — 单选模式下的基础选择

##### 尺寸 — 提供 large、middle、small 三种尺寸

##### 校验状态 — 设置 status 以展示 error 或 warning 状态

##### 禁用 — 设置 disabled 禁用整个选择器，或在选项中设置 disabled 禁用单个选项

##### 加载中 — 设置 loading 展示加载状态

##### 可清除 — 设置 allowClear 可清空已选值；可切换主题，对比三种尺寸和两种外观

#### 数据与搜索

##### 分组选项 — 使用 （ label, options ） 结构对选项进行分组

##### 可搜索 — 设置 searchable 支持关键字过滤

#### 多选能力

##### 多选模式 — 设置 multiple 启用多选

##### 标签数量限制 — 多选模式下设置 maxTagCount 限制展示的标签数量

</div>

<ComponentDemos demos={demos} />

[打开完整组件工作台](/components/rc-select/workbench/)

## 使用说明

在下拉列表中选择一个或多个值，支持搜索选项。


### 尺寸与外观

`size` 支持 `small`、`middle`、`large`。省略时继承最近的 ConfigProvider；没有配置时使用 `middle`。统一设置和局部覆盖见[尺寸指南](/guides/component-sizing)。

`appearance="outlined"` 是默认的描边外观，`appearance="filled"` 使用填充背景和底线；两者共享值、搜索和键盘操作。“可清除”示例可对比三种尺寸、两种外观及浅深色主题。

### 选中值与清除

`options` 中的 `value` 必须是唯一字符串，`label` 用于显示。用 `multiple` 开启多选：

| 模式 | 受控 `value` | 清除后的 `onChange` 参数 |
| --- | --- | --- |
| 单选 | `string` 或 `null` | `(null, undefined)` |
| 多选 | `string[]` | `([], [])` |

传入 `value` 时，在 `onChange` 中更新状态；单选空值应使用 `null`。不传 `value`（或传入 `undefined`）时，由组件维护选中值，可用 `defaultValue` 设置初始值。

设置 `allowClear` 后，有已选项且组件未禁用时，鼠标移入字段会将尾部箭头切换为清除图标；移到图标上仍可清除，离开字段后恢复箭头。两种图标共用一个位置，切换不会挤动文字。Tab 聚焦尾部按钮时也会显示清除；无悬停能力的触屏设备直接显示清除。

点击清除会清空选中值、关闭列表，并把焦点移回选择器。要修改已有值，请点击字段的内容区域打开列表；空值或未启用 `allowClear` 时，也可点击尾部箭头。禁用时这些操作不可用。

### 搜索与加载

`searchable` 在展开的列表中提供本地过滤：文本 `label` 按内容匹配，非文本 `label` 则按 `value` 匹配，忽略大小写。

组件没有搜索词回调，不能直接把 `searchable` 当作远程搜索接口。异步加载选项时，由应用获取数据并更新 `options`，请求期间可设置 `loading`；`loading` 只显示加载状态，不会发起请求。

### 标签与校验

- 用 `label` 提供可见字段名称，用 `supportingText` 补充选择要求。不能展示标签时，提供 `aria-label` 或指向外部标签的 `aria-labelledby`。
- `errorText` 会启用错误状态并替换辅助说明。`status="warning"` 只改变视觉状态，警告原因应通过 `supportingText` 等可见文字说明。
- `required` 提供必填语义和标签星号，不会自动校验或提交表单。
- 自定义选项和标签时保留可读名称；纯装饰图标使用 `aria-hidden="true"`。

### 键盘操作

| 操作 | 效果 |
| --- | --- |
| Tab | 在选择器、尾部按钮及页面其他控件之间移动焦点；有可清除值时，尾部按钮切换为清除 |
| 上 / 下方向键 | 展开列表并移动高亮选项 |
| Enter | 展开列表，或选择当前高亮项 |
| Space | 选择器收起时展开列表；搜索输入中可正常输入空格 |
| Escape | 关闭列表 |
| Backspace | 多选且搜索词为空时，删除最后一个已选标签 |
| 清除按钮上的 Enter / Space | 清空选中值，焦点回到选择器 |

更多要求见[无障碍使用指南](/guides/accessibility)。

### 适用场景

选项多到不适合全部并列展示。 用户必须从合法集合中选择，并可能需要搜索或多选。

少量互斥选项使用 Radio；用户应自由输入新值时使用 LineEdit 或组合输入。

### 使用要点

- options 中的 value 是唯一且稳定的标识，label 是显示内容。
- multiple 开启多选：单选值为 string 或 null，多选值为 string[]。
- searchable 过滤已传入的 options；组件没有向外提供搜索词回调。

### 无障碍

- 触发器与列表框使用正确 combobox/listbox 语义，并支持方向键、Enter 和 Escape。
- 多选标签、错误、必填和清除按钮都要有可读名称与稳定焦点。

## 常见问题

### label 与 value 有什么区别？

label 是用户看到的内容；value 是唯一且稳定的字符串标识，不应随显示文案改变。

### 为什么清除后应保存 null？

单选的 null 表示受控空值，undefined 表示由组件维护状态。多选使用空数组。接入受控状态时应保存 onChange 返回的值。

### searchable 会请求远程数据吗？

不会。它只过滤传入的 options，当前没有搜索词回调。异步选项由应用加载后传入，loading 仅负责显示加载状态。

## API

<API source="../_generated_api/rc-select.ts" symbol="SelectProps" component="Select" />
