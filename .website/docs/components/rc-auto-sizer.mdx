+++
title = "AutoSizer 自适应容器"
description = "自动测量容器宽高并传入子渲染函数，常与虚拟滚动组件配合使用"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import ComponentApi from "../site/componentApi.js";
import { api, demos } from "../_generated/rc-auto-sizer.js";

# AutoSizer 自适应容器

自动测量容器的宽高，并将 `{ width, height }` 通过渲染函数传递给子组件。常与 `Virtual` 虚拟滚动组件配合使用。

## 组件预览

<ComponentDemos demos={demos} />

## 何时使用

- 需要将外层容器的实际像素尺寸传入子组件（如虚拟列表的 `viewportWidth` / `viewportHeight`）。
- 需要在容器大小变化时自动重新布局（响应式窗口调整、面板拖拽等场景）。
- 希望组件与具体容器尺寸解耦，只接收数值而非自行测量 DOM。





## API

<ComponentApi api={api} />
