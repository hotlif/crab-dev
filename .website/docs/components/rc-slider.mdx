+++
title = "Slider 滑块"
description = "拖动滑块调整连续数值，适用于音量、比例与范围设置。"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-slider.js";

# Slider 滑块

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-slider</div>

<div id="基础示例" data-docs-example-anchor /><div id="全部示例" data-docs-example-anchor />

## 示例

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 滑块基础用法 — 这是一个滑块的基础示例

##### Expressive 五档尺寸 — 新版竖向手柄、轨道间隙、终点与五档轨道高度。

</div>

<ComponentDemos demos={demos} />

[打开完整组件工作台](/components/rc-slider/workbench/)

## 使用说明

通过拖动滑块在指定数值范围内选择值。


### 何时使用

用于音量、亮度或阈值等有明确范围的数值调整，可选择连续值或按步长选择离散值。需要精确键入数值时可使用 NumberEdit。

### Expressive 外观

使用新版竖向手柄、轨道间隙与终点标记。`size` 提供 `xs`（默认）、`s`、`m`、`l`、`xl`，轨道高度分别为 16、24、40、56、96px。按压时手柄变细；数值更新立即跟随拖动，不对拖动位置添加延迟。

### 适用场景

音量、亮度、比例等范围值需要快速调整。 min、max 和 step 明确，用户可以根据相对位置做决定。

精确数值输入使用 NumberEdit；少量离散选择使用 Segmented 或 Radio。

### 使用要点

- min、max、step 和 value 必须使用同一单位。
- 受控 onChange 处理拖动过程，必要时区分提交完成事件。
- 标签或值显示帮助用户理解手柄位置代表什么。

### 无障碍

- 提供可访问名称、当前值文字、上下限和键盘增减操作。
- 轨道颜色之外要有可定位手柄与焦点，触控目标大于可见手柄。

## 常见问题

### 如何精确键入数字？

需要键入时配合 NumberEdit，并让两个控件共享同一数值状态。

## API

<API source="../_generated_api/rc-slider.ts" symbol="SliderProps" component="Slider" />
