+++
title = "Switch 开关"
description = "切换立即生效的开启或关闭状态"
kind = "component"
status = "experimental"
+++

{/* THIS FILE IS AUTO-GENERATED. DO NOT MODIFY MANUALLY. */}

import ComponentDemos from "../site/componentDemos.js";
import { demos } from "../_generated/rc-switch.js";
import Tutorial from "../site/tutorial.js";
import FirstExample from "../site/firstExample.js";
import { tutorial } from "../_generated_tutorials/rc-switch.js";

# Switch 开关

<div hidden aria-hidden="true" data-docs-search-index="package">@crab-dev/rc-switch</div>

## 基础示例

<FirstExample tutorial={tutorial} />

## 开始之前

已经了解 JSX、组件属性与 useState。先安装本页组件包；示例中的其他 Crab UI 组件也需安装。示例使用公开组件入口，样式由 Wake 自动加载。

开关用于立即生效的布尔设置，状态说明让用户知道当前是否开启。

## 跟着做

<Tutorial tutorial={tutorial} />

<div hidden aria-hidden="true" data-docs-search-index="tutorial">

**设置初始值**

使用 defaultChecked 设置初始选中状态。 开关用于立即生效的布尔设置，状态说明让用户知道当前是否开启。 点击选项，观察选中状态。 选项可在选中与未选中之间切换。

**改为受控**

将 checked 与 onChange 连接到同一个 state。 开关用于立即生效的布尔设置，状态说明让用户知道当前是否开启。 切换选项，再查看状态文字。 控件与文字始终保持一致。

</div>

## 常见问题

### 受控与非受控可以混用吗？

同一实例应选择一种方式。需要业务代码读取或修改状态时，用 checked 和 onChange。


## API

<API source="../_generated_api/rc-switch.ts" symbol="SwitchProps" component="Switch" />

## 更多示例

<details>
<summary>展开进阶示例（3 个）</summary>

<div hidden aria-hidden="true" data-docs-search-index="demos">

##### 基础用法 — 最简单的 Switch 用法

##### 禁用状态 — 添加 `disabled` 属性即可让开关处于禁用状态

##### 开关尺寸 — 通过 `size` 属性设置开关尺寸, 支持 `large`、`middle`、`small` 三种

</div>

<ComponentDemos demos={demos} />
</details>

[打开完整组件工作台](/components/rc-switch/workbench/)

## 使用说明

切换立即生效的开启或关闭状态。


### 何时使用

用于通知开关、功能启停等立即生效的布尔设置。选中状态表示开启，未选中表示关闭。需要先选择、再随表单提交的选项通常使用 Checkbox。

### 视觉状态与验证

切换轨道使用独立的实线焦点环，强制颜色下保留轨道、手柄位置与焦点。立即生效操作如涉及异步保存，应在业务层明确处理中和失败恢复；组件本身不会请求服务器。

粗指针环境下，独立操作的命中区至少为 44×44 CSS px。标签容器扩展命中区，方框、圆点或轨道仍保留原有 size 视觉尺寸。长标签采用正文行高；紧密组合需检查目标区域没有相互重叠。此规则不新增 density 属性，也不把全部组件改成同一视觉高度。

键盘焦点使用公共 L2 焦点角色，通过 L3 覆盖可调整；减少动态效果时移除非必要过渡。实际浏览器应逐项检查 Tab / Shift+Tab、原生激活方式、两种主题、强制颜色、长标签与粗指针布局。当前源码修复与自动检查状态见质量审查记录，不能把 CSS 中存在媒体查询等同于设备验收通过。

规则依据：[状态系统](/design/interaction)、[无障碍与键盘](/design/accessibility)、[布局与密度](/design/layout)。
