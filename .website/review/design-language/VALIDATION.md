# Crab Design Language v1 验收记录

日期：2026-09-14。入口：`/design/language`。预览仅监听 `127.0.0.1:4173`。

## 后续调整：Material 3 基准紫色

- 文档站与设计样板共用 `docs/site/palette.ts` 和 `paletteStyles.ts`：Material Web v0.192 原色转换为等价 OKLCh；浅色主色 #6750A4，深色主色 #D0BCFF。画布、内容、浮层、选中、正文和必要边界均采用明确的明暗配对。
- 通过文档站和样板的明确作用域覆盖现有 L2 变量，浮层使用相同主题边界。主按钮的悬停和按压分别叠加 8% / 12% 前景色；选中文字、错误反馈和焦点指示同步更新。独立 iframe 组件示例保留自己的主题。
- 色彩、概览、令牌章节与维护说明同步更新；色卡直接读取运行时消费的静态令牌。模拟项目“海蓝设计系统”未改名。默认 `system` 与已有偏好记忆逻辑保持不变。
- 修改前及修改后的 ESLint、类型检查均通过，没有新增告警。配色与设计契约共 15 项通过；覆盖 sRGB 等价性、两种主题、混色后的交互状态、三层表面、功能反馈和业务数据契约。文档生成器 23 项通过，生成检查 0 个文件变化。
- Wake React 回归仍被既有 `WAKE_TEST_REACT_VERSION` 阻断：Wake 0.1.38 要求 React / React DOM `>=19.2.8 <19.3.0`，当前锁定 19.3.0。未降级依赖或绕过入口，不能视为交互测试通过。
- 本轮日志：`../material-baseline-eslint.log`、`../material-baseline-typecheck.log`、`../material-eslint.log`、`../material-typecheck.log`、`../material-contract.log`、`../material-react.log`、`../material-build.log`。
- 以下旧记录与历史截图描述此前海蓝配色；本轮生产验证结果另附于本节。

### 本轮生产浏览器验证

- 首页、组件目录、Button 文档、开始使用指南、设计概览及色彩页均采用新主题。顶部始终保留品牌、设计语言、组件、搜索及主题设置，不恢复“开发指南”顶部入口。通过搜索进入指南，再切换组件、设计语言；浏览器前进与后退恢复正确地址和标题。
- 在独立 `localhost:4173` 入口首次访问时，顶部选中 `system`，解析为 `dark`，与当前系统媒体查询一致。在主预览入口验证浅色 / 深色手动切换与刷新记忆；原有浏览器浅色偏好在验收后恢复。临时视口已清理，测试标签页已关闭。
- 生产页面实测画布、主色、选中容器、卡片、搜索、下拉和抽屉与集中令牌一致。明暗 × 标准 / 紧凑状态矩阵中按钮高 36 / 28px，禁用与加载语义保留，关注操作同时更新文字和 `aria-pressed`。工作台行高 44 / 36px；表单输入外框为 36 / 28px。原始测量见 `../material-browser.json`。
- 工作台：含空白的小写项目编号命中正确数据；行选择更新数量，键盘清空搜索后恢复 12 项并清空选择；分页、已完成筛选、加载、错误重试和空状态均可操作。下拉继承深色浮层背景。320px 下搜索负责人和切换紧凑模式正常，表格内容 660px，显示区域 213px，页面滚动宽度与内容宽度均为 305px，没有整体横向溢出。
- 表单：必填姓名和无效邮箱显示就近错误并聚焦首个字段；修改有效数据和团队后，提交出现 `aria-busy=true` 与保存文案，成功后摘要更新、姓名空白与邮箱大小写被规范化、焦点恢复编辑入口。取消修改出现确认，继续编辑保留草稿；放弃后摘要不变并恢复编辑入口焦点。
- 1440px、768px、320px 均检查布局。320px 下编辑抽屉宽 305px，搜索对话框宽 273px；规范表格在 273px 容器内滚动。移动导航通过 Enter 展开分类和切换章节；搜索支持方向键、结果打开和 Escape，关闭后焦点回到搜索入口。768px 下目录卡片及侧栏没有页面溢出。
- 补齐文档站按钮和链接的显式焦点轮廓。最终生产页面明暗实测均为对应主色的 2px 实线、2px 偏移；输入框的焦点阴影同样使用当前焦点色。当前系统确实启用减少动态效果，实测过渡时长 0s、动画 none。
- 最终生产标签页控制台没有 warning / error。较早的开发预览记录过 Motion 对设备减少动态效果设置的提示，不属于本次新增代码告警；开发预览缓存未作为本轮配色验收依据。

### 自动检查与验证边界

- 两次生产构建均成功并保留 80 个路由；最终编译耗时 2m41.2s，221,295 个模块、2,540 个文件。最终绑定检查覆盖 1,890 个生产 JavaScript 文件，0 个未绑定引用；日志为 `../material-build-final.log`。
- `yarn generate:token` 的 48 项任务全部成功；与本轮修改前 SHA256 对比，公共 `src/token.ts` 变化为 0。记录为 `../material-generate-token.log`、`../material-token-baseline.json`。
- 强制颜色：当前设备未启用该模式，浏览器工具未提供媒体模拟。已检查生产 CSS 中的 Canvas / CanvasText / Highlight 映射、显式焦点轮廓及边界规则，未宣称完成强制颜色下的视觉验收。
- 跟随系统的首次解析、手动选择和持久化已验证；未改变设备全局主题，也未模拟运行过程中系统明暗变化。该分支继续使用 Wake 现有主题管理，React 自动测试仍受前述版本限制。
- 本轮截图在 `../screenshots/material-*.png`，包括明暗色卡、工作台、表单、手机表单、移动导航、搜索与最终焦点。明暗色卡及焦点截图来自最终构建；补齐焦点轮廓未改变已验收的业务逻辑与配色。

## 后续调整：移除顶部开发指南入口

- 顶部移除“开发指南”，保留“设计语言”“组件”、品牌、搜索和主题工具。指南仍通过侧栏、搜索及原地址访问。
- ESLint、类型检查通过；生产构建保留 80 个路由，1,890 个脚本文件的未绑定引用为 0。生成文档内容变化为 0。日志为 `../header-guide-eslint.log`、`../header-guide-typecheck.log`、`../header-guide-build.log`。
- 生产浏览器验证：当前 `/guides/getting-started` 标题仍为“开始使用”，暗黑模式下侧栏“开发指南”保持展开；桌面和手机顶部均没有该入口。
- 320px 下页面宽度与滚动宽度均为 305px；移动目录仍显示“开发指南”和“开始使用”。检查后恢复默认视口，控制台无 warning / error。
- 调整既有导航测试中的旧入口断言；本次未重复运行已知受 React 版本限制的 Wake React 测试，限制说明见下文。

下方为此前规范扩展的验收记录；其中三个顶部入口与历史截图记录的是本次移除之前的状态。

## 交付

- 中文设计规范扩展为 6 类、19 个独立手写章节、136 个二级小节。覆盖开始使用、视觉基础、交互规则、组件规范、业务模式和实施与维护；细则包含目的、选型、结构、参数、状态组合、无障碍、禁例与验收步骤。
- 使用真实 Crab 组件的数据工作台与资料编辑抽屉；固定数据、本地模拟保存。
- 所有页面共用原有顶部导航、搜索、移动导航与主题切换；设计语言使用专属章节侧栏，不混入项目组件目录。切回组件或指南后恢复对应文档侧栏。
- 原有七个章节地址保留，新增布局、形状、图标、动效、无障碍、文案、操作、输入、导航、数据展示、浮层和反馈十二章。左侧六个可折叠分类用于选页，桌面右侧二级目录用于段落定位，三级小节保留父级高亮；1280px 以下提供正文前的折叠目录。
- 七个交互展示组件继续共用 `preview.tsx` 的主题、密度与触控设置。规范正文在 `docs/design/*.mdx` 维护；样板留在 `docs/site/design/`。Markdown 表格使用 Wake 的 `table-scroll` 包装层，在设计页作用域内提供间距、边界、表头与局部滚动，不影响实时组件样板。
- 概览注明参考 Material Web 官方色彩与按钮规范的组织方式；Crab 数值由本项目定义。无障碍章节引用 W3C，并区分 AA、AAA 与项目触控约定，不声称完整 WCAG 认证或全库已迁移。
- 浅色 / 深色与顶部主题设置同步，支持跟随系统与刷新记忆。标准 / 紧凑密度仅影响样板。
- 候选参数集中于 `docs/site/design/tokens.ts`，用 `defineTokens` 支持构建期求值。
- 后续迁移清单见 [维护说明](../../docs/site/design/README.md)。未修改公共组件接口、依赖版本或已有组件默认外观。

## 自动检查

命令均在 `.website` 目录执行。

| 检查 | 结果 |
| --- | --- |
| 修改前 `yarn eslint`、`yarn typecheck` | 均通过 |
| 修改后 `yarn eslint`、`yarn typecheck` | 通过，无新增告警 |
| `yarn test docs/site/design/__tests__/contract.test.ts` | 4 项通过：明暗对比度、组合筛选、表单验证与规范化 |
| 文档生成器测试 | 23 项通过 |
| 文档一致性检查 | 52 个组件页、55 份教程、117 个教学示例、246 个 Demo，0 个生成文件变化 |
| 根目录 `yarn generate:token` | 48 个任务通过；生成前后 SHA256 对比，源码变化 0 个 |
| 章节结构检查 | 19 个唯一导航地址全部对应手写文件；136 个二级小节；设计章节内部链接无失效目标 |
| `yarn build` | 最终生产构建通过：80 个路由；检查 1,890 个生产 JavaScript 文件，0 个未绑定引用 |
| React 回归测试 | 被现有 `WAKE_TEST_REACT_VERSION` 限制阻止运行 |

React 测试限制在修改前的完整 `yarn test` 已复现：Wake 0.1.38 要求 React / React DOM `>=19.2.8 <19.3.0`，仓库锁定 `19.3.0`。本次 `yarn test docs/site/__tests__/ui.test.tsx` 同样受阻。已提供资料编辑与共用顶部、正文布局切换的回归测试源码，未降级版本或绕过 Wake 测试入口，不能宣称 React 自动测试已通过。

本轮补充分组目录、段落链接、修饰键、当前项与三级小节父级高亮的 React 回归断言，类型检查通过，执行仍受上述版本限制。日志见 `../design-handbook-eslint.log`、`../design-handbook-typecheck.log`、`../design-handbook-contract.log`、`../design-handbook-ui.log`、`../design-handbook-generate-token.log` 与 `../design-handbook-build-final.log`。最终构建后未再修改页面实现。

## 浏览器验收

使用生产预览，真实组件、真实浏览器交互。无需后端服务，不发送通知。

### 本轮分类与长文复验

- 逐项访问全部 19 章，确认正文已加载、二级小节数量、独立 URL、侧栏标题与当前项一致；1440px 下无页面横向溢出。
- 侧栏只包含设计语言的六个分类；通过分类入口切换章节，当前分类展开。全局搜索“浮层与对话”命中章节及其段落，结果标明“设计语言 / 组件规范”，打开后聚焦新页标题。
- 顶部连续切换设计语言、组件与开发指南；组件恢复项目 / 组件侧栏，指南显示“开始使用”。浏览器后退、前进恢复指南与设计语言各自的地址、标题和目录。
- 浅色下正文与样板同步，色彩样板显示 Blue 600；暗色状态样板可切紧凑并切换“关注项目”，选中语义保持正确。
- 桌面右侧目录定位正文；进入“错误与聚焦”三级小节时，父级“状态如何组合”保持高亮。定位后标题距视口顶部约 144px，顶部高 65px，没有遮挡。
- 768px：正文总宽 753px，网格为 224px 侧栏 + 529px 正文，右侧目录隐藏，正文前目录可展开；无页面横向溢出。
- 320px：正文总宽 305px，无整体横向溢出。规范表格容器 273px，四列表格 384px、三列表格 288px，均在容器内滚动；单元格内边距 12px。本页目录触发目标高 44px。
- 手机通过 Enter 展开分类、选择状态章节，抽屉自动关闭，焦点恢复到正文标题。标题顶部 185px，固定顶部底部 161px；页内目录通过 Enter 定位后标题约 192px，仍可见。
- 最终样板复验：工作台搜索含两端空格的小写 `prj-001` 命中海蓝设计系统一项；表单空白姓名提交后显示错误并聚焦无效字段，修正保存后摘要更新、抽屉关闭、焦点恢复编辑按钮。两者的业务实现未因长文排版改变。
- 已验收的生产浏览器操作未出现 console warning / error。截图使用最终生产页面；媒体偏好和辅助技术验证边界仍见下节。

### 既有业务状态矩阵

以下为此前同一组样板参数与业务逻辑的验证记录，本轮保留这些实现，并按上述流程复验主要交互。

| 组合 | 控件实测高度 | 数据行实测高度 | 品牌背景 |
| --- | --- | --- | --- |
| 浅色 · 标准 | 36px | 44px | `oklch(0.546 0.245 262)` |
| 浅色 · 紧凑 | 28px | 36px | `oklch(0.546 0.245 262)` |
| 深色 · 标准 | 36px | 44px | `oklch(0.714 0.165 259)` |
| 深色 · 紧凑 | 28px | 36px | `oklch(0.714 0.165 259)` |

- 主题同步：页面使用 Wake 的站点主题状态；顶部与页面内的切换控制同一主题，下拉菜单与抽屉继承当前主题及密度。仅脱离站点上下文展示时使用本地主题状态。
- 顶部入口专项复验：从“组件”通过顶部“设计语言”返回专页，当前入口有 `aria-current="page"`；320px 下仍可见，交互高度 44px。页面切换浅色后全站同步，顶部切换深色并刷新后页面与样板均为深色；跟随系统时两者与系统偏好一致。
- 上一版章节拆分验证：原七章的独立地址、标题与侧栏当前项一致；本轮十九章分类的结果见上方专项记录。
- 拆分后业务复验：色彩页明暗分别显示 Blue 600 / 400；状态页可切换关注；工作台搜索 `PRJ-001` 命中一项，紧凑行高 36px；进入表单章节密度恢复标准，暗黑抽屉保存后更新摘要并恢复编辑按钮焦点。令牌映射保留七行。
- 共用顶部复验：设计语言与组件栏目互相切换时，顶部始终保留；设计侧栏仅显示设计章节，组件侧栏恢复项目与组件分组。此前已验证全局搜索、指南切换与浏览器前进后退。
- 手机导航既有验证：320px 下顶部三个栏目均可见；选择工作台后自动关闭并聚焦页面标题。切换组件后导航抽屉恢复项目、组件分组，不含设计章节。本轮改为十九章分组目录，具体结果见上方专项记录。
- 宽度：1440 / 768 / 320px 视口对应文档宽度 1425 / 753 / 305px（保留 15px 滚动条），无页面横向溢出。移动端表格保留内部横向滚动条。
- 搜索：`  prj-001  ` 命中海蓝设计系统；清除按钮恢复完整 12 项。搜索标签与清除操作分别关联。
- 选择与分页：第一页全选 5 项；切换第二页后选择归零。
- 场景：加载显示 spinner 与 `aria-busy`；模拟错误可重试；空数据可重置筛选恢复列表。
- 表单：键盘清空姓名后，保存显示就近错误并聚焦姓名输入；取消确认保留草稿；保存显示 pending、禁止重复提交，完成后更新摘要并恢复编辑按钮焦点。
- 最终生产包专项复验：有效草稿点击取消不保存；继续编辑保留内容；确认放弃后重新打开恢复为原值。抽屉内通过方向键与 Enter 选择“工程研发”，保存后正确更新摘要并恢复焦点。
- 浏览器控制台：已验收操作未出现新增 warning / error。

## 可访问性与验证边界

- 对比度测试覆盖两主题下的正文、辅助文字、按钮三个状态、选中文字，以及画布 / 内容 / 浮层上的边界与焦点。文字至少 4.5:1，必要边界和焦点至少 3:1。浅色边界采用 Zinc 600，以满足灰底上的阈值。
- 实际键盘验证涵盖必填错误聚焦与弹层关闭后的焦点恢复。
- 生产 CSS 中确认存在 `forced-colors: active`、`prefers-reduced-motion: reduce` 与 `pointer: coarse` 规则；分别提供系统色映射、去除非必要动效和 44px 触控目标。当前浏览器控制接口没有媒体偏好模拟能力，因此尚未完成这些模式的真实设备截图验收。
- 不将 DOM 单元测试当作布局测试；布局以生产浏览器检查为准。

## 截图

- [六类规范目录与浅色概览](handbook-overview-light.png)
- [暗黑状态规范与页内目录](handbook-states-dark.png)
- [手机分类目录与键盘焦点](handbook-mobile-navigation.png)
- [手机可折叠页内目录](handbook-mobile-toc.png)

上一版七章布局记录：

- [设计侧栏与暗黑概览](chapters-overview-dark.png)
- [色彩章节与浅色主题](chapters-color-light.png)
- [手机设计目录](chapters-mobile-navigation.png)
- [手机工作台与共用顶部](chapters-mobile-workbench.png)

以下为早期样板状态记录，页面外壳以最终截图为准：

- [浅色规范](desktop-light.png)
- [深色规范](desktop-dark.png)
- [浅色数据工作台](workbench-light.png)
- [768px 深色布局](tablet-dark.png)
- [320px 深色工作台](mobile-dark.png)
- [深色编辑抽屉](editor-dark.png)
- [320px 浅色编辑抽屉](editor-mobile-light.png)

后续全库迁移应先解决 Wake 与 React 的测试版本兼容问题，再按维护说明逐层推广。
