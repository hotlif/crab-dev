# 全局令牌页面视觉优化验收

日期：2026-09-15。页面：`http://127.0.0.1:4173/components/rc-token-global`。

## 实施范围

- 去掉 137 项令牌的重复卡片外框与内部横线，以标题、对齐和间距表达归属。保留真实样本所需的轮廓，例如圆角形状、极浅色块与层级示意。
- 五组颜色各自形成连续的 11 阶色带，另有黑白组。每组共用一个详情区，左右方向键、Home、End 切换，Tab 直接移至详情操作；窄屏局部横向滚动。
- 间距以实际像素长度对齐展示；字体与动效使用样本行；圆角、阴影和透明度使用共享舞台；九个层级在同一堆叠上下文内按实际数值比较。
- 列表默认保留名称、数值和样本，按需展开代码；共享舞台通过选择样本查看原始值、公共引用和变量名。两个复制入口明确区分用途，切换样本清除上一次反馈。
- 选择规则移至各类目录之前，源码维护说明收于末章。完整的 13 个二级章节、137 项参数、工作台、基础示例、目录与搜索保留。
- 未修改公共令牌源定义、公共导出、主题实现或依赖；生成文件由 `generate:docs` 刷新。

## 维护入口

| 内容 | 源文件 |
| --- | --- |
| 手册正文及插入顺序 | `components/rc-token-global/docs/index.mdx` |
| 私有展示与交互 | `.website/docs/site/tokenReference.tsx` |
| 静态样式与响应式规则 | `.website/docs/site/tokenReferenceStyles.ts` |
| 参数与样本生成 | `.website/scripts/generate-token-reference.mjs` |
| 参考页模板 | `.website/scripts/generate-component-docs.mjs` |
| 维护约定 | `.website/content/README.md` |

## 工程检查

- 文档生成：52 个组件页、55 份教程、117 个教学示例、246 个唯一 Demo；刷新后无生成漂移。
- `check:docs` 与 26 项 `test:generator` 通过；覆盖参数目录、原始值与引用、未知语法、漂移、专用参考模板和普通组件模板。
- 受影响展示组件、样式、交互测试与参数生成脚本的定向 ESLint 通过；`.website` 类型检查通过。
- 最终 `yarn build` 退出码 0：编译耗时 3m39.8s，221304 modules、2542 files、178.44 MB、80 routes；随后绑定检查通过，1891 个 JavaScript 文件、0 个未绑定引用。

已执行 `yarn test docs/site/__tests__/tokenReference.test.tsx`，前置生成器和文档检查通过。Wake Test 0.1.38 在运行 React 测试前报 `WAKE_TEST_REACT_VERSION`：当前 React / React DOM 19.3.0 超出其要求的 `>=19.2.8 <19.3.0`。五项交互测试已更新，不能标为执行通过；本任务未通过调整依赖绕过限制。

## 生产页面实测

| 主题 | 视口宽度 | 文档宽度 / 滚动宽度 | 参数样本 | 非色带样本越界 |
| --- | --- | --- | --- | --- |
| 浅色 | 320 px | 305 / 305 px | 137 | 0 |
| 浅色 | 768 px | 753 / 753 px | 137 | 0 |
| 浅色 | 1440 px | 1425 / 1425 px | 137 | 0 |
| 深色 | 320 px | 305 / 305 px | 137 | 0 |
| 深色 | 768 px | 753 / 753 px | 137 | 0 |
| 深色 | 1440 px | 1425 / 1425 px | 137 | 0 |

文档宽度与视口差值为滚动条预留空间。320 px 下完整色带宽 492 px，在 281 px 容器内滚动；768 px 下容器为 465 px；1440 px 下无需横向滚动。所有参数外层的计算边框宽度均为 0。

- 已看截图：连续色阶的明暗对照、间距标尺、圆角共享舞台、320 px 阴影与排版详情。样本文字位于色块之外，长代码在详情换行或正文代码块内局部滚动。
- 实测 Blue 50 → 100 → 950 与 Home 返回；详情同步切换，组内仅一个 Tab 入口，焦点轮廓 2 px，Tab 到当前引用的复制按钮。
- 实测引用与变量名复制均显示就近“已复制”，切换样本清除旧反馈；展开、收起间距代码正常。未读取系统剪贴板来核验实际载荷。
- 动效初始静止，Enter 播放后位移 96 px，再次 Enter 反向；实际时长为 0 / 100 / 200 / 300 ms。
- 检查 Blue 各阶实际 OKLCh、全部圆角、字体回退和九个层级的实际 `z-index`。样本继续局部使用 L1 回退值，未受站点字体覆盖污染。
- 页内 33 个锚点链接无失效目标；搜索 `font.family.sans` 返回手册，关闭搜索后焦点回到搜索按钮。
- 最终修正后再次检查六种组合，表中结果不变；控制台无 error / warn。阴影在 none 与最长 lg 间切换时，320 px 下均为 256 px，768 / 1440 px 下均为 208 px；悬停按钮的背景计算值为透明，名称区域保留反馈。
- 相同 998 × 912 px 视口、详情未展开时，文档高度由优化前 28,963 px 降至最终 22,805 px，缩短约 21.3%；未据此删除参数或正文章节。

## 检查中修正的问题

1. 共享样本按钮内部由公共 Button 添加一层文本容器，已显式处理其宽度与换行，让连续色块填满各自位置。
2. 阴影的最长三层定义在 320 px 下使详情从 208 px 增至 245 px，在 768 px 下使详情从 176 px 增至 182 px。为阴影组分别预留 256 / 208 px，避免切换常规样本时推动后续内容；保留高度自适应以容纳更大字体或失败反馈。
3. 样本悬停原本继承公共文本按钮的整块底色，已将底色收敛到名称区域，焦点环与选中标记保留。
4. 生成页面末尾多出空行，已在参考页模板对搜索元数据使用 `trimEnd()`，通过重新生成修正，生成器测试与一致性检查复验通过。

## 验证边界

- 减少动态效果与强制颜色规则已检查源码和生产 CSS；未实际切换系统媒体偏好、触控设备或运行读屏审计。
- 200% 缩放未实测，不标为通过。
- 剪贴板拒绝与重试由保留的自动测试覆盖，但受上述工具链版本限制尚未执行；浏览器未注入失败条件。
- 未执行与本页无关的全仓组件测试，也未修改其既有工作区内容。

公共紫色色板、全库统一密度及后续语义令牌迁移仍需独立建设；此次视觉优化不改变这些差距的落实状态。
