# 首页视觉与查找体验优化

日期：2026-09-16。范围：首页与站点维护说明。

## 六项审查建议的落实

| 审查问题 | 本次修改 |
| --- | --- |
| 分类概念重叠、用途说明抽象 | 保留现有分类与目录锚点，说明改为填写表单、展示头像、确认操作、虚拟滚动、树形数据等具体内容 |
| 精选入口容易被理解为完整目录 | 标题明确为常用组件，说明列出 24 个精选入口并指向完整目录；数量由分类条目计算 |
| 双语名称桌面过紧、手机相距过远 | 中英文始终相邻，横向间距 8 px；入口按可用宽度自动一列或两列，每项至少预留 144 px，支持同一链接内的文本换行 |
| 章节间留白叠加过大 | 章节上 24 px、下 32 px，相邻约 56 px；标题下方 24 px，窄屏 16 px；卡片内边距仍为 24 / 16 px |
| 分类容器与可点击标题的边界含糊 | 标题改为普通 h3，旁边提供明确的“查看分类 →”；链接的可访问名称包含对应分类 |
| 首屏重复入口使用不同名称 | 主要入口保留“开始使用”“组件目录”；右侧改为设计语言、无障碍指南和本页业务实战，去掉装饰序号 |

## 范围与维护

- 修改 `docs/site/learningHome.tsx`、`docs/site/homeStyles.ts`，同步 `.website/README.md`。
- 六个分类名称、24 个组件地址及 `components`、`practice`、`learning-path`、`first-example` 锚点保留。
- 本页业务实战使用原生 `#practice` 链接，经现有 `useSiteHref` 原样保留；页面已有章节滚动间距。
- 分类卡片继续复用 Card、具体操作复用 Button；没有新增依赖、公共令牌、公共属性或运行时宽度监听。
- 144 px 为首页入口局部规格，引用现有全局间距组合；自适应网格最多两列，避免出现三个入口一行、最后一个孤立的排布。

## 工程检查

- 定向 ESLint：通过。
- `.website typecheck`：通过。
- `check:docs`：通过，0 个文件漂移。
- `test:generator`：26 项通过。
- `yarn build`：通过，80 个路由、2539 个产物文件；检查 1889 个生产 JavaScript 文件，0 个未绑定引用，完整命令退出码为 0。
- 构建后预览地址的 HTTP 请求返回 200；这仅验证服务可访问，不等同于页面渲染或交互通过。
- 变更文件 `git diff --check`：通过。

本次没有为文案和可逆排版变更新增镜像单元测试。没有重复运行此前受 React 19.3.0 与 Wake Test 0.1.38 兼容检查阻挡的站点 React 测试。

## 视觉验收限制与待复核项

浏览器工具及 computer-use 备用工具均在初始化阶段返回：`failed to write kernel assets: 系统找不到指定的路径。 (os error 3)`。没有控制浏览器或采集本轮截图，因此不能把此前的截图、颜色估算或构建成功记作本轮浏览器通过。

待工具恢复后复核：

- 浅色、深色下的 320、640、768、1440 px，以及分类切换断点。
- 双语名称保持相邻；最长 AutoSizer、Pagination 不被裁切；放大时允许合理换行。
- 分类标题为普通文字；“查看分类”与组件入口的鼠标反馈、Tab 顺序、Enter 跳转和焦点轮廓。
- “业务实战”定位本页对应区域，标题不被固定顶栏遮挡。
- 外部留白收紧后，卡片分组、章节层级和触控命中区域仍清楚。
