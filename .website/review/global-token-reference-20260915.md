# 全局令牌参考手册验收记录

日期：2026-09-15。页面：`http://127.0.0.1:4173/components/rc-token-global`。

## 交付内容

- 重构为 13 个正文章节：定位、基础示例、颜色、间距、圆角、字体、阴影、动效、透明度、层级、三层令牌与主题、维护验收、工作台。
- 完整目录由公共 TOML 生成：颜色 57、间距 18、圆角 12、排版 18、阴影 6、动效 8、透明度 9、层级 9，共 137 项。
- 每项提供原始值、公共表达式、变量名、可视化样本与复制操作；动效主动播放，可反向。
- 文档正文维护于 `components/rc-token-global/docs/index.mdx`；教学内容在 `.website/content/rc-token-global.json` 与两个独立示例中维护。
- `generate-token-reference.mjs` 生成完整目录、样本静态 CSS 与搜索所需数据。其他组件继续使用原有教学模板。
- 保留 URL、工作台和基础示例锚点。公共 `token.toml`、`src/token.ts`、`src/index.ts` 未修改；未新增依赖或公共 API。

## 工程检查

| 检查 | 结果 |
| --- | --- |
| generate:docs / check:docs | 52 组件页、55 教程、117 教学示例、246 唯一 Demo；最终无生成漂移 |
| test:generator | 26 项通过，包括目录完整性、前缀、零值、重复键、未知语法、生成漂移和参考页布局 |
| 定向 ESLint | 站点展示、样式、测试、两步示例、生成脚本与工作台示例通过 |
| .website typecheck | 通过 |
| 最终生产编译 | 2m15.0s；221304 modules、2542 files、178.42 MB、80 routes |
| 最终生产绑定检查 | 通过：1891 个 JavaScript 文件，0 个未绑定引用；yarn build 退出码 0 |
| 内部文档引用 | 19 处引用均对应现有页面；页内目录与分类链接无失效锚点 |

`yarn test docs/site/__tests__/tokenReference.test.tsx` 已执行：前置生成器与一致性检查通过，Wake 在运行交互测试前以 `WAKE_TEST_REACT_VERSION` 拒绝启动。当前仓库 React / React DOM 为 19.3.0，Wake Test 0.1.38 要求匹配版本 `>=19.2.8 <19.3.0`。三项测试保留，覆盖复制成功、复制拒绝后重试、主动动效状态切换；不能标为运行通过。本任务未调整依赖来规避版本限制。

## 生产浏览器检查

使用 Codex 内置浏览器，检查最终生产页面。

| 主题 | 视口宽度 | 文档实际宽度 | 样本数 | 溢出卡片 | 可局部滚动的正文表格 |
| --- | --- | --- | --- | --- | --- |
| 浅色 | 320 | 305 | 137 | 0 | 11 |
| 浅色 | 768 | 753 | 137 | 0 | 11 |
| 浅色 | 1440 | 1425 | 137 | 0 | 11 |
| 深色 | 320 | 305 | 137 | 0 | 11 |
| 深色 | 768 | 753 | 137 | 0 | 11 |
| 深色 | 1440 | 1425 | 137 | 0 | 11 |

宽度差来自浏览器滚动条预留空间；六种组合均无整页横向溢出。

- 实际 CSS 逐项比对：131 项非阴影样本与显示原始值一致；6 项阴影核对各层偏移、模糊、扩展与颜色，均保持源码定义。
- 截图检查首屏、320px 浅色色块、1440px 深色色阶及阴影。极浅色块有外边界；说明文字位于独立表面；深色下阴影克制的真实效果已在正文说明。
- 复制按钮实际出现“已复制”；Tab 移至下一项复制入口并显示 2px 焦点轮廓。未将该观察等同于剪贴板失败路径通过。
- Enter 播放与反向切换动效；第二步教学示例实际加载，工作台 iframe 显示修正后的 L1 示例。
- 搜索 `font.family.sans` 返回本手册；关闭搜索后焦点恢复到搜索按钮。
- 浏览器未记录新的 error / warn。生产 CSS 包含减少动态效果下移除过渡与显示说明，以及强制颜色轮廓规则。

## 修正与验证边界

1. 初次构建发现 L1 工作台示例导入 L2 违反 PnP 依赖边界，已改为纯 L1 示例；站点教学中的 L1/L2 对照仍保留。
2. 首次 320px 检查发现动效说明表撑宽页面，已为本页 Markdown 表格添加局部滚动，最终六种组合通过。
3. 逐项计算值检查发现站点覆盖了全局 sans 字体。样本现在仅将被展示的 L1 变量局部设为 initial，以使用公共回退值；最终 sans 列表与公共源码一致，外围阅读样式保持站点主题。
4. 200% 缩放快捷键未改变内置浏览器的实际倍率，因此不标为通过。没有实际切换系统减少动态效果、强制颜色或触控设备；这些项仅核对代码与生产 CSS，不能代替实机验收。
5. 剪贴板拒绝与重试保留自动测试，但受上述 React/Wake 版本限制未执行；浏览器未主动注入拒绝条件。未执行完整读屏审计或全仓组件测试。

## 后续公共实现差距

公共紫色色板、统一密度与设计语言样板的部分业务参数仍待独立评审和迁移。本页将公共实现、站点覆盖、设计建议分开说明，不因文档重构而声称全库主题或组件默认规格已经统一。
