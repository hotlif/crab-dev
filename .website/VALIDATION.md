# 文档站验收记录

最新工具链验证见 [Wake 0.1.43 与原生 Lint 迁移](./review/toolchain/README.md)；持续视觉审查见 [质量提升记录](./review/quality/README.md)。下列记录保留各次验证时的版本与结果，不代表当前候选版本全部通过。

## 视觉细节审查与修正（2026-09-11）

从实际生产页面审查首页、目录、指南、Button / NumberEdit / TextEdit / LineEdit 页面、三个实战，以及搜索和移动导航。审查结果已直接落实到站点和示例源，没有修改组件公共 API、Wake、依赖版本或发布网站。

| 发现 | 修正 |
| --- | --- |
| 顶栏和导航对齐不一致，标题、分组与说明字重层级偏重 | 对齐站点外壳；标题与首页卡片使用 600 字重，侧栏分组使用 500；桌面搜索与主题控件等高 36 px。 |
| 手机搜索文字纵向换行，首页导航被挤到第三行，主标题在词语中间断开 | 减少搜索内边距并保持文字不换行；首页复用手机内边距；标题按两个完整短句换行。三个顶栏控件同排，均高 44 px。 |
| 预览标题、重置与装饰横线相互争抢注意力 | 通过公开 ComponentPreview 令牌移除装饰线，标题使用轻背景，重置降低字重并增加间距。 |
| 教程步骤像一排主操作按钮，讲解与练习层级混杂 | 步骤采用中性选中态，桌面高 40 px、手机高 44 px；操作和预期结果放入独立说明区。首轮出现的 60 px 过高按钮已修正。 |
| API 筛选框过窄，属性类型、正文和默认值缺乏层次 | 筛选框可伸缩、最大 384 px，加入搜索图标；属性说明使用 14 px，类型与默认值降为辅助层级，保留必填和弃用标记。行内代码增加轻底色。 |
| 13 个示例的标签紧贴输入，资料表单提示和提交结果间距不一致 | 在示例源内定义上置标签、8 px 间距；资料表单控制宽度，提示靠近字段，成功结果与按钮间隔 16 px。NumberEdit、TextEdit、三个实战同步调整。 |
| 实战表格只占 320 px，筛选与操作纵向堆叠 | 采用 Table 已有的自动列宽和 40 px 行高，筛选区自适应两列，操作使用可换行工具栏；手机自然堆叠。选区填充改为透明主题色。 |
| 深色成功提示呈蓝色，编辑抽屉提示留白过大 | 反馈背景使用对应色相并以 Oklab 混合 OKLCh 令牌，避免深色 Zinc 的色相参与极坐标插值；抽屉提示去掉默认段落外边距，关闭与保存点击区域为 44 px。 |

本轮日志为 `review/*-visual-audit*.log`，最终构建见 `build-visual-audit-final-colors.log`。

| 检查 | 真实结果 |
| --- | --- |
| `corepack yarn generate:token` | 48 个任务成功、全部缓存命中；已有循环依赖告警仍存在。 |
| `corepack yarn workspace @crab-dev/website test` | 最终 23 项生成器测试和内容一致性检查通过；React 测试仍被 `WAKE_TEST_REACT_VERSION` 阻塞，实际 19.3.0 不在 `>=19.2.8 <19.3.0` 范围。 |
| `corepack yarn workspace @crab-dev/website eslint` | 未通过：typescript-eslint 不支持 TS 7.0，初始化失败。 |
| `corepack yarn workspace @crab-dev/website typecheck` | 未通过：TS6053 无法解析 PnP 共享配置，并产生后续 JSX / 模块解析错误。 |
| `corepack yarn exec tsc -p .website/review/visual-audit-typecheck.json` | 最终辅助诊断通过，覆盖适配器及本轮修改的示例；不替代正式类型检查。 |
| `corepack yarn workspace @crab-dev/website build` | 最终通过，2 分 17.5 秒；221332 模块、2502 文件、179.84 MB、61 路由。52 个组件页、55 份教程、117 个教学示例、246 个唯一 Demo，0 个生成内容漂移。 |

生产浏览器实际检查：

- 首页检查 1440 / 320 px；目录深色 1440 px 保留 52 个入口；指南深色 1440 px 的代码块保留 1 / 10 / 1 / 1 / 2 行，行内代码为 14 px，标题为 600 字重。320 / 768 / 1440 px 的已检查页面可用宽度与滚动宽度分别相等，为 305 / 753 / 1425 px。
- API 搜索 `size` 只显示对应属性，无匹配词时显示明确空状态；NumberEdit 与 TextEdit 标签均正常，多行输入没有重复 outline。搜索方向键选中“链接按钮”，输入保持单层焦点；手机搜索弹层宽 288 px、无内部横向溢出。
- 数据列表在 1440 px 下三列各 283 px，在 320 px 下三列各约 77.66 px；搜索“林晓1”返回 3 条数据。模拟失败后可重试，模拟空数据后有说明且没有残留表格。完整拖拽框选没有完成专项回归，不记为通过。
- 资料表单检查浅色手机与深色手机 / 平板。用键盘清空姓名后提交，出现“请输入姓名”和红色边框；正常提交显示“已保存：林晓”。最终深色成功背景为 `oklab(0.20996 -0.0222336 0.00895408)`，已呈绿色，结果与按钮间距 16 px。
- 后台实战在 320 px 可打开成员管理和编辑抽屉；字段宽 272 px，无整页溢出；关闭和保存按钮均高 44 px，提示 14 px、外边距为 0。保存后抽屉关闭并显示“已保存：林晓”。
- 移动导航检查分组、当前项和关闭入口；Escape 关闭后焦点回到“导航”。最终实际 Chrome 的 LineEdit 标签已刷新，地址保留，加载到新的 36 px 搜索入口与 600 字重标题。独立最终首页预览控制台无错误，临时视口尺寸已恢复。

验收期间替换生产构建后，旧预览曾请求已经被替换的示例 chunk，刷新后恢复；最终检查使用最新入口。没有将该旧资源错误误判为组件回归。此轮未重复全仓 Library 构建、全部 246 个 Demo、独立 iframe 工作台、子路径部署、系统强制颜色或屏幕阅读器验收；这些项目不计为本轮通过。

## 输入框焦点修正（2026-09-11，上轮记录）

用户截图中的亮蓝双框已在实际 Chrome 的 LineEdit 基础示例复现：输入容器为 1 px 蓝色边框加 3 px 完全不透明蓝色阴影，内部 input 又被站点全局规则加上 2 px outline。此次将站点通用焦点规则降为低优先级兜底，并排除真实示例边界；通过站点 L2 主题将焦点改为中性灰边框及 18% 透明度光晕。强制颜色模式仍保留系统色 outline，因为该模式不显示阴影。

LineEdit 两个教学步骤的标签改为上置，与输入间隔 8 px，基础步骤同时显示已有的 30 字长度限制。运行源码与展示源码由生成器同步。没有更改组件库默认令牌、组件公共 API、依赖或 Wake 源码。

本轮日志为 `review/*-preview-focus.log`。

| 检查 | 真实结果 |
| --- | --- |
| `corepack yarn generate:token` | 48 个任务成功、全部缓存命中；已有循环依赖告警仍存在。 |
| `corepack yarn workspace @crab-dev/website generate:docs` | 从两个示例源更新 1 个生成文件；52 个组件页、55 份教程、117 个教学示例、246 个唯一 Demo 保留。 |
| `corepack yarn workspace @crab-dev/website test` | 23 项生成器测试和一致性检查通过；React 测试仍被 `WAKE_TEST_REACT_VERSION` 阻塞，React 19.3.0 不在要求的 `>=19.2.8 <19.3.0` 范围。 |
| `corepack yarn workspace @crab-dev/website eslint` | 未通过：typescript-eslint 不支持 TS 7.0，初始化失败。 |
| `corepack yarn workspace @crab-dev/website typecheck` | 未通过：TS6053 无法解析 PnP 共享配置，后续产生 JSX、模块解析等错误。 |
| `corepack yarn exec tsc -p .website/.tmp/ui-typecheck/tsconfig.json` | 适配器辅助类型诊断通过；不能替代正式类型检查。 |
| `corepack yarn workspace @crab-dev/website build` | 通过，2 分 43.4 秒；221332 模块、2500 文件、179.80 MB、61 路由。 |

生产浏览器检查：

- 用户实际 Chrome 标签已刷新。键盘 Tab 聚焦基础输入后，`:focus-visible` 为 true，input 的 outline 为 none，容器保留 1 px 中性边框与 3 px、18% 透明度光晕。标签为 grid，间距为 8 px。
- 浅色与深色焦点均检查；深色使用浅灰边框。搜索输入仍只有宿主的 2 px 焦点环，内部 input 无 outline、无阴影；Escape 关闭后恢复搜索触发器焦点，触发器有可见 outline。API 筛选输入也无重复 outline。
- 第二步输入“验证输入”后计数为 4/30、输出同步更新；点击清除后输入为空，输出为“当前内容：空”。
- 320 / 768 / 1440 px 下页面可用宽度和滚动宽度分别为 305 / 753 / 1425 px，无整页横向溢出。320 px 下输入宽 235 px，桌面最大宽度为 320 px。
- Chrome 错误日志为空；恢复原窗口大小、浅色主题和第一步，保留基础输入供查看。内置预览也已打开 LineEdit 页面并确认新的标签、无内部 outline 和 18% 光晕。

本轮为文档主题与示例排版修正，没有重复全仓 Library 构建、全部路由、独立 iframe 工作台和子路径验收。系统强制颜色、屏幕阅读器未做实机验证，不计为通过。

## 搜索弹层优化（2026-09-11，上轮记录）

针对搜索截图中的重复蓝色边框、结果选中态与输入焦点混淆、输入随列表滚动、底部提示不可见，以及手机长标题形成多行粗体的问题重新调整。输入容器提供单层焦点环；结果通过柔和填充、文字和打开标记表达选中态。输入、结果数量和快捷键提示保持固定，仅结果列表滚动。示例标题按“名称 — 说明”分层展示，并保留原来源和完整无障碍描述。

继续复用 Crab Dialog、LineEdit、Button、Empty、Alert 和 Spin，使用现有公开属性及站点自有类名。搜索关键词、结果和当前项仍由 Wake 管理。输入与结果使用 combobox / listbox / option 和 aria-activedescendant 关联；加入清除、IME 防误触、加载与错误时禁止旧结果导航。没有结果时，“编辑关键词”操作只聚焦输入。此轮没有更改依赖、Wake 源码、组件公共 API 或发布网站。

本轮日志为 `review/*-search-polish.log`。

| 检查 | 真实结果 |
| --- | --- |
| `corepack yarn generate:token` | 48 个任务成功，缓存命中；原循环包依赖告警仍存在。 |
| `corepack yarn workspace @crab-dev/website test` | 23 项生成器测试和内容一致性检查通过；React 测试继续被 `WAKE_TEST_REACT_VERSION` 阻塞，实际 19.3.0 不在要求的 `>=19.2.8 <19.3.0` 范围。新增与修改的搜索回归用例没有记为通过。 |
| `corepack yarn exec tsc -p .website/.tmp/ui-typecheck/tsconfig.json` | 最终辅助诊断通过，覆盖适配器及测试；不能替代正式类型检查。测试数据曾直接赋值只读 results，已改为构造初始状态并重新通过诊断。 |
| `corepack yarn workspace @crab-dev/website eslint` | 未通过：typescript-eslint 不支持 TS 7.0，初始化失败。 |
| `corepack yarn workspace @crab-dev/website typecheck` | 未通过：TS6053 无法解析 PnP 共享配置，并产生 JSX 和模块解析等后续错误。 |
| `corepack yarn workspace @crab-dev/website build` | 最终通过，2 分 35.7 秒；61 路由、2499 文件、179.79 MB。52 个组件页、55 份教程、117 个教学示例、246 个唯一 Demo 均保留，0 个内容漂移。 |

本轮在用户实际 Chrome 标签中检查生产页面，并同步内置预览：

- 输入元素的 outline 为 none，站点输入容器有单独焦点环；选中结果 outline 为 none，采用填充和打开标记。焦点保留在 combobox。
- 连续 7 次 ArrowDown 后，选中“无障碍使用指南”，结果区 scrollTop 为 329；输入与提示坐标完全不变，当前结果底部与滚动区域底部对齐。
- 搜索 Button 得到 12 个结果；清除后关键词为空，焦点回到输入。无匹配词时没有 option、aria-expanded 为 false、没有 aria-activedescendant，Enter 不导航，“编辑关键词”保持弹层打开并聚焦输入。
- Enter 打开 Button 组件页，关闭动画结束后焦点进入 H1；浏览器后退回到首页。Escape 关闭搜索后焦点回到“搜索文档”触发器。
- 检查浅色与深色、1440 / 768 / 320 px，未发生整页或结果项横向溢出。320 × 568 px 下弹层底部约 497 px，结果区仍可滚动；清除、关闭、打开操作均为 44 px 高。
- 首轮手机检查发现示例完整标题过长，已在最终产物确认显示“链接按钮”“按钮组”等独立名称，说明与来源分层；正文仍完整保留在 DOM 和无障碍关联中。最终 Chrome 错误日志为空。

最终恢复 Chrome 原窗口大小与浅色主题，两个浏览器均保留首页的 Button 搜索结果用于查看。加载失败状态、真实中文输入法候选确认、屏幕阅读器、强制颜色和系统减少动态模式没有完成实机验证；相关行为代码与测试已补充，不能视为测试已通过。本轮没有重复全仓 Library 构建、全部组件路由、246 个 Demo 或子路径构建验收。

## 组件目录重构（2026-09-11，上轮记录）

后续用户截图核对：Chrome 中同地址的已打开标签仍保留旧“组件学习地图”，实际有 7 个表格、0 个新目录入口；内置浏览器已是新版本。已刷新用户实际查看的 Chrome 标签，确认标题变为“组件目录”、表格数量为 0、组件入口为 52。Chrome 当前 1920 px 窗口下可用宽度与滚动宽度均为 1905 px，目录显示四列，浏览器错误日志为空，用户浅色主题保留。此次只同步预览并补充维护说明，没有再次修改产品设计或重新构建。

针对 `/learn/components` 的视觉反馈，将紧密排列的原始 Markdown 表格改为分类组件目录。页面标题改为“组件目录”，地址保持不变。名称、中文用途名和原学习步骤分层显示，每个入口整体可点击；分类数量由现有记录生成，常用界面组件先于基础工具。该页面使用两栏外壳与正文分类索引，取消重复的右目录和折叠目录。指南与组件详情继续使用原阅读布局。

目录内容仍来自 `navigation.toml` 与 `content/*.json`，通过 `generate:docs` 生成，没有手改生成页；52 个包、7 个原分类锚点以及每个包的完整学习步骤均保留。展示使用 Crab Button 和站点公开链接解析，没有新增依赖、修改组件公共 API、Wake 源码或发布网站。

本轮日志为 `review/*-catalog.log`，初次构建失败日志单独保存在 `review/build-catalog-initial.log`。

| 检查 | 本轮结果 |
| --- | --- |
| `corepack yarn generate:token` | 48 个任务成功，缓存命中；循环包依赖告警仍存在。 |
| `corepack yarn workspace @crab-dev/website test:generator` | 23 项通过，包含新增分类顺序、数量、入口、步骤和锚点回归检查。 |
| `corepack yarn workspace @crab-dev/website check:docs` | 52 个组件页、55 份教程、117 个教学示例、246 个唯一 Demo，0 个文件漂移。 |
| `corepack yarn exec tsc -p .website/review/catalog-typecheck.json` | 辅助诊断通过，覆盖目录组件、样式与 UI 适配器；沿用此前物理类型映射，不替代正式类型检查。 |
| `corepack yarn workspace @crab-dev/website build` | 最终通过，4 分 10.7 秒；61 路由、2499 文件、179.79 MB。 |
| `corepack yarn workspace @crab-dev/website test` | 前置生成器及一致性检查通过；React 测试继续被 `WAKE_TEST_REACT_VERSION` 阻塞，实际 React 19.3.0 不在要求的 `>=19.2.8 <19.3.0` 范围。 |
| `corepack yarn workspace @crab-dev/website eslint` | 未通过：typescript-eslint 不支持 TS 7.0，初始化失败。 |
| `corepack yarn workspace @crab-dev/website typecheck` | 未通过：TS6053 无法解析 PnP 共享配置，并产生 JSX、模块解析等后续错误。 |

本轮新增构建失败已单独定位并修复：Wake 将目录 JSX 属性展开编译为非法表达式，报 `WAKE0200`。生成器已改为单个显式 `item` 数据属性，并增加禁止该展开写法的回归检查；随后重新生成、检查类型、运行生成器测试并完成生产构建。没有修改 Wake 或生成中间文件。

生产浏览器验证：

- 实际渲染 52 个唯一组件入口，7 个 H2 的原分类 ID 均存在。1440 px 为三列，默认窗口约 1000 px 为两列，768 px 与 320 px 根据可用正文宽度变为单列。
- 1440 / 768 / 320 px 的可用宽度与滚动宽度分别为 1425 / 753 / 305 px，没有整页横向溢出。768 和 320 px 下检查全部 52 个入口，均无内部横向溢出。
- 检查明暗主题、长英文名称、中文步骤及悬停状态。手机分类入口高 44 px；键盘 Tab 有可见焦点环，Enter 从 Button 入口进入真实组件页后焦点落到 H1。
- “基础能力”和“反馈与浮层”索引跳转有效；320 px 下“反馈与浮层”标题位于视口约 144 px，顶栏底部约 97 px，没有遮挡。对应分类深链接刷新后保留正确标题与位置。
- 回归指南页面，1440 px 下仍是三栏阅读布局，右目录可见。最终浏览器错误日志为空。

验收后恢复默认视口与深色主题，保留 `http://127.0.0.1:4173/learn/components` 预览。本轮没有重新执行全部 Library 构建、52 条组件路由逐项交互、246 个 Demo 操作或子路径生产构建；系统强制颜色与屏幕阅读器也未做实机验证，不计为通过。此前范围保留在下方历史记录。

## 侧栏与阅读细节修正（2026-09-11，上轮记录）

针对用户提供的指南侧栏截图重新检查：原字符箭头贴住文字，层级缩进不足，当前页只有单侧标记而悬停填充更醒目。此次将分组箭头放在行尾，使用独立 SVG 和公开展开状态驱动；分组标题、子页缩进及中性导线分别表达层级。当前页采用蓝色填充与加重文字，悬停采用中性色，键盘焦点独立显示。右侧与页内目录同步调整状态和间距。

检查正文时发现自定义 CodeBlock 缺失行边界，导致高亮节点连成一行。现在按公开源码建立站点自有行容器，保留空行与缩进；节点数量不匹配时使用完整源码，复制仍由 Wake 执行。移动导航改用已有 Crab Drawer，从左侧打开，只有可聚焦的关闭按钮，不再出现无关的“取消／确定”。使用站点挂载点捕获原生 close 事件，在退出动画完成后处理选页焦点；没有依赖 Drawer 私有结构或扩大组件 API。

本轮日志为 `review/*-navigation-polish.log`。

| 检查 | 本轮结果 |
| --- | --- |
| `corepack yarn generate:token` | 48 个任务成功，缓存命中；Turbo 仍报告循环包依赖告警，未在本轮修复。 |
| `corepack yarn workspace @crab-dev/website build` | 最终构建成功，2 分 28.3 秒；61 条路由、2498 个文件。生成器确认 52 个组件页、55 份教程、117 个教学示例、246 个唯一 Demo，0 个内容漂移。 |
| `corepack yarn exec tsc -p .website/.tmp/ui-typecheck/tsconfig.json` | 最终辅助类型诊断通过；不替代正式类型检查。适配 Drawer 时尝试的 `onCloseCapture` 不在公开类型中，已改为站点挂载点原生事件监听并重新通过诊断。 |
| `corepack yarn workspace @crab-dev/website test` | 22 项生成器测试及内容一致性检查通过；React 测试仍被 `WAKE_TEST_REACT_VERSION` 阻塞。要求 `>=19.2.8 <19.3.0`，实际为 `19.3.0`。新增代码行和移动导航关闭用例没有被记为通过。 |
| `corepack yarn workspace @crab-dev/website eslint` | 未通过：typescript-eslint 不支持 TS 7.0，初始化失败。 |
| `corepack yarn workspace @crab-dev/website typecheck` | 未通过：TS6053 无法解析 PnP 内共享 tsconfig，产生 JSX 和模块解析等后续错误。 |

生产页面实际检查：

- 1440 / 768 / 320 px 的文档可用宽度与滚动宽度分别为 1425 / 753 / 305 px，没有整页横向溢出。检查明暗主题，展开多个侧栏分组后链接内容仍在侧栏内。
- 鼠标停在“无障碍使用指南”时，“开始使用”仍保持更明确的当前页状态。分组按钮按 Enter 可以展开和收起非当前分组，操作后焦点保留在按钮；当前分组展开策略仍交由 Wake 管理。
- 320 px 的导航抽屉只显示分类入口和关闭按钮；导航行至少 44 px，关闭按钮为 44 × 44 px。Escape 关闭后焦点回到“导航”，选择“无障碍使用指南”后抽屉完成退出，焦点进入目标页 H1。点击关闭按钮也可正常关闭。
- 指南代码块行数为 1 / 10 / 1 / 1 / 2，JSX 示例的空行和缩进可见。实际复制得到 264 个字符的完整源码，保留 10 行、空行和缩进。
- 最终生产页面错误日志为空。验收后恢复默认视口、深色主题、关闭导航，保留指南地址 `http://127.0.0.1:4173/guides/getting-started`。

本轮只修改站点适配器、样式、相关测试和维护记录，没有修改依赖、Wake 源码、组件公共 API 或发布网站。本轮没有重复全仓 Library 构建、全部路由与 246 个 Demo 逐项交互、子路径部署、系统强制颜色和屏幕阅读器检查；这些范围不计为本轮通过。下方保留此前验收历史。

## 组件陈列视觉重构（2026-09-11，上轮记录）

根据本轮“相比优秀组件库仍有明显视觉差距”的反馈重新设计。对照 [shadcn/ui 首页](https://ui.shadcn.com/) 的实际页面，重点检查首屏构图、组件组合和信息密度；没有复制其组件实现或引入第三方 UI。以下为本轮结果，下方记录保留为此前验收历史。

- 首页从双栏介绍与单个资料表单改为居中介绍和三列真实组件陈列。六张卡片展示发布清单、状态标签、访问图表、通知偏好、团队邀请和键盘操作。通过现有 Card、Checkbox、Avatar、Tag、BarChart、Segmented、Switch、Form、LineEdit、Button 组合，保持一个按需加载的运行入口。
- 明暗主题采用中性主操作色，页面、卡片和输入区域分层；蓝色用于图表、链接及焦点。减少首屏文案和留白，移除陈列外层重复预览框。使用公开 ComponentPreview 令牌降低文档示例的边框和阴影，保留其源码、复制与状态功能。
- 调整中文字体回退顺序，改善导航、页标题与辅助文字的节奏。搜索框填满可用宽度，手机端主操作保持并排；团队邮箱可换行，任务文字与复选框共同构成触控目标。
- 任务勾选驱动完成数量、进度条与完成状态；切换周期更新图表和统计值；通知状态独立。邀请表单验证邮箱、提供异步反馈，仅创建本地演示结果，不发送邮件。重置重新挂载完整陈列。
- 首页仍沿用 `docs/examples/home/profile.tsx` 作为真实源码源文件；通过 `generate:docs` 更新展示源码。六组组件索引、全部公开路径、三个实战和独立组件工作台入口保留。本轮没有修改依赖、组件公共 API 或 Wake 源码，没有发布网站。

本轮命令日志为 `review/*-gallery.log` 和 `review/build-component-gallery.log`。

| 检查 | 本轮结果 |
| --- | --- |
| `corepack yarn generate:token` | 48 个任务通过（缓存命中）。 |
| `corepack yarn workspace @crab-dev/website test:generator` | 22 项通过。 |
| `corepack yarn workspace @crab-dev/website check:docs` | 52 个组件页、55 份教程、117 个教学示例、246 个唯一 Demo，0 个漂移。 |
| `corepack yarn exec tsc -p .website/.tmp/ui-typecheck/tsconfig.json` | 辅助类型诊断通过；不能替代正式类型检查。 |
| `corepack yarn workspace @crab-dev/website build` | 最终生产构建通过，2 分 29.4 秒；61 条路由、2500 个文件，输出 `docs-dist/`。 |
| `corepack yarn workspace @crab-dev/website test` | 前置生成器和一致性检查通过；React 测试仍被 `WAKE_TEST_REACT_VERSION` 阻塞。要求 `>=19.2.8 <19.3.0`，实际保留 `19.3.0`。 |
| `corepack yarn workspace @crab-dev/website eslint` | 未通过；`typescript-eslint` 不支持当前 TS 7，初始化失败。 |
| `corepack yarn workspace @crab-dev/website typecheck` | 未通过；PnP 共享 tsconfig 解析失败，伴随 JSX 和模块解析错误。 |

浏览器检查覆盖 1440 / 768 / 320 px 与明暗主题，页面宽度与滚动宽度分别为 1425 / 753 / 305 px，没有整页横向溢出。320 px 下六张陈列卡片宽度 271 px、内部滚动宽度 269 px，没有内容横向裁切；首页两个主操作处于同一行。768 px 陈列为两列，团队与键盘卡片在后一行并列。

已经实际操作任务勾选、周期切换、通知开关、邮箱错误与成功提交、输入焦点、完整重置、源码展开与复制。复制内容包含完整 `ComponentGallery`、样式、依赖导入和源码标记，并与本次运行文件一致。源码展开时仍只有一个 preview，且不撑宽页面。搜索方向键与 Enter 导航后焦点进入 Button 页 H1，Escape 取消后焦点回到搜索触发器。指南、Button 组件页和三个实战入口完成加载检查。

最终产物复核：四项任务全部选中后进度为 4，项目状态显示“✓ 已完成”；320 px 下每项任务的触控目标为 229 × 44 px，搜索输入框为 225 × 44 px，填满搜索区域。合并可点击任务文字时发现默认行高压紧说明，已修正；最终标题行高 22.4 px、说明行高 19.2 px、复选框与文字间距 12 px。再次读取剪贴板，确认包含完成状态、触控区域与行距修正后的完整源码。生产页面错误日志为空，视口已恢复默认，深色首页预览保留在 `http://127.0.0.1:4173/`。

本轮没有重新执行全部 Library 构建、全部组件路由交互、子路径部署或 246 个 Demo 的逐个操作；这些范围的此前结果保留在下方，不计为本轮重新通过。强制颜色、减少动态和屏幕阅读器仍未完成实际系统环境检查。正式测试、ESLint 与类型检查的兼容阻塞没有被隐藏或标为通过。

## 视觉重构（2026-09-11，上轮记录）

本节取代下方统一改版中关于首页表现的描述。此次保留 Wake / Crab CSS `0.1.38` 和所有文档内容，重新设计首页及共享阅读界面，没有修改组件公共 API、依赖版本或发布网站。

- 首页从纵向宣传区与两份表单改为双栏介绍和一个真实交互示例。示例标题通过站点容器组合，继续复用 ComponentPreview 的源码、复制功能；重置、校验和异步保存保留。
- 组件目录提供 24 个常用组件直达链接。六个分类入口进入学习地图对应章节，修复了分类名称与单个组件目标不匹配的问题；52 个包的完整入口保留。
- 学习路径收敛为三个操作入口，三个实战改为紧凑介绍和直接链接。原实战目录仍保留三个真实运行示例，完整教程没有删除。
- 明暗主题在站点作用域覆盖 L2 语义颜色，区分页面、面板、边界和文字；蓝色用于主要操作、焦点与强调。补充中文无衬线字体回退，统一正文、导航和元信息字号。
- 生产截图发现并修复了品牌标志换行、浏览器默认访问链接颜色、源码展开导致介绍下移，以及长搜索结果行距过紧的问题。
- 内页截图进一步发现 Button 的链接分支遗漏 `aria-current`。在已有属性中补充透传，并添加当前页状态变化的回归测试；没有扩大公共类型或增加新属性。Button Library 构建通过；包内 ESLint 与测试仍被同样的 TS 7 / React 版本兼容问题阻塞。

本轮日志为 `review/*-visual-refactor.log`。令牌生成 48 个任务通过；生成器 22 项通过；内容一致性检查仍为 52 个组件页、55 份教程、117 个教学示例、246 个唯一 Demo，0 个漂移。辅助类型诊断通过。正式站点测试、ESLint 和类型检查仍分别被 React 版本检查、TS 7 解析器支持和 PnP 共享配置解析问题阻塞，没有标为通过。

浏览器已检查 1440 / 768 / 320 px 与明暗主题。首页真实表单保存、重置、源码展开及复制通过；实际剪贴板内容包含本次更新后的完整 Profile 源码。分类索引跳到正确章节，锚点在顶栏下方；搜索支持方向键与 Enter 选择，关闭后焦点进入目标页面 H1，Escape 取消后焦点恢复搜索触发器。320 px 首页与 Button 页可用宽度、滚动宽度均为 305 px，768 px 均为 753 px，没有整页横向溢出。浏览器前进后退通过；源码展开前后介绍的页面坐标保持 133 px；长搜索标题行高为 22.4 px，窄屏弹层宽度正常。

最终生产构建通过（2 分 29.7 秒，61 条路由、2500 个文件）。生产页面确认顶栏“组件”和左侧“Button 按钮”均带有 `aria-current="page"`，当前章节目录同时显示选中样式。最后一次浏览器错误日志为空。验收后恢复默认视口与深色主题，预览保留在首页 `http://127.0.0.1:4173/`。本轮没有重新逐一操作全部 246 个进阶示例，未把历史验收视为本轮执行；系统强制颜色、减少动画及屏幕阅读器仍未做真实环境验证。

## Wake 0.1.38 统一改版（2026-09-11，基础接入结果）

环境为 Windows / PowerShell 7.6.5、Node.js 22.15.0、Corepack / Yarn 4.18.0 PnP。全仓直接声明的 Wake 与 Crab CSS 已精确统一为 `0.1.38`，项目约定同步更新；按要求保留 React `19.3.0` 与 TypeScript `7.0.2`。已有工作区改动保留，本次没有修改 Wake 源码或发布网站。

本节替代后文历史记录中关于 0.1.34、未替换 Wake 外壳、首页独立导航和外部补丁待实施的描述。当前通过 `.website/wake.config.toml#docs.ui` 注册全部 14 个公开展示组件，依据 [Docs UI API](https://github.com/hotlif/wake/blob/canary/docs/reference/docs-ui.mdx) 和 [Crab UI 接入指南](https://github.com/hotlif/wake/blob/canary/docs/wake-docs/crab-ui.mdx) 实现。路由、搜索、主题偏好和 Demo 状态仍交由 Wake 管理。旧外部补丁没有在本次应用。

### 交付内容

- 统一品牌顶栏、分类导航、搜索弹层、主题选择和三栏阅读界面；平板隐藏右目录，手机提供导航弹层与页内目录。
- 首页提供快速开始、六组组件入口和真实组件示例，保留学习路径及三个实战。
- 组件页按用途与包名、基础示例、分步教程、API、进阶示例和使用说明组织；基础示例复用教程第一步的真实源码与加载入口。
- 所有 52 个组件页、55 份教程、117 个教学示例（116 个步骤加首页示例）、246 个唯一进阶 Demo 和 61 条生产文档路由保留；原 API 锚点、组件路径与独立工作台入口保留。
- 教程目录使用站点 Context 与自有挂载区域；主题和文章样式使用 Crab 令牌，不再覆盖 `.docs-shell`、`.toc-column` 等 Wake 内部结构。正文链接、查询参数、锚点及 iframe 支持部署前缀。
- 新增 10 项展示适配器行为测试，覆盖导航回调、搜索键盘与关闭焦点、主题、目录、复制失败、API 空结果与警告、页面重试、部署链接和全屏唯一 preview。正式执行受下述 React 限制阻塞。

### 本轮命令结果

除注明外，从仓库根目录运行。完整输出保存在 `review/*-0.1.38.log`。

| 命令 | 真实结果 |
| --- | --- |
| `corepack yarn install`，随后 `corepack yarn install --immutable` | 通过；安装过程更新 lockfile 与 PnP，现有 peer 告警仍存在。 |
| `corepack yarn generate:token` | 48 个任务通过。 |
| `corepack yarn build:library` | 52 / 52 任务通过，无缓存，56.4 秒。 |
| `corepack yarn test` | 未通过，38 / 71 任务成功；React 测试被 `WAKE_TEST_REACT_VERSION` 阻塞，Turbo 报告 `rc-tag#test` 失败。 |
| `corepack yarn workspace @crab-dev/website generate:docs` | 通过；页面由生成器更新，没有手改生成页。 |
| `corepack yarn workspace @crab-dev/website test:generator` | 22 项通过。 |
| `corepack yarn workspace @crab-dev/website check:docs` | 上述内容库存一致，0 个文件漂移。 |
| `corepack yarn workspace @crab-dev/website eslint` | 未通过；`typescript-eslint` 初始化报告不支持 TS 7.0，未完成源码检查。 |
| `corepack yarn workspace @crab-dev/website typecheck` | 未通过；TS6053 无法解析 PnP 内共享 tsconfig，并产生 JSX、模块解析等后续错误。 |
| `corepack yarn workspace @crab-dev/website test` | 前置 22 项生成器测试及一致性检查通过；Wake React 测试启动失败。 |
| `corepack yarn workspace @crab-dev/website build` | 通过，61 路由、2501 文件，2 分 32.5 秒；输出 `docs-dist/`。 |
| 在 `.website` 运行 `corepack yarn exec wake docs build . --base /handbook/ --outdir .tmp/docs-handbook` | 通过，61 路由、2501 文件，2 分 38.5 秒。 |
| 在 `.website` 运行 `corepack yarn exec wake docs build review/ui-fixture --outdir .tmp/ui-fixture` | 独立验收夹具构建通过，1 路由；不进入正式文档内容。 |

Wake 0.1.38 的 React 测试要求匹配的 `react` / `react-dom >=19.2.8 <19.3.0`，当前为 `19.3.0`。没有通过降级、关闭版本检查或更换工具来掩盖失败。TS 7 / PnP 和 ESLint 初始化问题也未宣称解决。

辅助类型诊断：仅在被忽略的 `.tmp/ui-typecheck/` 中将当前安装版本的类型声明映射到物理路径，运行 `corepack yarn exec tsc -p .website/.tmp/ui-typecheck/tsconfig.json` 通过，覆盖新适配器、相关站点源码和新增测试。正式配置没有替换；该结果不能替代上表失败的正式 `typecheck`。

### 最终生产浏览器检查

- 首页、指南、全部 52 个组件路由及其基础示例完成加载冒烟；三个实战的 12 个步骤逐一切换，无示例错误提示。没有把历史轮次的全面交互结果视为本轮重新执行。
- 检查 1440、768、320 px 以及明暗主题。320 px 下文档可用宽度与滚动宽度均为 305 px；768 px 均为 753 px。宽源码在自身容器内滚动，API 长类型与正文表格没有造成整页横向溢出。
- 搜索关键词、方向键和 Enter 选择、关闭后正文标题焦点通过；移动导航选择页面后焦点进入正文，Escape 取消后恢复导航触发器。主题选择和导航分类展开状态可用。
- API 快捷入口跳转至原 `#api`，属性筛选与空结果反馈正确；查看源码、复制反馈通过，实际读取剪贴板确认包含完整 Button 导入与源码。
- 浏览器历史返回、组件深链直接加载、旧工作台入口通过。`/handbook/` 生产站的首页、搜索、正文旧链接、章节锚点与 iframe 路径均保留前缀；实际从 Button 正文进入无障碍指南成功。
- 新增页面加载错误适配器的重试回调、复制失败可读源码与反馈，在独立生产验收夹具中通过。Demo 全屏打开、退出动画期间、关闭完成三阶段均只有一个 preview，窄视口操作生效。这是受控夹具验证，不等同于网络故障注入。
- 展开进阶示例时发现 `loadingStyle is not defined` 生产运行错误，已将加载状态样式改为单一静态 CSS 配合数据属性，并重新完成根路径及子路径构建。最终 Button 的 12 张卡片与可见 iframe、Dialog 的两个 iframe 以及 Segmented 的可见进阶示例加载正常。
- 未知文档地址显示自定义 NotFound 并返回 HTTP 404；根首页与子路径组件返回 200，子路径服务拒绝前缀外地址并返回 404。生产预览仅监听本机；正式站可用 `corepack yarn workspace @crab-dev/website preview` 启动，默认地址为 `http://127.0.0.1:4173/`。

### 仍未通过或未覆盖

1. 正式全仓/站点 React 测试、站点 ESLint 和类型检查仍有上表兼容阻塞；新增行为测试不能标为通过。
2. 强制颜色与减少动画的 CSS 规则已静态检查；浏览器工具不提供对应媒体偏好模拟，未完成实际系统模式和屏幕阅读器验收。
3. 246 个进阶 Demo 完成库存与入口保留检查，未逐一操作全部示例。一次较大批次浏览器检查超时后改为小批次复验，没有将未返回的结果计为通过。
4. 没有进行真实网络模块故障注入；重试和复制失败通过受控适配器夹具验证。后文已有组件问题仍作为历史记录保留，本次没有扩展组件公共 API。

## 历史记录（以下保留 2026-09-10 的原始验收结果）

验证日期：2026-09-10。验证环境：Windows、Node.js 22.15.0、Yarn 4.18.0 PnP。保留工作区现有 React 19.3.0、TypeScript 7.0.2、Wake / Crab CSS 0.1.34，本次没有调整这些版本。

## 交付范围

- 52 个包的教学页面、3 个四步实战教程，共 55 份教程、116 个独立 TSX 步骤。
- 文章位于 `content/`，运行源码位于 `docs/examples/`，教学容器位于 `docs/site/tutorial.tsx`。
- 实际源码通过生成器直接嵌入展示数据；动态导入仅加载当前步骤。属性调整写在对应示例内，复制代码包含调整项、数据、导入与样式，不依赖站点辅助文件。
- 原有 246 个进阶示例、组件地址、API 与工作台入口保留。学习地图、实战目录、首页、搜索索引与开发监听同步更新。

## 命令结果

以下命令从仓库根目录运行。

| 检查 | 结果 |
| --- | --- |
| `corepack yarn install --immutable` | 通过；现有 peer 依赖警告仍存在。 |
| `corepack yarn generate:token` | 48 个任务通过。 |
| `corepack yarn workspace @crab-dev/website test:generator` | 20 项通过，包含独立源码、路径边界、生成一致性、旧入口保留及 MDX 文字转义。 |
| `corepack yarn workspace @crab-dev/website check:docs` | 52 个组件页、55 份教程、116 个教学示例、246 个唯一 Demo，0 个文件漂移。 |
| `corepack yarn workspace @crab-dev/website build` | 生产构建通过；61 条路由，输出 `docs-dist/`。 |
| `corepack yarn workspace @crab-dev/website test` | 前置生成器测试与一致性检查通过；Wake React 测试未执行成功，见下文。 |
| `corepack yarn workspace @crab-dev/website typecheck` | 未通过，见下文。 |
| `corepack yarn workspace @crab-dev/website eslint` | 未通过，见下文。 |

补充诊断：临时将 PnP 中的类型声明映射到物理路径后，当前 TypeScript 对站点代码、116 个示例及新增测试的检查通过。该诊断未改变共享配置，临时脚本已移除；它不能替代正式 `typecheck`。

## 生产站浏览器验证

- 52 个组件页面及其 104 个教学步骤完成加载与切换冒烟；发现的 Router 隔离导航、TablePro 示例固定列问题已在教学范围内处理并复验。
- Button：外观调整、点击计数、切换步骤与重置；源码随步骤更新，复制内容包含完整导入与组件代码。返回步骤时状态重新初始化。
- 资料表单：受控输入、键盘清空姓名后的必填提示、保存中的禁用状态、成功反馈、模拟失败与重试；错误说明关联字段。
- 数据列表：搜索、分页、模拟失败、重试恢复、空数据与重新加载。
- 后台页面：标签切换、进入成员管理、编辑姓名并保存回列表；窄屏侧边导航入口可用。
- 三个实战教程的 12 个步骤逐一切换加载，没有出现示例错误提示。
- Router：成员页与概览往返，外层文档地址不变。模拟 History 完整包含在示例源码中，不发送网络请求。
- TablePro：第二步分页能展示第二页成员；Canvas：主题颜色可见，移动矩形同步更新位置文字。
- Tree：收起后隐藏子节点，展开后恢复；键盘 Enter 激活外部选择按钮后显示 `选中 ID：readme`。
- 桌面右侧步骤目录可用，键盘 Enter 切换后焦点进入教学步骤正文。搜索能命中新增步骤中的“每两条一页”相关文字。
- 浅色、深色主题及 320 / 768 / 1440 px 布局已检查；320 px 后台示例未造成页面横向溢出，宽代码在自身区域滚动。
- 旧 Button 工作台地址 `/components/rc-button/workbench/` 正常加载；未展开进阶示例时页面没有创建工作台 iframe。

## 尚未通过或仍需补验

1. **Wake React 测试版本限制**：`WAKE_TEST_REACT_VERSION` 要求 React / React DOM `>=19.2.8 <19.3.0`，工作区为 `19.3.0`。新增的懒加载、步骤隔离、重置、加载失败与重试测试已编写，但受此限制未运行通过。浏览器已验证业务模拟失败与重试；动态模块加载失败后的重试尚未完成真实浏览器故障注入验证。
2. **TypeScript 7 与 PnP**：正式 `tsc` 无法解析 `@crab-dev/standards-typescript-preset/tsconfig.browser.react.json`，报 `TS6053` 并引发 JSX、模块解析的后续错误。
3. **ESLint 解析器兼容**：现有 `typescript-eslint` 启动即报告 `does not support TS 7.0`，尚未进入源码检查。
4. **现有组件限制**：TablePro 默认固定序号列路径会触发 `isSkipCell is not defined`；教学显式设置 `showRowNumber={false}`。Tree 的不可拖拽节点仍有 `aria-disabled` 语义问题；教学提供独立的展开、选择按钮作为键盘入口。后台教程也提供直接打开成员页的按钮。这些改动没有修改组件公共 API，组件本身仍需独立修复。
5. **系统无障碍偏好**：已加入并静态检查 `forced-colors` 与 `prefers-reduced-motion` 样式；当前浏览器控制能力未提供媒体偏好模拟，尚未完成真实系统强制颜色、减少动画模式及屏幕阅读器验收。此记录不代表完整 WCAG 审计。

## 本地复验

先运行 `corepack yarn workspace @crab-dev/website build`，再运行 `corepack yarn workspace @crab-dev/website preview`，访问 <http://127.0.0.1:4173/>。预览服务只监听本机，不会发布网站。开发时运行根目录 `corepack yarn docs:dev`，内容与示例修改会触发重新生成。

## Button 页面视觉复验

本次后续调整统一了居中网格与 sticky 导航，限制文章样式对教学导航和讲解的影响，收紧章节间距，移除重复卡片与预览阴影。教学源码改为默认折叠，仍可展开查看。Button 两步示例显式使用 `justify-items: start`，实测按钮从 952 × 32 px 恢复为 80 × 32 px。

浏览器复验包含步骤切换、计数、重置、源码展开，以及桌面、768 px 和 320 px 布局与深色主题。生成器 20 项测试、一致性检查与令牌生成通过。正式站点测试、类型检查和 ESLint 仍受上文相同版本问题阻塞。本轮复制按钮可触发，但自动化未读到预期剪贴板内容，不能将本轮复制复验标为通过。

最终生产构建通过（61 条路由）。320 px 下页面宽度为 305 px；代码保留整行，在 227 px 宽的代码框内滚动，没有造成页面横向溢出。桌面步骤目录通过 Enter 切换后焦点进入当前教学正文。

## 教学源码与操作组织重新审查

再次审查了全部 116 个运行文件，删除 48 个没有必要的外层包装；单组件示例直接返回组件，不再统一引入布局工具。必要的字段分组、容器尺寸、坐标和实战布局继续保留。Button 最小示例为 6 行，只有组件及样式导入和默认导出；第二步只增加状态与事件，不含布局 CSS。维护约定已补充相同要求，展示代码仍直接提取实际运行文件。

重置移至运行预览标题旁，上一步与下一步集中在教程翻页区。源码去除重复边框、行号和阴影，保留空行；窄屏在代码框内横向滚动。复制失败时提供展开源码后手动复制的提示。

生产浏览器已逐一加载并切换 52 个组件及 3 个实战的全部 116 个步骤，没有出现示例错误提示。补充验证了 Button 计数与重置、Select 选择杭州、资料表单保存林晓，以及 320 px 布局。Chrome 中复制按钮显示“已复制”，确认页面的复制调用成功；自动化剪贴板读取返回旧内容或空值，因此复制内容的逐字核对仍未完成，不能用读取到旧 Button 源码作为本轮通过证据。生成器 20 项、一致性与令牌生成通过；正式 React 测试、类型检查、ESLint 的现有版本阻塞未改变。

本轮最终生产构建通过，仍包含 61 条路由；最后一次一致性检查为 0 个文件变化。源码空行、隐藏行号与窄屏滚动已在生产页面复验。

## 首页示例与视觉验收（2026-09-10）

首页改为独立宽幅布局，保留顶部搜索和主题入口；组件内页继续使用文档导航。学习路径由重复文字卡片改为三步引导，三个实战增加明确标注的页面结构预览。正文链接配色限定于无 class 的文章链接，避免覆盖组件按钮前景色。

首页资料表单位于 `docs/examples/home/profile.tsx`，使用公开的 Form、LineEdit 和 Button。关键代码和可复制完整代码由生成器读取同一运行文件，新增生成器测试验证源码关联与缺失标记报错。库存现为 55 份教程、116 个教程步骤示例及 1 个首页示例；保留 52 个组件页和 246 个进阶 Demo。

本轮已验证：21 项生成器测试、一致性检查（0 漂移）、48 个令牌生成任务；浏览器验证空邮箱错误提示、姓名修改、Enter 提交、模拟保存成功、重置、源码展开及复制成功提示。剪贴板内容未做逐字读取验证。已检查桌面、768 px 深色和 320 px 布局；320 px 页面宽度 305 px，没有页面横向溢出，代码在框内滚动。强制颜色和减少动画规则已补充，未模拟系统偏好，不能宣称完整无障碍验收。

正式站点 React 测试仍被 WAKE_TEST_REACT_VERSION 阻塞；类型检查仍因 TS 7 / PnP 无法解析共享 tsconfig 而失败；ESLint 仍因 typescript-eslint 不支持 TS 7 在初始化时失败。没有修改工具版本。开发服务已重启，以刷新缓存的首页样式与生成器。

最终生产构建通过（2m7.4s，61 条路由）；最后一次一致性检查为 0 漂移。最终生产包在 320 px 下复验：正文宽度 305 px，首页标题 31.5 px，移动文档菜单隐藏；进入学习地图和 Button 内页后导航恢复，原 API 锚点存在。已恢复验收前的浅色主题和默认视口。5173 开发服务保持运行。

## 组件复用修复（2026-09-10）

本节替代上文关于手画实战结构预览、首页独立源码面板和网站覆盖预览边框、行号的描述。首页与实战入口现在使用 Crab Tabs 切换三个教程的真实最终步骤，加载入口和展示源码直接复用生成的教程数据。LiveExample 统一使用 ComponentPreview、Button、Alert 和 Spin；工作台 iframe 的失败、重试、空状态与加载提示也复用对应组件。

ComponentPreview 内部操作栏改用 Crab Button，移除独立按钮样式，展开操作通过 useId 关联源码区域。长代码保持整行，组件与站点 Grid 分别约束自身宽度；不再通过网站样式隐藏行号或修改组件边框。新增 Button 工作区依赖，包内补充 Wake Test 入口与复制成功、复制失败两项回归测试，未调整依赖版本。

已通过：22 项生成器测试（包括站点、教学示例与 ComponentPreview 的基础控件复用检查）、文档一致性检查、ComponentPreview 令牌生成与 Library 构建、`yarn install --immutable`。安装仍报告工作区已有的 ESLint、TypeScript peer 范围等告警，不代表工具链兼容问题已解决。

生产浏览器验证了三个实战真实挂载、数据加载失败与重试恢复 23 条成员、空数据反馈、后台 Drawer 编辑并保存、切换实战恢复初始数据、表单重置、方向键切换 Tabs。复制操作显示“已复制”，未核对系统剪贴板逐字内容。320 px 下展开完整源码时页面宽度与滚动宽度均为 305 px，源码容器约 229 px，长代码在容器内滚动；768 px 下两者均为 753 px。复制与源码操作的 DOM 类名确认为 `rc-button-*`。系统强制颜色与减少动画仍未完成真实环境模拟。

正式站点测试与新增组件测试均被 `WAKE_TEST_REACT_VERSION` 阻塞；站点类型检查仍有 TS 7 / PnP 共享 tsconfig 解析失败，站点与组件 ESLint 仍在 TS 7 初始化时失败。不得将这些检查标记为通过。

Wake 外部文档壳层尚未完成替换。已根据用户提供的 `C:\Users\zhang\Desktop\wake` 准备 `.website/review/wake-self-hosting/wake-components.patch`，拟把搜索、导航、全屏预览等改用 Crab 组件。自动审批因跨仓库核心修改可能影响共享工具及已有改动而拒绝写入，补丁待用户明确授权；当前生产站仍使用已安装 Wake 的顶栏、搜索与基础文章样式。Wake 仓库中已补充契约草稿与先失败的组件复用测试，该新增测试在补丁实施前预期失败；不能宣称整个网站已经完全自洽。

最终生产构建通过（2m24.9s，61 条路由）。内页复验发现并修复了移除覆盖样式时遗漏的 `instructionStyle`、`actionsStyle` 定义，生成器测试已增加未定义样式引用检查。最终生产包确认 Slider 步骤加载与切换正常，Button 点击计数、重置与源码展开正常；320 px 下正文和滚动宽度均为 305 px，操作栏的图标使用 Button 自身插槽。Slider 方向键操作本轮未观察到数值变化，不能宣称其键盘调节通过，需单独核验现有 Slider 实现。开发服务已重启以刷新缓存，验收视口恢复默认，主题恢复本轮开始时的深色。

## 组件过渡与状态衔接审查（2026-09-11）

本轮修正的是组件过渡与布局稳定性：按钮遗漏的文字、边框和透明度过渡；输入组件对全部属性做动画；开关与分段滑块参数不一致；导航直接隐藏和源码直接卸载导致的突变；弹窗弹簧的残余运动；抽屉背景未锁定；重置示例重复创建 lazy 资源导致的加载占位闪动。Wake / Crab CSS 保持 0.1.38，没有修改 Wake 源码或组件 Props API。

CSS 状态反馈为 150 ms，滑块与淡入为 200 ms，源码展开为 300 ms，缓动从语义令牌统一派生。Dialog 进出场为 200 / 150 ms，Drawer 为 300 / 200 ms，均显式响应减少动画偏好。导航与源码收起时保留退场容器，同时通过 inert / aria-hidden 移除交互。选中按钮不再临时增加边框宽度。运行示例重置保留已加载的模块，只重建组件实例；明确重试加载失败时才重建 lazy 资源。

验证结果：

- `corepack yarn generate:token`：48 个任务通过。
- `corepack yarn build:library`：52 个任务通过；随后 Button、Drawer 的增量 Library 构建通过。沿用已有的 Turbo 循环依赖告警，没有将其记为无告警。
- 语义令牌包测试：2 项通过。
- 站点生成器：23 项通过；一致性检查为 52 个组件页、55 份教程、117 个教学示例、246 个唯一 Demo，0 个文件漂移。
- 最终生产构建：通过，2m26.0s，61 条路由，2502 个文件。日志见 `review/build-motion-audit-final.log`。
- 52 个组件地址 HTTP 冒烟均返回 200；这只代表路由响应，不代表逐页全部运行示例通过。
- 辅助物理路径类型检查通过：`review/motion-audit-typecheck.json` 覆盖站点适配器、LiveExample、Tutorial 与相关测试；它不替代正式 PnP 类型检查，也不代表组件库全部类型检查通过。

生产浏览器已复验：

- Button、LineEdit、Switch、Checkbox、Segmented 的实际 computed style 使用新的时长、属性列表与缓动。输入框仅对视觉属性过渡，聚焦仍为细边框与低透明度外圈，没有新增双层描边。
- Switch 连续切换与首页任务勾选、统计周期切换、通知开关结果正确。导航折叠高度归零，Tab 跳过收起项；分类展开仍由 Wake 回调和公开状态决定。
- 源码展开 / 收起按钮均为 60 px 宽；收起后源码区域高度为 0 且 inert，已经加载的源码保留，运行中的开关状态未重置。
- 搜索关闭后没有遗留打开的 dialog，滚动恢复，焦点回到搜索触发器。
- 320 px 下，移动抽屉打开前、打开后、关闭后的正文宽度均为 305 px，左边界均为 0；打开时背景 overflow 为 hidden，关闭后恢复。Escape 后焦点回到“导航”，选择 Button 后焦点进入新页面 H1。
- 首页已检查 320 / 768 / 1440 px 与浅色 / 深色主题；检查范围未出现整页横向溢出。首页重置后即时采样仍有 4 个复选框，恢复 2 / 4 状态，未再显示“正在加载当前步骤”的占位。Button 教学第二步重置恢复“已保存 0 次”；往返步骤后仍隔离状态。
- 最终浏览器控制台未发现错误。验收后恢复默认视口与深色主题。

新增回归测试覆盖源码收起时保留单个运行实例、导航收起的交互隔离、Dialog / Drawer 退出完成前保持滚动锁及卸载清理、教学重置不重复调用模块加载器。React 测试受下列既有兼容问题阻塞，不能将新增测试记为执行通过。

未通过与边界：

- 全仓 `test` 首次遇到 Yarn 对 AppData 的一次 lstat UNKNOWN 错误；随后逐包重试，12 个相关组件 / 站点 React 测试均明确被 `WAKE_TEST_REACT_VERSION` 阻塞（要求 >=19.2.8 <19.3.0，当前 19.3.0）。逐包结果见 `review/package-checks-motion-audit.json`。没有通过替换 React 版本绕过限制。
- 13 个相关包的 ESLint 仍在初始化时失败：typescript-eslint 不支持 TS 7.0。
- 正式全仓 `typecheck` 仍出现 TS6053：PnP 下共享 tsconfig 无法解析，随后产生 JSX / 模块解析错误。React 19.3.0、TypeScript 7.0.2 保持原选择。
- 本轮核对的是动画参数、起止状态和真实交互，没有测量连续帧率。最终刷新用户 Chrome 时实测 prefers-reduced-motion 为 true，CSS 过渡为 none；内置预览为 false，使用完整过渡。Chrome 的真实减少动画模式下，搜索面板正常打开、关闭，滚动锁释放且焦点恢复。没有修改用户偏好；减少动画下的全部组件、强制颜色、全部 246 个进阶 Demo、工作台 iframe、部署子路径与屏幕阅读器仍未逐一验收。

## 组件可用性与浮层视觉审查（2026-09-12）

本轮从 `/components/rc-date-picker` 的真实点击故障开始，保留 Wake / Crab CSS 0.1.38、React 19.3.0、TypeScript 7.0.2，未修改 Wake 源码、发布网站或扩展组件 Props API。

### 查明并修复的问题

- DatePicker 输入框能够挂载，但点击后生产代码抛出 `ReferenceError: centerFlexStyle is not defined`。最小构建确认：Wake 0.1.38 在替换直接 `cx(...)` 调用时可能插回已经重命名的局部标识符。静态检查同时发现表格固定列、菜单、选择器、虚拟滚动等工作台的同类引用。
- 对已确认受影响的 18 个组件包、32 个源码 / Demo 文件保留公开 `cx.call(undefined, ...)` 调用，共 118 处，不复制函数实现。批量部分先做 AST 往返一致性检查，再限定到已确认失效的文件；保留了原参数和工作区已有改动。
- 日历月份切换改用具名 Button；日期采用 36 px 点击区域，统一圆角和状态色。月份、时区正常排版，收紧头部行高，消除因几像素超高出现的滚动条。相邻月份使用可读的次级文字，选中后恢复完整对比度。
- DatePicker 提供“选择日期”占位，默认只显示年月日；DateTimePicker 保留日期时间格式。教学示例增加“计划日期”可见标签，说明选择后确认。方向键选择、Enter 确认 / 重新打开、Escape 关闭和取消后恢复原值已验证。
- DateTimePicker 窄屏改为日历与时间上下排列；三列时间均保持在视口内，长面板在浮层内纵向滚动。时间列表提供小时 / 分钟 / 秒名称，触屏无需 hover 即可滚动。
- DropdownContainer 在原生 dialog 内保留 top-layer 交互；其余情况继承最近的公开主题容器，修复主站弹层字体、焦点色与正文不一致。移除纵向压缩动画，改为 150 ms 的淡入与 4 px 位移，响应减少动画偏好。边界和阴影移到滚动外层，避免白色弹层轮廓被裁掉；空间不足时优先向上展开。
- 两个 DropdownContainer 教学示例原先只渲染 Button，没有连接 refs 与开关回调。现通过 `useDropdownContext` 正确连接定位、开关和 Escape；收藏示例确实更新结果。
- Drawer 的标题通过 useId 与 dialog 建立可访问名称关联，仍允许调用方显式提供名称。

### 实际验证结果

- 全仓 Library 构建：52 个任务通过；最后 DatePicker、DropdownContainer、Drawer 的增量 Library 构建通过。令牌均由包内 `generate:token` 生成，没有手改产物。沿用既有 Turbo 循环依赖告警。
- 生成器测试：23 项通过。构建产物检查的回归用例：5 项通过。
- 一致性：52 个组件页、55 份教程、117 个教学示例文件、246 个唯一进阶 Demo，0 个漂移文件。
- 最终生产构建通过：2m27.6s，61 条路由，2503 个文件。随后检查 1867 个生产 JavaScript 文件，未定义引用为 0。日志：`review/build-availability-production.log`；报告：`docs-dist/binding-check.json`。
- `build` 现在必须执行 `check:build`。该检查包含主站和工作台输出，允许依赖中受保护的 CommonJS / AMD、DevTools 及旧 IE 宿主分支标识符，不等同于完整运行时验证。
- 辅助类型检查通过：`review/availability-typecheck.json` 覆盖日期源码、新日历测试、日期 / 下拉教程和既有站点适配器；使用物理路径诊断，不替代正式 PnP 类型检查。
- HTTP 冒烟：52 个组件地址和 52 个旧工作台地址全部返回 200，见 `review/routes-availability.json`。

浏览器实测：

- 逐页遍历 52 个组件页和 104 个组件教学步骤，切换并重置，没有错误界面。三个实战的 12 个步骤也正常挂载；117 个教学示例文件还包含首页示例，不能把文件数当作教学步骤数。
- DatePicker：打开、选择、确认、取消、重新打开、方向键与 Enter；日期 / 日期时间 / 时间工作台实际展开并确认有效结果。日期时间在 320 px 下能访问三列时间并完成选择。
- ColorPicker：打开并显示颜色控件；Select：展开并选择上海；CronPicker：切换分钟规则与小时页签，表达式更新；Dialog / Drawer：打开与 Escape 退出；Message / Notification：真实提示内容出现。
- DropdownContainer：基础说明真正出现、Escape 关闭；第二步收藏后输出“已收藏”。Table 工作台：双击进入行编辑、修改姓名、确认保存、再次编辑后取消，均未出现引用错误。
- DatePicker 检查浅 / 深主题及 320 / 768 / 1440 px。320 px 下页面 scrollWidth 为 305 px，日历浮层宽 278 px；768 px 下页面 scrollWidth 为 753 px。浅色最终浮层 clientHeight / scrollHeight 均为 376 px，阴影完整、没有多余滚动条。跨月选中项为深底白字。
- 最终浏览器审查时段没有新增控制台错误。预览尺寸和深色主题已恢复。

### 未通过与边界

- 20 个相关组件包及站点的 React 测试仍被 `WAKE_TEST_REACT_VERSION` 拦截：Wake 要求 >=19.2.8 <19.3.0，当前为 19.3.0。新增日历和抽屉 / 主题回归测试不能记为执行通过。逐包日志见 `review/rc-*-test-availability.log`。
- 正式 ESLint 仍在初始化阶段失败：typescript-eslint 不支持 TS 7.0。正式站点 typecheck 仍受 PnP 共享 tsconfig 解析问题影响，伴随 JSX / 模块错误；没有将辅助检查当成正式检查通过。
- 未穷尽 246 个进阶 Demo 的全部交互、屏幕阅读器、强制颜色、部署子路径和连续帧率；时间列的完整键盘操作也未做专门验收。本轮区分页面挂载、实际交互抽检和生产静态检查，不宣称全绿。
